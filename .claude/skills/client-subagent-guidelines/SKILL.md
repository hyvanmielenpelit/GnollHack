---
name: subagent-guidelines
description: Guidelines for using AI subagents and human pair programming in GnollHack implementation plans. Covers when to spawn subagents, model selection defaults, and planning constraints such as file-level exclusivity.
---

The full skill lives in this repository's tool-neutral agent directory (`.agents/`),
which is shared with other AI coding agents. This file is only a pointer.

Read `.agents/skills/subagent_guidelines/SKILL.md` (path relative to the repository root) in full
before proceeding, and follow it. Any `references/` files it links are relative to that
same directory.
