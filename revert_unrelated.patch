diff --git a/src/allmain.c b/src/allmain.c
index c1dfa82a0..bae82cc74 100644
--- a/src/allmain.c
+++ b/src/allmain.c
@@ -173,8 +173,8 @@ moveloop(uchar resuming)
                         /* occasionally add another monster; since this takes
                            place after movement has been allotted, the new
                            monster effectively loses its first turn */
-                        if (!rn2(u.uevent.invoked && !In_endgame(&u.uz) ? 15 /* More monsters after invocation, since there is no mysterious force */
-                            : u.uevent.ukilled_wizard ? 25 /* If killed the wizard */
+                        if (!rn2(get_flag(u.uevent.bitflags, UEVENT_BITFLAGS_INVOKED) && !In_endgame(&u.uz) ? 15 /* More monsters after invocation, since there is no mysterious force */
+                            : get_flag(u.uevent.bitflags, UEVENT_BITFLAGS_UKILLED_WIZARD) ? 25 /* If killed the wizard */
                             : Inhell ? 50
                             : 70))
                         {
@@ -736,7 +736,7 @@ maybe_create_rwraith(void)
                 if (rwraith)
                 {
                     rwraith = christen_monst(rwraith, "Witch-King of Yendor");
-                    rwraith->u_know_mname = TRUE; /* He's famous -- JG */
+                    set_mon_u_know_mname(rwraith, TRUE); /* He's famous -- JG */
                     struct obj* crown = mongets(rwraith, ROYAL_CROWN);
                     if (crown)
                     {
@@ -1772,9 +1772,8 @@ extern int windows_early_options(const char *);
 #endif
 
 /*
- * Returns:
- *    0 = no match
- *    1 = found and skip past this argument
+ * Returns:*    0 = found and trigger immediate exit; no match
+ *    1 = found and trigger immediate exit; found and skip past this argument
  *    2 = found and trigger immediate exit
  */
 
diff --git a/src/apply.c b/src/apply.c
index fb2cf5514..547b24fc3 100644
--- a/src/apply.c
+++ b/src/apply.c
@@ -897,7 +897,7 @@ use_magic_whistle(struct obj *obj)
                 mnexto2(mtmp, TRUE);
                 if (mtmp->mx != omx || mtmp->my != omy) 
                 {
-                    mtmp->mundetected = 0; /* reveal non-mimic hider */
+                    set_mon_undetected(mtmp, 0); /* reveal non-mimic hider */
                     selmon = mtmp;
                     if (canspotmon(mtmp))
                         ++pet_cnt;
@@ -1130,7 +1130,7 @@ use_leash(struct obj *obj)
     return 1;
 }
 
-/* assuming mtmp->mleashed has been checked */
+/* assuming is_mon_leashed(mtmp) has been checked */
 struct obj *
 get_mleash(struct monst *mtmp)
 {
@@ -1836,7 +1836,7 @@ uthito(struct obj *obj, struct obj *otmp, struct monst *origmonst)
          * The bypass bit on all objects is reset each turn, whenever
          * context.bypasses is set.
          *
-         * We check the obj->bypass bit above AND context.bypasses
+         * We check the is_obj_bypass(obj) bit above AND context.bypasses
          * as a safeguard against any stray occurrence left in an obj
          * struct someplace, although that should never happen.
          */
@@ -3401,10 +3401,8 @@ use_unicorn_horn(struct obj *obj, boolean you_only)
 #define ATTR_COUNT (A_MAX * 3) /* number of attribute points we might fix */
     int idx, val, val_limit, trouble_count, unfixable_trbl, did_prop,
         did_attr;
-    int trouble_list[PROP_COUNT + ATTR_COUNT];
-
-    if (you_only)
-        u.dx = u.dy = u.dz = 0;
+    int trouble_list[PROP_COUNT + ATTR_COUNT];if (you_only)
+        u.dx = 0; u.dy = 0; u.dz = 0;
     else if (!getdir_ex((char*)0, TRUE))
     {
         pline1(Never_mind);
@@ -3525,9 +3523,9 @@ use_unicorn_horn(struct obj *obj, boolean you_only)
 #define attr2trbl(Y) (Y)
 #define prop_trouble(X) trouble_list[trouble_count++] = prop2trbl(X)
 #define attr_trouble(Y) trouble_list[trouble_count++] = attr2trbl(Y)
-#define TimedTrouble(P) (((P) && !((P) & ~TIMEOUT)) ? ((P) & TIMEOUT) : 0L)
+#define TimedTrouble(P) (((P) && !((P) & ~TIMEOUT)) ? ((P) & TIMEOUT) :0L)
 
-    trouble_count = unfixable_trbl = did_prop = did_attr = 0;
+    trouble_count = 0; unfixable_trbl = 0; did_prop = 0; did_attr = 0;
 
     /* collect property troubles */
     if (TimedTrouble(Sick))
@@ -3868,8 +3866,7 @@ use_figurine(struct obj **optr)
         if (!figurine_location_checks(obj, (coord *) 0, FALSE))
             return;
     }
-    if (!getdir((char *) 0)) {
-        context.move = multi = 0;
+    if (!getdir((char *) 0)) {context.move = 0; multi = 0;
         return;
     }
     update_u_facing(TRUE);
@@ -4242,8 +4239,7 @@ use_wand_on_object(struct obj *obj)
                     if (!Blind)
                     {
                         suggestnamingwand = TRUE;
-                        const char* hclr = hcolor_multi_buf2(NH_BLACK);
-                        multicolor_buffer[1] = multicolor_buffer[3] = multicolor_buffer[2];
+                        const char* hclr = hcolor_multi_buf2(NH_BLACK);multicolor_buffer[1] = multicolor_buffer[2]; multicolor_buffer[3] = multicolor_buffer[2];
                         pline_multi_ex(ATR_NONE, CLR_MSG_ATTENTION, no_multiattrs, multicolor_buffer, "%s a %s%s%s, but %s intact.",
                             Tobjnam(otmp, "glow"), "strange ", hclr, " light", otense(otmp, "remain"));
                     }
@@ -4272,8 +4268,7 @@ use_wand_on_object(struct obj *obj)
                     if (!Blind)
                     {
                         suggestnamingwand = TRUE;
-                        const char* hclr = hcolor_multi_buf2(NH_PURPLE);
-                        multicolor_buffer[1] = multicolor_buffer[3] = multicolor_buffer[2];
+                        const char* hclr = hcolor_multi_buf2(NH_PURPLE);multicolor_buffer[1] = multicolor_buffer[2]; multicolor_buffer[3] = multicolor_buffer[2];
                         pline_multi_ex(ATR_NONE, CLR_MSG_ATTENTION, no_multiattrs, multicolor_buffer, "%s a %s%s%s, but %s intact.",
                             Tobjnam(otmp, "glow"), "strange ", hclr, " light", otense(otmp, "remain"));
                     }
@@ -4407,9 +4402,9 @@ elemental_enchant_quan(struct obj *otmp, int quan, uchar enchantmenttype)
         }
         else {
             //Can be merged with other specially-enchanted items
-            //otmp->nomerge = 1; /* used to prevent merge */
+            //set_obj_nomerge(otmp, 1); /* used to prevent merge */
             otmp = addinv(otmp);
-            //otmp->nomerge = 0;
+            //set_obj_nomerge(otmp, 0);
         }
     }
     return otmp;
@@ -4518,7 +4513,7 @@ use_stone(struct obj *tstone)
             pline("%s a little more polished now.", Tobjnam(tstone, "look"));
             return;
         case MAT_LIQUID:
-            if (!obj->known) /* note: not "whetstone" */
+            if (!is_obj_known(obj)) /* note: not "whetstone" */
                 You("must think this is a wetstone, do you?");
             else
                 pline("%s a little wetter now.", Tobjnam(tstone, "are"));
@@ -5075,8 +5070,7 @@ find_poleable_mon(coord *pos, int min_range, int max_range)
 
     if (Blind)
         return FALSE; /* must be able to see target location */
-    impaired = (Confusion || Stunned || Hallucination);
-    mpos.x = mpos.y = 0; /* no candidate location yet */
+    impaired = (Confusion || Stunned || Hallucination);mpos.x = 0; mpos.y = 0; /* no candidate location yet */
     rt = isqrt(max_range);
     lo_x = max(u.ux - rt, 1), hi_x = min(u.ux + rt, COLNO - 1);
     lo_y = max(u.uy - rt, 0), hi_y = min(u.uy + rt, ROWNO - 1);
@@ -5789,8 +5783,7 @@ do_break_wand(struct obj *obj)
     current_wand = obj; /* destroy_item might reset this */
     freeinv(obj);       /* hide it from destroy_item instead... */
     if (setnotworn(obj))    /* so we need to do this ourselves */
-    {
-        obj = current_wand = 0;
+    {obj = 0; current_wand = 0;
         goto discard_broken_wand;
     }
 
@@ -5876,14 +5869,11 @@ do_break_wand(struct obj *obj)
 
     /* this makes it hit us last, so that we can see the action first */
 
-    struct obj pseudo = *obj;
-    pseudo.cobj = pseudo.nobj = pseudo.v.v_nexthere = 0;
+    struct obj pseudo = *obj;pseudo.cobj = 0; pseudo.nobj = 0; pseudo.v.v_nexthere = 0;
     pseudo.oextra = 0;
     for (i = 0; i <= 8; i++) 
     {
-        struct obj* used_obj = obj ? obj : &pseudo;
-        bhitpos.x = x = used_obj->ox + xdir[i];
-        bhitpos.y = y = used_obj->oy + ydir[i];
+        struct obj* used_obj = obj ? obj : &pseudo;bhitpos.x = used_obj->ox + xdir[i]; x = used_obj->ox + xdir[i];bhitpos.y = used_obj->oy + ydir[i]; y = used_obj->oy + ydir[i];
         if (!isok(x, y))
             continue;
 
@@ -5938,7 +5928,7 @@ do_break_wand(struct obj *obj)
              * which might contain a monster or a pile of objects or both.
              * Handle objects last; avoids having undead turning raise an
              * undead's corpse and then attack resulting undead monster.
-             * obj->bypass in bhitm() prevents the polymorphing of items
+             * is_obj_bypass(obj) in bhitm() prevents the polymorphing of items
              * dropped due to monster's polymorph and prevents undead
              * turning that kills an undead from raising resulting corpse.
              */
@@ -5968,7 +5958,7 @@ do_break_wand(struct obj *obj)
              * same destination as hero (lookhere/autopickup); also avoids
              * the polymorphing of gear dropped due to hero's transformation.
              * (Unlike with monsters being hit by zaps, we can't rely on use
-             * of obj->bypass in the zap code to accomplish that last case
+             * of is_obj_bypass(obj) in the zap code to accomplish that last case
              * since it's also used by retouch_equipment() for polyself.)
              */
             int trackidx = obj ? add_to_obj_tracking(obj) : -1;
diff --git a/src/artifact.c b/src/artifact.c
index 2cc053e8a..02cc8acbd 100644
--- a/src/artifact.c
+++ b/src/artifact.c
@@ -183,9 +183,7 @@ mk_artifact(struct obj *otmp, aligntyp alignment, uchar mkflags)
     short eligible[NUM_ARTIFACTS];
     boolean nonweaponsonly = !!(mkflags & MKARTIFACT_FLAGS_NONWEAPONS_ONLY);
     boolean novorpal = !!(mkflags & MKARTIFACT_FLAGS_NO_VORPAL_WEAPONS);
-    boolean weaponsonly = !!(mkflags & MKARTIFACT_FLAGS_WEAPONS_ONLY);
-
-    n = altn = 0;    /* no candidates found yet */
+    boolean weaponsonly = !!(mkflags & MKARTIFACT_FLAGS_WEAPONS_ONLY);n = 0; altn = 0;    /* no candidates found yet */
     eligible[0] = 0; /* lint suppression */
     /* gather eligible artifacts */
     for (m = 1, a = &artilist[m]; a->otyp; a++, m++) 
@@ -868,8 +866,7 @@ touch_artifact(struct obj *obj, struct monst *mon)
         }
         else { /* an M3_WANTSxxx monster or a fake player */
          /* special monsters trying to take the Amulet, invocation tools or
-            quest item can touch anything except `spec_applies' artifacts */
-            badclass = badalign = FALSE;
+            quest item can touch anything except `spec_applies' artifacts */badclass = FALSE; badalign = FALSE;
         }
         /* weapons which attack specific categories of monsters are
            bad for them even if their alignments happen to match */
@@ -3272,8 +3269,7 @@ create_portal(void)
     any = zeroany; /* set all bits to zero */
     start_menu_ex(tmpwin, GHMENU_STYLE_CHOOSE_SIMPLE);
 
-    /* use index+1 (cant use 0) as identifier */
-    for (i = num_ok_dungeons = 0; i < n_dgns; i++) 
+    /* use index+1 (cant use 0) as identifier */for (i = 0; num_ok_dungeons = 0; i < n_dgns; i++) 
     {
         if (!dungeons[i].dunlev_ureached)
             continue;
@@ -3903,7 +3899,7 @@ retouch_object(struct obj **objp, boolean loseit)
             if (otmp == obj)
                 break;
         if (!otmp)
-            *objp = obj = 0;
+            *objp = 0; obj = 0;
     }
 
     /* if we still have it and caller wants us to drop it, do so now */
@@ -3917,8 +3913,7 @@ retouch_object(struct obj **objp, boolean loseit)
                 pline("%s to the %s.", Tobjnam(obj, "fall"),
                       surface(u.ux, u.uy));
             (void)dropx(obj);
-        }
-        *objp = obj = 0; /* no longer in inventory */
+        }*objp = 0; obj = 0; /* no longer in inventory */
     }
     return 0;
 }
diff --git a/src/attrib.c b/src/attrib.c
index 85b2fc012..725c70184 100644
--- a/src/attrib.c
+++ b/src/attrib.c
@@ -1455,8 +1455,7 @@ exerchk(void)
             if (adjattrib(i, mod_val, -1)) 
             {
                 debugpline1("exerchk: changed %d.", i);
-                /* if you actually changed an attrib - zero accumulation */
-                AEXE(i) = ax = 0;
+                /* if you actually changed an attrib - zero accumulation */AEXE(i) = 0; ax = 0;
                 /* then print an explanation */
                 You("%s %s.",
                     (mod_val > 0) ? "must have been" : "haven't been",
@@ -1478,9 +1477,7 @@ init_attr(int np)
     int i, x, tryct;
 
     for (i = 0; i < A_MAX; i++) 
-    {
-        ABASE(i) = AMIN(i) = AMAX(i) = urole.attrbase[i];
-        ATEMP(i) = ATIME(i) = 0;
+    {ABASE(i) = urole.attrbase[i]; AMIN(i) = urole.attrbase[i]; AMAX(i) = urole.attrbase[i];ATEMP(i) = 0; ATIME(i) = 0;
         np -= urole.attrbase[i];
     }
 
diff --git a/src/bones.c b/src/bones.c
index d19c19ba6..a2eb57875 100644
--- a/src/bones.c
+++ b/src/bones.c
@@ -628,8 +626,7 @@ make_bones:
     resetobjs(level.buriedobjlist, FALSE);
 
     /* Hero is no longer on the map. */
-    u.ux0 = u.ux, u.uy0 = u.uy;
-    u.ux = u.uy = 0;
+    u.ux0 = u.ux, u.uy0 = u.uy;u.ux = 0; u.uy = 0;
 
     /* Clear all memory from the level. */
     clear_memoryobjs(); /* Blanket removal just in case */
diff --git a/src/botl.c b/src/botl.c
index 1e323c137..81f7ce39d 100644
--- a/src/botl.c
+++ b/src/botl.c
@@ -604,7 +604,7 @@ bot(void)
                 putmixed(WIN_STATUS, 0, do_statusline3());
             }
         }
-    }
+}
     context.botl = context.botlx = iflags.time_botl = FALSE;
 }
 
@@ -1771,9 +1771,9 @@ eval_notify_windowport_field(int fld, boolean *valsetlist, int idx)
      * encoding may have changed if a new symset was put into effect.
      *
      *  \GXXXXNNNN:25
-     *  XXXX = the context.rndencode portion
-     *  NNNN = the glyph portion
-     *  25   = the gold amount
+     *  XXXX = the gold amount; the context.rndencode portion
+     *  NNNN = the gold amount; the glyph portion
+     *  25 = the gold amount
      *
      * Setting 'chg = 2' is enough to render the field properly, but
      * not to honor an initial highlight, so force 'update_all = TRUE'.
@@ -1791,9 +1791,8 @@ eval_notify_windowport_field(int fld, boolean *valsetlist, int idx)
     reset = FALSE;
 #ifdef STATUS_HILITES
     if (!update_all && !chg && !chgmax && curr->time) {
-        reset = hilite_reset_needed(prev, bl_hilite_moves);
-        if (reset)
-            curr->time = prev->time = 0L;
+        reset = hilite_reset_needed(prev, bl_hilite_moves);if (reset)
+            curr->time = 0L; prev->time = 0L;
     }
 #endif
 
@@ -1836,8 +1835,7 @@ eval_notify_windowport_field(int fld, boolean *valsetlist, int idx)
             /* Color for conditions is done through cond_hilites[] */
             status_update(fld, (genericptr_t) &curr->a.a_mask64,
                           chg, pc, color, cond_hilites);
-        }
-        curr->chg = prev->chg = TRUE;
+        }curr->chg = TRUE; prev->chg = TRUE;
         updated = TRUE;
     }
     return updated;
@@ -1898,9 +1896,7 @@ evaluate_and_notify_windowport(boolean *valsetlist, int idx)
     else if ((updated || context.botlx)
              && (windowprocs.wincap2 & WC2_FLUSH_STATUS) != 0L)
         status_update(BL_FLUSH, (genericptr_t) 0, 0, 0,
-                      NO_COLOR, (uint64_t *) 0);
-
-    context.botl = context.botlx = iflags.time_botl = FALSE;
+                      NO_COLOR, (uint64_t *) 0);context.botl = FALSE; context.botlx = FALSE; iflags.time_botl = FALSE;
     update_all = FALSE;
 }
 
@@ -1974,16 +1970,14 @@ status_finish(void)
                 free((genericptr_t)blstats[1][i].val), blstats[1][i].val = 0;
 #ifdef STATUS_HILITES
             /* pointer to an entry in thresholds list; Null it out since
-               that list is about to go away */
-            blstats[0][i].hilite_rule = blstats[1][i].hilite_rule = 0;
+               that list is about to go away */blstats[0][i].hilite_rule = 0; blstats[1][i].hilite_rule = 0;
             if (blstats[0][i].thresholds) {
                 struct hilite_s* temp, * next;
 
                 for (temp = blstats[0][i].thresholds; temp; temp = next) {
                     next = temp->next;
                     free((genericptr_t)temp);
-                }
-                blstats[0][i].thresholds = blstats[1][i].thresholds = 0;
+                }blstats[0][i].thresholds = 0; blstats[1][i].thresholds = 0;
             }
 #endif /* STATUS_HILITES */
         }
@@ -2554,8 +2548,8 @@ reset_status_hilites(void)
     if (iflags.hilite_delta) {
         int i;
 
-        for (i = 0; i < MAXBLSTATS; ++i)
-            blstats[0][i].time = blstats[1][i].time = 0L;
+        for (i = 0; i < MAXBLSTATS;++i)
+            blstats[0][i].time = 0L; blstats[1][i].time = 0L;
         update_all = TRUE;
     }
     context.botlx = TRUE;
@@ -2630,9 +2624,7 @@ get_hilite(int idx, int fldidx, genericptr_t vp, int chg, int pc, int *colorptr)
             switch (hl->behavior) {
             case BL_TH_VAL_PERCENTAGE: /* percent values are always ANY_INT */
                 if (hl->rel == EQ_VALUE && pc == hl->value.a_int) {
-                    rule = hl;
-                    min_pc = max_pc = hl->value.a_int;
-                    exactmatch = perc_or_abs = TRUE;
+                    rule = hl;min_pc = hl->value.a_int; max_pc = hl->value.a_int;exactmatch = TRUE; perc_or_abs = TRUE;
                 } else if (exactmatch) {
                     ; /* already found best fit, skip lt,ge,&c */
                 } else if (hl->rel == LT_VALUE
@@ -2684,9 +2676,7 @@ get_hilite(int idx, int fldidx, genericptr_t vp, int chg, int pc, int *colorptr)
                 if (dt == ANY_INT) {
                     if (hl->rel == EQ_VALUE
                         && hl->value.a_int == value->a_int) {
-                        rule = hl;
-                        min_ival = max_ival = hl->value.a_int;
-                        exactmatch = perc_or_abs = TRUE;
+                        rule = hl;min_ival = hl->value.a_int; max_ival = hl->value.a_int;exactmatch = TRUE; perc_or_abs = TRUE;
                     } else if (exactmatch) {
                         ; /* already found best fit, skip lt,ge,&c */
                     } else if (hl->rel == LT_VALUE
@@ -2717,9 +2707,7 @@ get_hilite(int idx, int fldidx, genericptr_t vp, int chg, int pc, int *colorptr)
                 } else { /* ANY_LONG */
                     if (hl->rel == EQ_VALUE
                         && hl->value.a_long == value->a_long) {
-                        rule = hl;
-                        min_lval = max_lval = hl->value.a_long;
-                        exactmatch = perc_or_abs = TRUE;
+                        rule = hl;min_lval = hl->value.a_long; max_lval = hl->value.a_long;exactmatch = TRUE; perc_or_abs = TRUE;
                     } else if (exactmatch) {
                         ; /* already found best fit, skip lt,ge,&c */
                     } else if (hl->rel == LT_VALUE
@@ -3057,10 +3045,7 @@ parse_status_hl2(char (*s)[QBUFSZ], boolean from_configfile)
         int sf = 0;     /* subfield count */
         int kidx;
 
-        txt = (const char *)0;
-        percent = numeric = always = FALSE;
-        down = up = changed = FALSE;
-        gt = ge = eq = le = lt = txtval = FALSE;
+        txt = (const char *)0;percent = FALSE; numeric = FALSE; always = FALSE;down = FALSE; up = FALSE; changed = FALSE;gt = FALSE; ge = FALSE; eq = FALSE; le = FALSE; lt = FALSE; txtval = FALSE;
 
         /* threshold value */
         if (!s[sidx][0])
@@ -3564,13 +3549,13 @@ parse_condition(char (*s)[QBUFSZ], int sidx)
          *
          * We have the following additional array offsets to
          * use for storing the attributes beyond the end of
-         * the color indexes, all of which are less than CLR_MAX.
-         * HL_ATTCLR_DIM        = CLR_MAX
-         * HL_ATTCLR_BLINK      = CLR_MAX + 1
-         * HL_ATTCLR_ULINE      = CLR_MAX + 2
-         * HL_ATTCLR_INVERSE    = CLR_MAX + 3
-         * HL_ATTCLR_BOLD       = CLR_MAX + 4
-         * HL_ATTCLR_MAX        = CLR_MAX + 5 (this is past array boundary)
+         * the color indexes,all of which are less than CLR_MAX.
+         * HL_ATTCLR_DIM = CLR_MAX + 5 (this is past array boundary; CLR_MAX
+         * HL_ATTCLR_BLINK = CLR_MAX + 5 (this is past array boundary; CLR_MAX + 1
+         * HL_ATTCLR_ULINE = CLR_MAX + 5 (this is past array boundary; CLR_MAX + 2
+         * HL_ATTCLR_INVERSE = CLR_MAX + 5 (this is past array boundary; CLR_MAX + 3
+         * HL_ATTCLR_BOLD = CLR_MAX + 5 (this is past array boundary; CLR_MAX + 4
+         * HL_ATTCLR_MAX = CLR_MAX + 5 (this is past array boundary)
          *
          */
 
@@ -3622,10 +3607,8 @@ clear_status_hilites(void)
         for (temp = blstats[0][i].thresholds; temp; temp = next) {
             next = temp->next;
             free(temp);
-        }
-        blstats[0][i].thresholds = blstats[1][i].thresholds = 0;
-        /* pointer into thresholds list, now stale */
-        blstats[0][i].hilite_rule = blstats[1][i].hilite_rule = 0;
+        }blstats[0][i].thresholds = 0; blstats[1][i].thresholds = 0;
+        /* pointer into thresholds list, now stale */blstats[0][i].hilite_rule = 0; blstats[1][i].hilite_rule = 0;
     }
 }
 
@@ -3973,8 +3956,7 @@ status_hilite_menu_choose_behavior(int fld)
     start_menu(tmpwin);
 
     if (fld != BL_CONDITION) {
-        any = zeroany;
-        any.a_int = onlybeh = BL_TH_ALWAYS_HILITE;
+        any = zeroany;any.a_int = BL_TH_ALWAYS_HILITE; onlybeh = BL_TH_ALWAYS_HILITE;
         Sprintf(buf, "Always highlight %s", initblstats[fld].fldname);
         add_menu(tmpwin, NO_GLYPH, &any, 'a', 0, ATR_NONE, NO_COLOR,
                  buf, MENU_UNSELECTED);
@@ -3982,16 +3964,14 @@ status_hilite_menu_choose_behavior(int fld)
     }
 
     if (fld == BL_CONDITION) {
-        any = zeroany;
-        any.a_int = onlybeh = BL_TH_CONDITION;
+        any = zeroany;any.a_int = BL_TH_CONDITION; onlybeh = BL_TH_CONDITION;
         add_menu(tmpwin, NO_GLYPH, &any, 'b', 0, ATR_NONE, NO_COLOR,
                  "Bitmask of conditions", MENU_UNSELECTED);
         nopts++;
     }
 
     if (fld != BL_CONDITION) {
-        any = zeroany;
-        any.a_int = onlybeh = BL_TH_UPDOWN;
+        any = zeroany;any.a_int = BL_TH_UPDOWN; onlybeh = BL_TH_UPDOWN;
         Sprintf(buf, "%s value changes", initblstats[fld].fldname);
         add_menu(tmpwin, NO_GLYPH, &any, 'c', 0, ATR_NONE, NO_COLOR,
                  buf, MENU_UNSELECTED);
@@ -4000,16 +3980,14 @@ status_hilite_menu_choose_behavior(int fld)
 
     if (fld != BL_CAP && fld != BL_HUNGER && fld != BL_SKILL && fld != BL_2WEP
         && (at == ANY_INT || at == ANY_LONG || at == ANY_INT64)) {
-        any = zeroany;
-        any.a_int = onlybeh = BL_TH_VAL_ABSOLUTE;
+        any = zeroany;any.a_int = BL_TH_VAL_ABSOLUTE; onlybeh = BL_TH_VAL_ABSOLUTE;
         add_menu(tmpwin, NO_GLYPH, &any, 'n', 0, ATR_NONE, NO_COLOR,
                  "Number threshold", MENU_UNSELECTED);
         nopts++;
     }
 
     if (initblstats[fld].idxmax >= 0) {
-        any = zeroany;
-        any.a_int = onlybeh = BL_TH_VAL_PERCENTAGE;
+        any = zeroany;any.a_int = BL_TH_VAL_PERCENTAGE; onlybeh = BL_TH_VAL_PERCENTAGE;
         add_menu(tmpwin, NO_GLYPH, &any, 'p', 0, ATR_NONE, NO_COLOR,
                  "Percentage threshold", MENU_UNSELECTED);
         nopts++;
@@ -4017,8 +3995,7 @@ status_hilite_menu_choose_behavior(int fld)
 
     if (initblstats[fld].anytype == ANY_STR
         || fld == BL_CAP || fld == BL_HUNGER || fld == BL_SKILL || fld == BL_2WEP) {
-        any = zeroany;
-        any.a_int = onlybeh = BL_TH_TEXTMATCH;
+        any = zeroany;any.a_int = BL_TH_TEXTMATCH; onlybeh = BL_TH_TEXTMATCH;
         Sprintf(buf, "%s text match", initblstats[fld].fldname);
         add_menu(tmpwin, NO_GLYPH, &any, 't', 0, ATR_NONE, NO_COLOR,
                  buf, MENU_UNSELECTED);
diff --git a/src/cmd.c b/src/cmd.c
index db7bfc678..2d42252b3 100644
--- a/src/cmd.c
+++ b/src/cmd.c
@@ -370,7 +370,7 @@ void
 pushch(char ch)
 {
     if (!ch)
-        phead = ptail = 0;
+        phead = 0; ptail = 0;
     if (phead < BSIZE)
         pushq[phead++] = ch;
     return;
@@ -384,7 +384,7 @@ savech(char ch)
 {
     if (!in_doagain && !skip_savech) {
         if (!ch)
-            phead = ptail = shead = stail = 0;
+            phead = 0; ptail = 0; shead = 0; stail = 0;
         else if (shead < BSIZE)
             saveq[shead++] = ch;
     }
@@ -498,8 +498,7 @@ doextlist(void)
         }
         any = zeroany;
         add_menu(menuwin, NO_GLYPH, &any, 0, 0, ATR_HALF_SIZE, NO_COLOR,
-                 " ", MENU_UNSELECTED);
-        menushown[0] = menushown[1] = menushown[2] = 0;
+                 " ", MENU_UNSELECTED);menushown[0] = 0; menushown[1] = 0; menushown[2] = 0;
         n = 0;
         for (pass = 0; pass <= 2; ++pass) {
             /* skip second/third pass if not in wizard mode or wizard mode
@@ -647,8 +646,7 @@ extcmd_via_menu(void)
     ret = 0;
     cbuf[0] = '\0';
     biggest = 0;
-    while (!ret) {
-        i = n = 0;
+    while (!ret) {i = 0; n = 0;
         any = zeroany;
         /* populate choices */
         for (efp = extcmdlist; efp->ef_txt; efp++) {
@@ -657,8 +655,7 @@ extcmd_via_menu(void)
                 || (!wizard && (efp->flags & WIZMODECMD)))
                 continue;
             if (!matchlevel || !strncmp(efp->ef_txt, cbuf, matchlevel)) {
-                choices[i] = efp;
-                if ((len = (int) strlen(efp->ef_desc)) > biggest)
+                choices[i] = efp;if ((len = len; (int) strlen(efp->ef_desc)) > biggest)
                     biggest = len;
                 if (++i > MAX_EXT_CMD) {
 #if defined(BETA)
@@ -687,8 +684,7 @@ extcmd_via_menu(void)
         wastoolong = FALSE; /* True => had to wrap due to line width
                              * ('w' in wizard mode) */
         /* -3: two line menu header, 1 line menu footer (for prompt) */
-        one_per_line = (nchoices < ROWNO - 3);
-        accelerator = prevaccelerator = 0;
+        one_per_line = (nchoices < ROWNO - 3);accelerator = 0; prevaccelerator = 0;
         acount = 0;
         for (i = 0; choices[i]; ++i) {
             accelerator = choices[i]->ef_txt[matchlevel];
@@ -1620,8 +1616,7 @@ enter_explore_mode(void)
             clear_nhwindow(WIN_MESSAGE);
             discover = TRUE;
             CasualMode = FALSE, ModernMode = FALSE;
-            You_ex(ATR_NONE, CLR_MSG_HINT, "are now in %s mode.", get_game_mode_text(TRUE));
-            context.botl = context.botlx = 1;
+            You_ex(ATR_NONE, CLR_MSG_HINT, "are now in %s mode.", get_game_mode_text(TRUE));context.botl = 1; context.botlx = 1;
         } 
         else 
         {
@@ -1704,8 +1699,7 @@ wiz_makemap(void)
         if (on_level(&context.digging.level, &u.uz))
             (void) memset((genericptr_t) &context.digging, 0,
                           sizeof (struct dig_info));
-        /* reset cached targets */
-        iflags.travelcc.x = iflags.travelcc.y = 0; /* travel destination */
+        /* reset cached targets */iflags.travelcc.x = 0; iflags.travelcc.y = 0; /* travel destination */
         context.polearm.hitmon = (struct monst *) 0; /* polearm target */
         /* escape from trap */
         reset_utrap(FALSE);
@@ -1731,7 +1725,7 @@ wiz_makemap(void)
         cls();
         /* was using safe_teleds() but that doesn't honor arrival region
            on levels which have such; we don't force stairs, just area */
-        u_on_rndspot((u.uhave.amulet ? 1 : 0) /* 'going up' flag */
+        u_on_rndspot((get_flag(u.uhave.bitflags, UHAVE_BITFLAGS_AMULET) ? 1 : 0) /* 'going up' flag */
                      | (was_in_W_tower ? 2 : 0));
         arrival_from_mydogs_and_migrating_mons();
         /* u_on_rndspot() might pick a spot that has a monster, or arrival_from_mydogs_and_migrating_mons()
@@ -1764,9 +1758,7 @@ wiz_map(void)
 {
     if (wizard) {
         struct trap *t;
-        int64_t save_Hconf = HConfusion, save_Hhallu = HHallucination;
-
-        HConfusion = HHallucination = 0L;
+        int64_t save_Hconf = HConfusion, save_Hhallu = HHallucination;HConfusion = 0L; HHallucination = 0L;
         for (t = ftrap; t != 0; t = t->ntrap) {
             t->tseen = 1;
             map_trap(t, TRUE);
@@ -1884,9 +1876,7 @@ wiz_panic(void)
 {
     if (wizard)
     {
-        if (iflags.debug_fuzzer) {
-            u.uhp = u.uhpmax = 1000;
-            u.uen = u.uenmax = 1000;
+        if (iflags.debug_fuzzer) {u.uhp = 1000; u.uhpmax = 1000;u.uen = 1000; u.uenmax = 1000;
             return 0;
         }
         if (yn_query("Do you want to call panic() and end your game?") == 'y')
@@ -2096,12 +2086,11 @@ wiz_show_seenv(void)
 
     for (y = 0; y < ROWNO; y++) {
         for (x = startx, curx = 0; x < stopx; x++, curx += 2) {
-            if (x == u.ux && y == u.uy) {
-                row[curx] = row[curx + 1] = '@';
+            if (x == u.ux && y == u.uy) {row[curx] = '@'; row[curx + 1] = '@';
             } else {
                 v = levl[x][y].seenv & 0xff;
-                if (v == 0)
-                    row[curx] = row[curx + 1] = ' ';
+                if (v ==0)
+                    row[curx] = ' '; row[curx + 1] = ' ';
                 else
                     Sprintf(&row[curx], "%02x", v);
             }
@@ -3621,8 +3610,7 @@ background_enlightenment(int unused_mode UNUSED, int final)
        claimed by the comment below; this reveals more information than
        the basic status display, but that's one of the purposes of ^X;
        similar information is revealed by #overview; the "You died in
-       <location>" given by really_done() is more rudimentary than this */
-    *buf = *tmpbuf = '\0';
+       <location>" given by really_done() is more rudimentary than this */*buf = '\0'; *tmpbuf = '\0';
     if (In_endgame(&u.uz)) {
         int egdepth = observable_depth(&u.uz);
 
@@ -5280,8 +5268,7 @@ minimal_enlightenment(void)
     fmtstr = iflags.menu_tab_sep ? tabbed_fmtstr : untabbed_fmtstr;
     deity_fmtstr = iflags.menu_tab_sep ? tabbed_deity_fmtstr
                                        : untabbed_deity_fmtstr;
-    any = zeroany;
-    buf[0] = buf2[0] = '\0';
+    any = zeroany;buf[0] = '\0'; buf2[0] = '\0';
     tmpwin = create_nhwindow(NHW_MENU);
     start_menu(tmpwin);
     add_extended_menu(tmpwin, NO_GLYPH, &any, 0, 0, iflags.menu_headings | ATR_HEADING, NO_COLOR,
@@ -6847,31 +6834,9 @@ dokeylist(void)
     /* directional keys */
     putstr(datawin, 0, "");
     putstr(datawin, 0, "Directional keys:");
-    show_direction_keys(datawin, '.', FALSE); /* '.'==self in direction grid */
-
-    keys_used[(uchar) Cmd.move_NW] = keys_used[(uchar) Cmd.move_N]
-        = keys_used[(uchar) Cmd.move_NE] = keys_used[(uchar) Cmd.move_W]
-        = keys_used[(uchar) Cmd.move_E] = keys_used[(uchar) Cmd.move_SW]
-        = keys_used[(uchar) Cmd.move_S] = keys_used[(uchar) Cmd.move_SE]
-        = TRUE;
-
-    if (!iflags.num_pad) {
-        keys_used[(uchar) highc(Cmd.move_NW)]
-            = keys_used[(uchar) highc(Cmd.move_N)]
-            = keys_used[(uchar) highc(Cmd.move_NE)]
-            = keys_used[(uchar) highc(Cmd.move_W)]
-            = keys_used[(uchar) highc(Cmd.move_E)]
-            = keys_used[(uchar) highc(Cmd.move_SW)]
-            = keys_used[(uchar) highc(Cmd.move_S)]
-            = keys_used[(uchar) highc(Cmd.move_SE)] = TRUE;
-        keys_used[(uchar) C(Cmd.move_NW)]
-            = keys_used[(uchar) C(Cmd.move_N)]
-            = keys_used[(uchar) C(Cmd.move_NE)]
-            = keys_used[(uchar) C(Cmd.move_W)]
-            = keys_used[(uchar) C(Cmd.move_E)]
-            = keys_used[(uchar) C(Cmd.move_SW)]
-            = keys_used[(uchar) C(Cmd.move_S)]
-            = keys_used[(uchar) C(Cmd.move_SE)] = TRUE;
+    show_direction_keys(datawin, '.', FALSE); /* '.'==self in direction grid */keys_used[(uchar) Cmd.move_NW] = TRUE; keys_used[(uchar) Cmd.move_N] = TRUE; keys_used[(uchar) Cmd.move_NE] = TRUE; keys_used[(uchar) Cmd.move_W] = TRUE; keys_used[(uchar) Cmd.move_E] = TRUE; keys_used[(uchar) Cmd.move_SW] = TRUE; keys_used[(uchar) Cmd.move_S] = TRUE; keys_used[(uchar) Cmd.move_SE] = TRUE;
+
+    if (!iflags.num_pad) {keys_used[(uchar) highc(Cmd.move_NW)] = TRUE; keys_used[(uchar) highc(Cmd.move_N)] = TRUE; keys_used[(uchar) highc(Cmd.move_NE)] = TRUE; keys_used[(uchar) highc(Cmd.move_W)] = TRUE; keys_used[(uchar) highc(Cmd.move_E)] = TRUE; keys_used[(uchar) highc(Cmd.move_SW)] = TRUE; keys_used[(uchar) highc(Cmd.move_S)] = TRUE; keys_used[(uchar) highc(Cmd.move_SE)] = TRUE;keys_used[(uchar) C(Cmd.move_NW)] = TRUE; keys_used[(uchar) C(Cmd.move_N)] = TRUE; keys_used[(uchar) C(Cmd.move_NE)] = TRUE; keys_used[(uchar) C(Cmd.move_W)] = TRUE; keys_used[(uchar) C(Cmd.move_E)] = TRUE; keys_used[(uchar) C(Cmd.move_SW)] = TRUE; keys_used[(uchar) C(Cmd.move_S)] = TRUE; keys_used[(uchar) C(Cmd.move_SE)] = TRUE;
         putstr(datawin, 0, "");
         putstr(datawin, 0,
           "Shift-<direction> will move in specified direction until you hit");
@@ -7006,9 +6971,7 @@ count_obj(struct obj *chain, int64_t *total_count, size_t *total_size, boolean t
 {
     int64_t count;
     size_t size;
-    struct obj *obj;
-
-    for (count = size = 0, obj = chain; obj; obj = obj->nobj) {
+    struct obj *obj;for (count = 0; size = 0, obj = chain; obj; obj = obj->nobj) {
         if (top) {
             count++;
             size += size_obj(obj);
@@ -7289,9 +7252,7 @@ wiz_show_stats(void)
     size_t total_obj_size, total_mon_size, total_ovr_size, total_misc_size;
     
     win = create_nhwindow(NHW_TEXT);
-    putstr(win, 0, "Current memory statistics:");
-
-    total_obj_count = total_obj_size = 0L;
+    putstr(win, 0, "Current memory statistics:");total_obj_count = 0L; total_obj_size = 0L;
     putstr(win, 0, stats_hdr);
     Sprintf(buf, "  Objects, base size %ld", (long) sizeof (struct obj));
     putstr(win, 0, buf);
@@ -7313,9 +7274,7 @@ wiz_show_stats(void)
     contained_stats(win, "contained", &total_obj_count, &total_obj_size);
     putstr(win, 0, stats_sep);
     Sprintf(buf, template, "  Obj total", total_obj_count, total_obj_size);
-    putstr(win, 0, buf);
-
-    total_mon_count = total_mon_size = 0L;
+    putstr(win, 0, buf);total_mon_count = 0L; total_mon_size = 0L;
     putstr(win, 0, "");
     Sprintf(buf, "  Monsters, base size %ld", (long) sizeof (struct monst));
     putstr(win, 0, buf);
@@ -7329,17 +7288,13 @@ wiz_show_stats(void)
                   &total_mon_count, &total_mon_size);
     putstr(win, 0, stats_sep);
     Sprintf(buf, template, "  Mon total", total_mon_count, total_mon_size);
-    putstr(win, 0, buf);
-
-    total_ovr_count = total_ovr_size = 0L;
+    putstr(win, 0, buf);total_ovr_count = 0L; total_ovr_size = 0L;
     putstr(win, 0, "");
     putstr(win, 0, "  Overview");
     overview_stats(win, template, &total_ovr_count, &total_ovr_size);
     putstr(win, 0, stats_sep);
     Sprintf(buf, template, "  Over total", total_ovr_count, total_ovr_size);
-    putstr(win, 0, buf);
-
-    total_misc_count = total_misc_size = 0L;
+    putstr(win, 0, buf);total_misc_count = 0L; total_misc_size = 0L;
     putstr(win, 0, "");
     putstr(win, 0, "  Miscellaneous");
     misc_stats(win, &total_misc_count, &total_misc_size);
@@ -7638,8 +7593,7 @@ reset_commands(boolean initial)
         alt_esc = FALSE;
 #endif
         escape_sequence_key_start_allowed = FALSE;
-        last_multi = 0;
-        phead = ptail = shead = stail = 0;
+        last_multi = 0;phead = 0; ptail = 0; shead = 0; stail = 0;
         backed_dir_cmd = FALSE;
         en_via_menu = FALSE;
         special_effect_shown = FALSE;
@@ -7651,8 +7605,7 @@ reset_commands(boolean initial)
         struct ext_func_tab* efp;
         for (efp = extcmdlist; efp->ef_txt; efp++)
             efp->bound_key = 0;
-        Cmd.num_pad = FALSE;
-        Cmd.pcHack_compat = Cmd.phone_layout = Cmd.swap_yz = FALSE;
+        Cmd.num_pad = FALSE;Cmd.pcHack_compat = FALSE; Cmd.phone_layout = FALSE; Cmd.swap_yz = FALSE;
         for (i = 0; i < SIZE(spkeys_binds); i++)
             Cmd.spkeys[spkeys_binds[i].nhkf] = spkeys_binds[i].key;
         commands_init();
@@ -8149,8 +8102,7 @@ rhack(char *cmd)
            feedback and led to strangeness if the key pressed
            ('u' in particular) was overloaded for num_pad use */
         You_cant("get there from here...");
-        clear_run_and_travel();
-        context.nopick = context.forcefight = FALSE;
+        clear_run_and_travel();context.nopick = FALSE; context.forcefight = FALSE;
         multi = 0;
         return;
     }
@@ -8301,8 +8253,7 @@ movecmd(char sym)
     u.dy = ydir[dp - Cmd.dirchars];
     u.dz = zdir[dp - Cmd.dirchars];
 #if 0 /* now handled elsewhere */
-    if (u.dx && u.dy && NODIAG(u.umonnum)) {
-        u.dx = u.dy = 0;
+    if (u.dx && u.dy && NODIAG(u.umonnum)) {u.dx = 0; u.dy = 0;
         return 0;
     }
 #endif
@@ -8313,8 +8264,8 @@ movecmd(char sym)
 int
 dxdy_moveok(void)
 {
-    if (u.dx && u.dy && NODIAG(u.umonnum))
-        u.dx = u.dy = 0;
+    if (u.dx && u.dy &&NODIAG(u.umonnum))
+        u.dx = 0; u.dy = 0;
     return u.dx || u.dy;
 }
 
@@ -8404,8 +8355,7 @@ retry:
 
     if (dirsym == Cmd.spkeys[NHKF_GETDIR_SELF]
         || dirsym == Cmd.spkeys[NHKF_GETDIR_SELF2])
-    {
-        u.dx = u.dy = u.dz = 0;
+    {u.dx = 0; u.dy = 0; u.dz = 0;
     } 
     else if (!(is_mov = movecmd(dirsym)) && !u.dz) 
     {
@@ -9058,8 +9008,7 @@ click_to_cmd(int x, int y, int mod)
             return cmd;
         }
         else
-        {
-            cmd[0] = cmd[1] = '\0';
+        {cmd[0] = '\0'; cmd[1] = '\0';
             if (is_mtmp_spotted)
             {
                 const char* spellnam = spl_book[context.quick_cast_spell_no].sp_id > STRANGE_OBJECT ? OBJ_NAME(objects[spl_book[context.quick_cast_spell_no].sp_id]) : "";
@@ -9164,8 +9113,7 @@ click_to_cmd(int x, int y, int mod)
             return cmd;
         }
         else
-        {
-            cmd[0] = cmd[1] = '\0';
+        {cmd[0] = '\0'; cmd[1] = '\0';
             if (is_mtmp_spotted)
             {
                 if (!x || !y || abs(x) == abs(y)) /* straight line or diagonal */
@@ -9793,8 +9741,7 @@ parse(void)
     if ((!Cmd.num_pad && !Cmd.gnh_layout) || ((foo = readchar()) != 0 && (foo == Cmd.spkeys[NHKF_COUNT] || foo == Cmd.spkeys[NHKF_COUNT2]))) 
     {
         int64_t tmpmulti = (int64_t)multi;
-        foo = get_count((char *) 0, '\0', LARGEST_INT, &tmpmulti, FALSE);
-        last_multi = multi = (int)tmpmulti;
+        foo = get_count((char *) 0, '\0', LARGEST_INT, &tmpmulti, FALSE);last_multi = (int; multi = (int)tmpmulti;
     }
 #ifdef ALTMETA
     alt_esc = FALSE; /* readchar() reset */
@@ -9809,8 +9756,7 @@ parse(void)
 
     if (foo == Cmd.spkeys[NHKF_ESC]) 
     { /* esc cancels count (TH) */
-        clear_nhwindow(WIN_MESSAGE);
-        multi = last_multi = 0;
+        clear_nhwindow(WIN_MESSAGE);multi = 0; last_multi = 0;
     }
     else if (foo == Cmd.spkeys[NHKF_DOAGAIN] || foo == Cmd.spkeys[NHKF_DOAGAIN2] || in_doagain) {
         multi = last_multi;
@@ -10059,9 +10005,7 @@ dotravel(void)
             return 0;
         }
     }
-    iflags.getloc_travelmode = FALSE;
-    iflags.travelcc.x = u.tx = cc.x;
-    iflags.travelcc.y = u.ty = cc.y;
+    iflags.getloc_travelmode = FALSE;iflags.travelcc.x = cc.x; u.tx = cc.x;iflags.travelcc.y = cc.y; u.ty = cc.y;
     cmd[0] = Cmd.spkeys[NHKF_TRAVEL];
     readchar_queue = cmd;
     return 0;
@@ -10721,7 +10665,7 @@ create_context_menu(enum create_context_menu_types menu_type)
                         //otmp = level.objects[x][y];
                         //if (otmp)
                         //{
-                        //    if (Is_proper_container(otmp) && otmp->olocked && !otmp->obroken && otmp->lknown)
+                        //    if (Is_proper_container(otmp) && is_obj_locked(otmp) && !get_flag(otmp->obj_bitflags, OBJ_BITFLAGS_OBROKEN) && is_obj_lknown(otmp))
                         //    {
                         //        kickotmp = otmp;
                         //        addkickmenu = TRUE;
@@ -10940,7 +10884,7 @@ dosetquickbag(void)
         play_sfx_sound(SFX_GENERAL_CANNOT);
         pline_ex(ATR_NONE, CLR_MSG_FAIL, "%s cannot be set as a quick bag without opening its lid first.", The(cxname(obj)));
     }
-    else if (!objects[obj->otyp].oc_name_known && !obj->cknown) /* This should prevent checking out which are non-containers */
+    else if (!objects[obj->otyp].oc_name_known && !is_obj_cknown(obj)) /* This should prevent checking out which are non-containers */
     {
         play_sfx_sound(SFX_GENERAL_CANNOT);
         pline_ex(ATR_NONE, CLR_MSG_FAIL, "%s cannot be set as a quick bag without investigating its contents or determining its nature first.", The(cxname(obj)));
diff --git a/src/detect.c b/src/detect.c
index 76738dfa5..741773f97 100644
--- a/src/detect.c
+++ b/src/detect.c
@@ -564,7 +562,7 @@ food_detect(struct obj *sobj)
         dozoommini();
 
         (void) unconstrain_map();
-        for (obj = fobj; obj; obj = obj->nobj)
+        for (obj = fobj; obj;obj = o_in(obj; obj->nobj)
             if ((temp = o_in(obj, oclass)) != 0) {
                 if (temp != obj) {
                     temp->ox = obj->ox;
@@ -574,7 +572,7 @@ food_detect(struct obj *sobj)
             }
         for (mtmp = fmon; mtmp; mtmp = mtmp->nmon)
             /* no DEADMONSTER() check needed -- dmons never have inventory */
-            for (obj = mtmp->minvent; obj; obj = obj->nobj)
+            for (obj = mtmp->minvent; obj;obj = o_in(obj; obj->nobj)
                 if ((temp = o_in(obj, oclass)) != 0) {
                     temp->ox = mtmp->mx;
                     temp->oy = mtmp->my;
@@ -1294,8 +1291,7 @@ use_crystal_ball(struct obj **optr)
             special_effect_wait_until_action(0);
             pline_ex(ATR_NONE, CLR_MSG_NEGATIVE, "%s!", Tobjnam(obj, "explode"));
             debugprint("use_crystal_ball: %d", obj->otyp);
-            useup(obj);
-            *optr = obj = 0; /* it's gone */
+            useup(obj);*optr = 0; obj = 0; /* it's gone */
             /* physical damage cause by the shards and force */
             losehp(adjust_damage(rnd(30), (struct monst*)0, &youmonst, AD_PHYS, ADFLAGS_NONE), "exploding crystal ball",
                    KILLED_BY_AN);
diff --git a/src/dig.c b/src/dig.c
index fe292d13e..c27630ab1 100644
--- a/src/dig.c
+++ b/src/dig.c
@@ -1520,8 +1520,7 @@ use_pick_axe2(struct obj *obj)
                     /* avoid messages if repeated autodigging */
                     did_dig_msg = TRUE;
                     context.digging.quiet = TRUE;
-                }
-                context.digging.down = context.digging.chew = FALSE;
+                }context.digging.down = FALSE; context.digging.chew = FALSE;
                 context.digging.warned = FALSE;
                 context.digging.pos.x = rx;
                 context.digging.pos.y = ry;
@@ -3044,8 +3041,7 @@ bury_objs(int x, int y)
     int64_t loss = 0L;
     boolean costly;
 
-    debugprint_pos();
-    costly = ((shkp = shop_keeper(*in_rooms(x, y, SHOPBASE)))
+    debugprint_pos();costly = shop_keeper(*in_rooms(x; ((shkp = shop_keeper(*in_rooms(x, y, SHOPBASE)))
               && costly_spot(x, y));
 
     if (level.objects[x][y] != (struct obj *) 0) {
@@ -3341,15 +3337,13 @@ int
 dodig(void)
 {
     if (uwep && (uwep->otyp == PICK_AXE || uwep->otyp == DWARVISH_MATTOCK || uwep->otyp == SPADE_OF_COLOSSAL_EXCAVATION))
-    {
-        u.dx = u.dy = 0;
+    {u.dx = 0; u.dy = 0;
         u.dz = 1;
         int res = use_pick_axe2(uwep);
         return res;
     }
     else if (u.twoweap && uarms && (uarms->otyp == PICK_AXE || uarms->otyp == DWARVISH_MATTOCK || uarms->otyp == SPADE_OF_COLOSSAL_EXCAVATION))
-    {
-        u.dx = u.dy = 0;
+    {u.dx = 0; u.dy = 0;
         u.dz = 1;
         int res = use_pick_axe2(uarms);
         return res;
diff --git a/src/display.c b/src/display.c
index e5446c104..cdee48639 100644
--- a/src/display.c
+++ b/src/display.c
@@ -1294,8 +1293,7 @@ newsym_with_extra_info_and_flags(int x, int y, uint64_t disp_flags, uint64_t dis
         /* RECALL FIRST WHETHER THE LOCATION IS LIT OR NOT */
         /*
          * Don't use templit here:  E.g.
-         *
-         *      lev->waslit = !!(lev->lit || templit(x,y));
+         *      set_rm_waslit(lev, !!is_rm_lit(lev) || templit(x,y));
          *
          * Otherwise we have the "light pool" problem, where non-permanently
          * lit areas just out of sight stay remembered as lit.  They should
@@ -1922,10 +1920,7 @@ clear_glyph_buffer_at(int x, int y)
 
 void
 reset_display(void)
-{
-    dela = delagr = FALSE;
-    lastx = lasty = 0;
-    lastswx = lastswy = 0;
+{dela = FALSE; delagr = FALSE;lastx = 0; lasty = 0;lastswx = 0; lastswy = 0;
     memset((genericptr_t)gbuf, 0 , sizeof(gbuf));
     memset((genericptr_t)gbuf_start, 0, sizeof(gbuf_start));
     memset((genericptr_t)gbuf_stop, 0, sizeof(gbuf_stop));
@@ -4712,8 +4707,7 @@ set_wall_state(void)
         }
 
 #ifdef WA_VERBOSE
-    /* check if any bad positions found */
-    for (x = y = 0; x < MAX_LEVTYPE; x++)
+    /* check if any bad positions found */for (x = 0; y = 0; x < MAX_LEVTYPE; x++)
         if (bad_count[x]) {
             if (y == 0) {
                 y = 1; /* only print once */
diff --git a/src/do.c b/src/do.c
index 0c5d17d86..4e580e7f7 100644
--- a/src/do.c
+++ b/src/do.c
@@ -5834,8 +5834,7 @@ flooreffects(struct obj *obj, int x, int y, const char *verb)
         panic("flooreffects: obj not free");
         return FALSE;
     }
-    /* make sure things like water_damage() have no pointers to follow */
-    obj->nobj = obj->nexthere = (struct obj *) 0;
+    /* make sure things like water_damage() have no pointers to follow */obj->nobj = (struct obj *; obj->nexthere = (struct obj *) 0;
 
     if (obj->otyp == BOULDER && boulder_hits_pool(obj, x, y, FALSE))
     {
@@ -6165,7 +6163,7 @@ dosinkring(struct obj *obj)
     boolean nosink = FALSE;
 
     You("drop %s down the drain.", doname(obj));
-    obj->in_use = TRUE;  /* block free identification via interrupt */
+    set_obj_in_use(obj, TRUE);  /* block free identification via interrupt */
     switch (obj->otyp) { /* effects that can be noticed without eyes */
     case RIN_SEARCHING:
         You_ex(ATR_NONE, CLR_MSG_ATTENTION, "thought %s got lost in the sink, but there it is!", yname(obj));
@@ -7243,7 +7241,7 @@ dodown(void)
             return 0;
         else
             pline_ex(ATR_NONE, CLR_MSG_GOD, "So be it.");
-        u.uevent.gehennom_entered = 1; /* don't ask again */
+        set_flag(u.uevent.bitflags, UEVENT_BITFLAGS_GEHENNOM_ENTERED, 1); /* don't ask again */
     }
 
     if (!next_to_u())
@@ -7553,8 +7551,8 @@ goto_level(d_level *newlevel, uchar at_location, boolean falling, boolean inside
                 (void) safe_teleds(FALSE, FALSE);
                 (void) next_to_u();
                 return;
-            } else
-                at_stairs = at_ladder = FALSE;
+            }else
+                at_stairs = FALSE; at_ladder = FALSE;
         }
     }
 #endif
@@ -7598,8 +7596,7 @@ goto_level(d_level *newlevel, uchar at_location, boolean falling, boolean inside
        keep context; if on the floor, it's about to be saved+freed and
        maybe_reset_pick() needs to do its carried() check before that */
     maybe_reset_pick((struct obj *) 0);
-    reset_trapset(); /* even if to-be-armed trap obj is accompanying hero */
-    iflags.travelcc.x = iflags.travelcc.y = 0; /* travel destination cache */
+    reset_trapset(); /* even if to-be-armed trap obj is accompanying hero */iflags.travelcc.x = 0; iflags.travelcc.y = 0; /* travel destination cache */
     context.polearm.hitmon = (struct monst *) 0; /* polearm target */
     /* digging context is level-aware and can actually be resumed if
        hero returns to the previous level without any intervening dig */
@@ -7617,8 +7614,7 @@ goto_level(d_level *newlevel, uchar at_location, boolean falling, boolean inside
     u.uundetected = 0; /* not hidden, even if means are available */
     move_monsters_to_mydogs(context.reviving, TRUE);
     removealtarsummons();
-    if (u.uswallow) /* idem */
-        u.uswldtim = u.uswallow = 0;
+    if (u.uswallow) /* idem */u.uswldtim = 0; u.uswallow = 0;
     debugprint_pos();
     recalc_mapseen(); /* recalculate map overview before we leave the level */
     /*
@@ -8995,9 +8991,7 @@ heal_legs(int how)
             /* this used to say "somewhat better" but that was
                misleading since legs are being fully healed */
             Your("%s %s better.", legs, vtense(legs, "feel"));
-        }
-
-        HWounded_legs = EWounded_legs = 0L;
+        }HWounded_legs = 0L; EWounded_legs = 0L;
 
         /* Wounded_legs reduces carrying capacity, so we want
            an encumbrance check when they're healed.  However,
@@ -9789,8 +9783,7 @@ heal_ailments_upon_revival(void)
     make_slimed(0L, (char*)0, 0, FALSE, 0);
     make_sick(0L, (char*)0, FALSE, 0);
     make_food_poisoned(0L, (char*)0, FALSE, 0);
-    make_hallucinated(0L, FALSE, 0L);
-    u.ubasehpdrain = u.ubaseendrain = u.basemhdrain = 0;
+    make_hallucinated(0L, FALSE, 0L);u.ubasehpdrain = 0; u.ubaseendrain = 0; u.basemhdrain = 0;
     updatemaxhp();
     updatemaxen();
 }
diff --git a/src/do_name.c b/src/do_name.c
index 45bd69e8c..5849e6216 100644
--- a/src/do_name.c
+++ b/src/do_name.c
@@ -472,9 +472,7 @@ gather_locs(coord **arr_p, int *cnt_p, int gloc)
      * be the shortest distance (namely, 0 units) away from <u.ux,u.uy>.
      */
 
-    gloc_filter_init();
-
-    *cnt_p = idx = 0;
+    gloc_filter_init();*cnt_p = 0; idx = 0;
     for (pass = 0; pass < 2; pass++) {
         for (x = 1; x < COLNO; x++)
             for (y = 0; y < ROWNO; y++) {
@@ -782,8 +780,7 @@ getpos(coord *ccp, boolean force, const char *goal, enum game_cursor_types curso
             msg_given = FALSE;
 
         if (c == Cmd.spkeys[NHKF_ESC]) 
-        {
-            cx = cy = -10;
+        {cx = -10; cy = -10;
             msg_given = TRUE; /* force clear */
             result = -1;
             break;
@@ -918,8 +915,7 @@ getpos(coord *ccp, boolean force, const char *goal, enum game_cursor_types curso
                 {
                     free((genericptr_t) garr[i]);
                     garr[i] = NULL;
-                }
-                gidx[i] = gcount[i] = 0;
+                }gidx[i] = 0; gcount[i] = 0;
             }
             pline("%s.", view_filters[iflags.getloc_filter]);
             msg_given = TRUE;
@@ -1450,7 +1446,7 @@ do_mname(void)
     }
     //else if (has_mname(mtmp))
     //{
-    //    if(!mtmp->u_know_mname || !*buf)
+    //    if(!is_mon_u_know_mname(mtmp) || !*buf)
     //        (void)u_name_monst(mtmp, buf);
     //    else
     //        pline("%s will not accept the name %s.", upstart(monnambuf), buf);
@@ -1487,7 +1483,7 @@ do_uoname(struct obj *obj)
     //    return;
     //}
 
-    //if (has_oname(obj) && obj->nknown) 
+    //if (has_oname(obj) && is_obj_nknown(obj)) 
     //{
     //    play_sfx_sound(SFX_GENERAL_CANNOT);
     //    pline_ex(AT_NONE, CLR_MSG_FAIL, "%s already has a true name.", Yname2(obj));
diff --git a/src/do_wear.c b/src/do_wear.c
index 8bb82242e..3b8baa63b 100644
--- a/src/do_wear.c
+++ b/src/do_wear.c
@@ -89,7 +89,7 @@ on_msg(struct obj *otmp)
     if (flags.verbose && otmp) {
         char how[BUFSZ];
         /* call xname() before obj_is_pname(); formatting obj's name
-           might set obj->dknown and that affects the pname test */
+           might set is_obj_dknown(obj) and that affects the pname test */
         const char *otmp_name = xname(otmp);
 
         how[0] = '\0';
@@ -826,8 +826,7 @@ Amulet_on(void)
     case AMULET_OF_STRANGULATION:
         if (can_be_strangled(&youmonst)) {
             makeknown(AMULET_OF_STRANGULATION);
-            Strangled = 6L;
-            context.botl = context.botlx = TRUE;
+            Strangled = 6L;context.botl = TRUE; context.botlx = TRUE;
             pline("It constricts your throat!");
         }
         break;
@@ -851,8 +850,7 @@ Amulet_off(void)
     switch (uamul->otyp) {
     case AMULET_OF_STRANGULATION:
         if (Strangled) {
-            Strangled = 0L;
-            context.botl = context.botlx = TRUE;
+            Strangled = 0L;context.botl = TRUE; context.botlx = TRUE;
             if (Breathless)
                 Your("%s is no longer constricted!", body_part(NECK));
             else
@@ -1367,9 +1365,7 @@ static NEARDATA int Narmorpieces, Naccessories;
 static void
 count_worn_stuff(struct obj **which, boolean accessorizing)
 {
-    struct obj *otmp;
-
-    Narmorpieces = Naccessories = 0;
+    struct obj *otmp;Narmorpieces = 0; Naccessories = 0;
 
 #define MOREWORN(x,wtyp) do { if (x) { wtyp++; otmp = x; } } while (0)
     otmp = 0;
@@ -1419,9 +1415,7 @@ armor_or_accessory_off(struct obj *obj)
     }
     if (obj == uskin || is_armor_covered(obj))
     {
-        char why[QBUFSZ], what[QBUFSZ];
-
-        why[0] = what[0] = '\0';
+        char why[QBUFSZ], what[QBUFSZ];why[0] = '\0'; what[0] = '\0';
         if (obj != uskin) 
         {
             print_covering_items(obj, what);
@@ -1726,8 +1720,7 @@ armoroff(struct obj *otmp)
         else
             setworn((struct obj *) 0, otmp->owornmask & W_ARMOR);
         //off_msg(otmp);
-    }
-    context.takeoff.mask = context.takeoff.what = context.wear.mask = context.wear.what = 0L;
+    }context.takeoff.mask = 0L; context.takeoff.what = 0L; context.wear.mask = 0L; context.wear.what = 0L;
     return 1;
 }
 
@@ -2738,7 +2731,7 @@ obj->known = 1;
             }
 
             if (!in_takeoff_wear)
-                context.takeoff.mask = context.takeoff.what = context.wear.mask = context.wear.what = 0L;
+                context.takeoff.mask = 0L; context.takeoff.what = 0L; context.wear.mask = 0L; context.wear.what = 0L;
         }
     } 
     else 
@@ -3924,8 +3917,7 @@ take_off(void)
 /* clear saved context to avoid inappropriate resumption of interrupted 'A' */
 void
 reset_remarm(void)
-{
-    context.takeoff.what = context.takeoff.mask = context.wear.what = context.wear.mask = 0L;
+{context.takeoff.what = 0L; context.takeoff.mask = 0L; context.wear.what = 0L; context.wear.mask = 0L;
     context.takeoff.command = 0;
     int i;
     for (i = 0; i < WEAR_OID_BITS; i++)
diff --git a/src/dog.c b/src/dog.c
index 6d3052725..7a7135e65 100644
--- a/src/dog.c
+++ b/src/dog.c
@@ -596,10 +596,9 @@ makedog(void)
                 petgender = doggender;
         }
 
-        if(!dogbreed && petdetails_used)
-            petbreed = petbreed_female = choose_pet_breed(pettype, petgender == 2);
-        else
-            petbreed = petbreed_female = dogbreed;
+        if(!dogbreed &&petdetails_used)
+            petbreed = choose_pet_breed(pettype; petbreed_female = choose_pet_breed(pettype, petgender == 2);else
+            petbreed = dogbreed; petbreed_female = dogbreed;
     }
     else if (pettype == PM_PONY)
     {
@@ -652,10 +651,9 @@ makedog(void)
                 petgender = catgender;
         }
 
-        if (!catbreed && petdetails_used)
-            petbreed = petbreed_female = choose_pet_breed(pettype, petgender == 2);
-        else
-            petbreed = petbreed_female = catbreed;
+        if (!catbreed &&petdetails_used)
+            petbreed = choose_pet_breed(pettype; petbreed_female = choose_pet_breed(pettype, petgender == 2);else
+            petbreed = catbreed; petbreed_female = catbreed;
     }
 
     char givennamebuf[PL_PSIZ] = "";
@@ -733,8 +731,7 @@ makedog(void)
         else if (!petbreed && !*petname && pettype == PM_KITTEN)
         {
             if ((Role_if(PM_WIZARD) || Role_if(PM_PRIEST) || Role_if(PM_MONK)) && u.ualign.type == A_CHAOTIC)
-            {
-                petbreed = petbreed_female = CAT_BREED_BLACK;
+            {petbreed = CAT_BREED_BLACK; petbreed_female = CAT_BREED_BLACK;
             }
         }
         else if (!*petname && pettype == PM_DIREWOLF_CUB)
@@ -1071,8 +1067,7 @@ mon_arrive(struct monst *mtmp, boolean with_you)
             impossible("mon_arrive: portal, no corresponding portal? from: %d/%d, to: %d/%d.", u.uz0.dnum, u.uz0.dlevel, u.uz.dnum, u.uz.dlevel);
         } /*FALLTHRU*/
     default:
-    case MIGR_RANDOM:
-        xlocale = ylocale = 0;
+    case MIGR_RANDOM:xlocale = 0; ylocale = 0;
         break;
     }
 
@@ -1098,9 +1093,8 @@ mon_arrive(struct monst *mtmp, boolean with_you)
             point.x = xlocale;
             point.y = ylocale;
             if (somexy_within_distance(&rooms[*r - ROOMOFFSET], point, wander, &c))
-                xlocale = c.x, ylocale = c.y;
-            else
-                xlocale = ylocale = 0;
+                xlocale = c.x, ylocale = c.y;else
+                xlocale = 0; ylocale = 0;
         } 
         else 
         { /* not in a room */
@@ -1569,8 +1561,7 @@ migrate_to_level(struct monst *mtmp, xchar tolev, xchar xyloc, coord *cc)
     mtmp->mtrack[0].x = xyloc;
     mtmp->mtrack[0].y = xyflags;
     mtmp->mux = new_lev.dnum;
-    mtmp->muy = new_lev.dlevel;
-    mtmp->mx = mtmp->my = 0; /* this implies migration */
+    mtmp->muy = new_lev.dlevel;mtmp->mx = 0; mtmp->my = 0; /* this implies migration */
     mtmp->mon_flags &= ~MON_FLAGS_SPLEVEL_RESIDENT; /* Not level's original resident anymore */
     if (mtmp == context.polearm.hitmon)
         context.polearm.hitmon = (struct monst *) 0;
@@ -1848,9 +1839,9 @@ tamedog(struct monst *mtmp, struct obj *obj, uchar forcetaming, int charm_type,
 
     if ((mtmp->mtame) ||  (!forcetaming && (mtmp->data->geno & G_UNIQ) && !is_cerberus) /* Unique monsters cannot be tamed, except Cerberus -- JG */
         /* monsters with conflicting structures cannot be tamed */
-        || mtmp->isshk || mtmp->isgd || mtmp->ispriest || mtmp->issmith || mtmp->isnpc /* shopkeepers, guards, and priests cannot be forced to be tame for now -- JG */
+        || is_mon_shk(mtmp) || is_mon_gd(mtmp) || is_mon_priest(mtmp) || is_mon_smith(mtmp) || is_mon_npc(mtmp) /* shopkeepers, guards, and priests cannot be forced to be tame for now -- JG */
         || (!forcetaming && 
-            (mtmp->isminion /* minions cannot be tamed, not sure why this is --JG */
+            (is_mon_minion(mtmp) /* minions cannot be tamed, not sure why this is --JG */
             || is_covetous(mtmp->data)
             || is_human(mtmp->data)
             || (is_demon(mtmp->data) && !is_demon(youmonst.data))
@@ -2048,8 +2038,7 @@ wary_dog(struct monst *mtmp, boolean was_dead)
         /* it's still a pet; start a clean pet-slate now */
         edog->revivals++;
         edog->killed_by_u = 0;
-        edog->abuse = 0;
-        edog->ogoal.x = edog->ogoal.y = -1;
+        edog->abuse = 0;edog->ogoal.x = -1; edog->ogoal.y = -1;
         if (was_dead || edog->hungrytime < monstermoves + 500L)
             edog->hungrytime = monstermoves + 500L;
         if (was_dead)
diff --git a/src/dogmove.c b/src/dogmove.c
index a6ddacbdf..0611a1acd 100644
--- a/src/dogmove.c
+++ b/src/dogmove.c
@@ -34,13 +34,11 @@ droppables(struct monst *mon)
     dummy = zeroobj;
     dummy.otyp = GOLD_PIECE; /* not STRANGE_OBJECT or tools of interest */
     dummy.oclass = COIN_CLASS;
-    dummy.oartifact = 1; /* so real artifact won't override "don't keep it" */
-    pickaxe = unihorn = key = (struct obj *) 0;
+    dummy.oartifact = 1; /* so real artifact won't override "don't keep it" */pickaxe = (struct obj *; unihorn = (struct obj *; key = (struct obj *) 0;
     wep = MON_WEP(mon);
 
     if (is_animal(mon->data) || mindless(mon->data)) {
-        /* won't hang on to any objects of these types */
-        pickaxe = unihorn = key = &dummy; /* act as if already have them */
+        /* won't hang on to any objects of these types */pickaxe = &dummy; unihorn = &dummy; key = &dummy; /* act as if already have them */
     } else {
         /* don't hang on to pick-axe if can't use one or don't need one */
         if (!tunnels(mon->data) || !needspick(mon->data))
@@ -1409,7 +1407,7 @@ dog_goal(struct monst *mtmp, struct edog *edog, int after, int udist, int whappr
   
     dog_has_minvent = (droppables(mtmp) != 0);
 
-    if (!edog || mtmp->mleashed || (mtmp->mcomingtou && in_masters_sight)) { /* he's not going anywhere... */
+    if (!edog || is_mon_leashed(mtmp) || (mtmp->mcomingtou && in_masters_sight)) { /* he's not going anywhere... */
         gtyp = APPORT;
         gx = u.ux;
         gy = u.uy;
@@ -1441,8 +1439,7 @@ dog_goal(struct monst *mtmp, struct edog *edog, int after, int udist, int whappr
         int min_x, max_x, min_y, max_y;
         int nx, ny;
 
-        gtyp = UNDEF; /* no goal as yet */
-        gx = gy = 0;  /* suppress 'used before set' message */
+        gtyp = UNDEF; /* no goal as yet */gx = 0; gy = 0;  /* suppress 'used before set' message */
 
         if ((min_x = omx - SQSRCHRADIUS) < 1)
             min_x = 1;
@@ -1571,8 +1568,7 @@ dog_goal(struct monst *mtmp, struct edog *edog, int after, int udist, int whappr
             }
             else 
             {
-                int fardist = FARAWAY * FARAWAY;
-                gx = gy = FARAWAY; /* random */
+                int fardist = FARAWAY * FARAWAY;gx = FARAWAY; gy = FARAWAY; /* random */
                 do_clear_area(omx, omy, 9, wantdoor, (genericptr_t) &fardist);
 
                 /* here gx == FARAWAY e.g. when dog is in a vault */
@@ -2216,9 +2212,7 @@ dog_move(struct monst *mtmp, int after)
                 chi = i;
                 break;
             }
-        }
-
-        j = ((ndist = GDIST(nx, ny)) - nidist) * appr;
+        }j = GDIST(nx; ((ndist = GDIST(nx, ny)) - nidist) * appr;
         if ((j == 0 && !rn2(++chcnt)) || j < 0
             || (j > 0 && !whappr
                 && ((omx == nix && omy == niy && !rn2(3)) || !rn2(12)))) 
@@ -2622,8 +2616,7 @@ quickmimic(struct monst *mtmp)
 
 void
 reset_dogmove(void)
-{
-    gtyp = gx = gy = 0;
+{gtyp = 0; gx = 0; gy = 0;
 }
 
 /*dogmove.c*/
diff --git a/src/dokick.c b/src/dokick.c
index 1f01198d1..4d36a766b 100644
--- a/src/dokick.c
+++ b/src/dokick.c
@@ -676,8 +676,7 @@ container_impact_dmg(struct obj *obj, xchar x, xchar y)
     if (!Is_container(obj) || !Has_contents(obj) || Is_mbag(obj))
         return;
 
-    debugprint_pos();
-    costly = ((shkp = shop_keeper(*in_rooms(x, y, SHOPBASE)))
+    debugprint_pos();costly = shop_keeper(*in_rooms(x; ((shkp = shop_keeper(*in_rooms(x, y, SHOPBASE)))
               && costly_spot(x, y));
     insider = (*u.ushops && inside_shop(u.ux, u.uy)
                && *in_rooms(x, y, SHOPBASE) == *u.ushops);
@@ -1987,8 +1986,7 @@ drop_to(coord *cc, schar loc)
             cc->x = valley_level.dnum;
             cc->y = valley_level.dlevel;
             break;
-        } else if (In_endgame(&u.uz) || Is_botlevel(&u.uz)) {
-            cc->y = cc->x = 0;
+        } else if (In_endgame(&u.uz) || Is_botlevel(&u.uz)) {cc->y = 0; cc->x = 0;
             break;
         }
         /*FALLTHRU*/
@@ -2003,8 +2001,7 @@ drop_to(coord *cc, schar loc)
         break;
     default:
     case MIGR_NOWHERE:
-        /* y==0 means "nowhere", in which case x doesn't matter */
-        cc->y = cc->x = 0;
+        /* y==0 means "nowhere", in which case x doesn't matter */cc->y = 0; cc->x = 0;
         break;
     }
 }
@@ -2042,8 +2039,7 @@ impact_drop(struct obj *missile, xchar x, xchar y, xchar dlev, boolean dropall)
         cc.y = dlev;
     }
 
-    costly = costly_spot(x, y);
-    price = debit = robbed = 0L;
+    costly = costly_spot(x, y);price = 0L; debit = 0L; robbed = 0L;
     angry = FALSE;
     shkp = (struct monst *) 0;
     /* if 'costly', we must keep a record of ESHK(shkp) before
@@ -2061,8 +2057,7 @@ impact_drop(struct obj *missile, xchar x, xchar y, xchar dlev, boolean dropall)
         }
     }
 
-    isrock = (missile && is_rock(missile));
-    oct = dct = 0L;
+    isrock = (missile && is_rock(missile));oct = 0L; dct = 0L;
     for (obj = level.objects[x][y]; obj; obj = obj2) {
         obj2 = obj->nexthere;
         if (obj == missile)
@@ -2328,8 +2323,7 @@ obj_delivery(boolean near_hero)
             nx = u.ux, ny = u.uy;
             break;
         default:
-        case MIGR_RANDOM:
-            nx = ny = 0;
+        case MIGR_RANDOM:nx = 0; ny = 0;
             break;
         }
         if (nx > 0) {
@@ -2352,8 +2346,7 @@ obj_delivery(boolean near_hero)
                 newsym(nx, ny);
         } else { /* random location */
             /* set dummy coordinates because there's no
-               current position for rloco() to update */
-            otmp->ox = otmp->oy = 0;
+               current position for rloco() to update */otmp->ox = 0; otmp->oy = 0;
             if (rloco(otmp) && !nobreak && breaktest(otmp)) {
                 /* assume it broke before player arrived, no messages */
                 debugprint("obj_delivery2: %d", otmp->otyp);
@@ -2387,8 +2380,7 @@ deliver_obj_to_mon(struct monst *mtmp, int cnt, uint64_t deliverflags)
         if ((mtmp->data->mflags2 & otmp->corpsenm) != 0) {
             debugprint("deliver_obj_to_mon");
             obj_extract_self(otmp);
-            otmp->owornmask = 0L;
-            otmp->ox = otmp->oy = 0;
+            otmp->owornmask = 0L;otmp->ox = 0; otmp->oy = 0;
 
             /* special treatment for orcs and their kind */
             if ((otmp->corpsenm & M2_ORC) != 0 && has_oname(otmp)) {
diff --git a/src/dothrow.c b/src/dothrow.c
index 08d9a8e87..0bc2a5152 100644
--- a/src/dothrow.c
+++ b/src/dothrow.c
@@ -176,8 +176,7 @@ throw_obj(struct obj *obj, int shotlimit, boolean firing)
             else if (m_shot.i < m_shot.n && ParanoidMultiShot && yn_query(firing ? "Target was killed. Continue firing?" : "Target was killed. Continue throwing?") != 'y')
                 break;
         }
-    }
-    m_shot.n = m_shot.i = 0;
+    }m_shot.n = 0; m_shot.i = 0;
     m_shot.o = STRANGE_OBJECT;
     m_shot.s = FALSE;
     return 1;
@@ -686,9 +685,7 @@ walk_path(coord *src_cc, coord *dest_cc, boolean (*check_proc)(genericptr_t, int
      * testing a possible move and actually attempting that move.
      */
     dx = dest_cc->x - src_cc->x;
-    dy = dest_cc->y - src_cc->y;
-    prev_x = x = src_cc->x;
-    prev_y = y = src_cc->y;
+    dy = dest_cc->y - src_cc->y;prev_x = src_cc->x; x = src_cc->x;prev_y = src_cc->y; y = src_cc->y;
 
     if (dx < 0) {
         x_change = -1;
@@ -699,9 +696,7 @@ walk_path(coord *src_cc, coord *dest_cc, boolean (*check_proc)(genericptr_t, int
         y_change = -1;
         dy = -dy;
     } else
-        y_change = 1;
-
-    i = err = 0;
+        y_change = 1;i = 0; err = 0;
     if (dx < dy) {
         while (i++ < dy) {
             prev_x = x;
@@ -902,7 +897,7 @@ hurtle_step(genericptr_t arg, int x, int y)
         const char *mnam, *pronoun;
         int glyph = glyph_at(x, y);
 
-        mon->mundetected = 0; /* wakeup() will handle mimic */
+        set_mon_undetected(mon, 0); /* wakeup() will handle mimic */
         mnam = a_monnam(mon); /* after unhiding */
         pronoun = noit_mhim(mon);
 
@@ -1422,8 +1417,7 @@ throwit(struct obj *obj, int64_t wep_mask)
     {
         You("have so little stamina, %s drops from your grasp.",
             the(xname(obj)));
-        exercise(A_CON, FALSE);
-        u.dx = u.dy = 0;
+        exercise(A_CON, FALSE);u.dx = 0; u.dy = 0;
         u.dz = 1;
     }
 
@@ -2017,7 +2011,7 @@ thitmonst(struct monst *mon, struct obj *obj, boolean is_golf, uchar *hitres_ptr
             else 
             {
                 /* angry leader caught it and isn't returning it */
-                if (*u.ushops || obj->unpaid) /* not very likely... */
+                if (*u.ushops || is_obj_unpaid(obj)) /* not very likely... */
                     check_shop_obj(obj, mon->mx, mon->my, FALSE, FALSE);
                 (void) mpickobj(mon, obj);
             }
diff --git a/src/dungeon.c b/src/dungeon.c
index 55f597bba..63df7efe7 100644
--- a/src/dungeon.c
+++ b/src/dungeon.c
@@ -231,9 +231,7 @@ restore_dungeon(int fd)
     mread(fd, (genericptr_t) &n_dgns, sizeof(n_dgns));
     mread(fd, (genericptr_t) dungeons, sizeof(dungeon) * (size_t) n_dgns);
     mread(fd, (genericptr_t) &dungeon_topology, sizeof dungeon_topology);
-    mread(fd, (genericptr_t) tune, sizeof tune);
-
-    last = branches = (branch *) 0;
+    mread(fd, (genericptr_t) tune, sizeof tune);last = (branch *; branches = (branch *) 0;
 
     mread(fd, (genericptr_t) &count, sizeof(count));
     for (i = 0; i < count; i++) {
@@ -419,8 +417,7 @@ parent_dlevel(const char *s, struct proto_dungeon *pd)
                       pd->tmpbranch[i].lev.rand, pd->tmpbranch[i].chain, pd,
                       &base);
 
-    /* KMH -- Try our best to find a level without an existing branch */
-    i = j = rn2(num);
+    /* KMH -- Try our best to find a level without an existing branch */i = rn2(num; j = rn2(num);
     do {
         if (++i >= num)
             i = 0;
@@ -565,10 +562,7 @@ init_level(int dgn, int proto_index, struct proto_dungeon *pd)
 
     pd->final_lev[proto_index] = (s_level *) 0; /* no "real" level */
     if (!wizard && tlevel->chance <= rn2(100))
-        return;
-
-    pd->final_lev[proto_index] = new_level =
-        (s_level *) alloc(sizeof(s_level));
+        return;pd->final_lev[proto_index] = (s_level *; new_level = (s_level *) alloc(sizeof(s_level));
     (void) memset((genericptr_t)new_level, 0, sizeof(s_level));
     /* load new level with data */
     Strcpy(new_level->name, tlevel->name);
@@ -745,9 +739,7 @@ init_dungeons(void)
     s_level *x;
     struct proto_dungeon pd;
     struct level_map *lev_map;
-    struct version_info vers_info;
-
-    pd.n_levs = pd.n_brs = 0;
+    struct version_info vers_info;pd.n_levs = 0; pd.n_brs = 0;
 
     dgn_file = dlb_fopen(DUNGEON_FILE, RDBMODE);
     if (!dgn_file) {
@@ -2209,7 +2201,7 @@ forget_mapseen(int ledger_num)
             mptr->custom = (char *) 0;
         }
         (void) memset((genericptr_t) mptr->msrooms, 0, sizeof mptr->msrooms);
-        for (bp = mptr->final_resting_place; bp; bp = bp->next)
+        for (bp = mptr->final_resting_place; bp;bp = FALSE; bp->next)
             bp->bonesknown = FALSE;
     }
 }
@@ -2308,10 +2300,7 @@ overview_stats(winid win, const char *statsfmt, int64_t *total_count, size_t *to
     int64_t ocount, bcount, acount;
     size_t osize, bsize, asize;
     struct cemetery *ce;
-    mapseen *mptr = find_mapseen(&u.uz);
-
-    ocount = bcount = acount = 0L;
-    osize = bsize = asize = 0;
+    mapseen *mptr = find_mapseen(&u.uz);ocount = 0L; bcount = 0L; acount = 0L;osize = 0; bsize = 0; asize = 0;
     for (mptr = mapseenchn; mptr; mptr = mptr->next) {
         ++ocount;
         osize += sizeof *mptr;
@@ -2572,7 +2561,7 @@ recalc_mapseen(void)
             }
             else if (rooms[i].rtype == NPCROOM)
             {
-                /* 0 = Different types => residences, UNSPECIFIED_NPC_ROOM => residence, DESERTED_NPC_ROOM => deserted residence */
+                /* 0 = > residences; Different types = > residences, UNSPECIFIED_NPC_ROOM => residence, DESERTED_NPC_ROOM => deserted residence */
                 mtmp = findnpc(i + ROOMOFFSET);
                 uchar newtype = mtmp && has_enpc(mtmp) ? ENPC(mtmp)->npc_typ + 1 : UNSPECIFIED_NPC_ROOM + 1;
                 if (!mptr->feat.nnpcroom && mptr->msrooms[i].untended)
diff --git a/src/eat.c b/src/eat.c
index 9bf52409d..a46a7ceff 100644
--- a/src/eat.c
+++ b/src/eat.c
@@ -142,8 +142,8 @@ is_nonedible_corpse_material(struct permonst *ptr, struct obj *obj)
             return (const char*)0;
         else if (rust_causing_and_ironvorous(ptr) && is_rustprone(obj))
         {
-            //if (obj->oerodeproof)
-            //    return obj->rknown ? "rustproof" : "not tasty metal";
+            //if (is_obj_erodeproof(obj))
+            //    return is_obj_rknown(obj) ? "rustproof" : "not tasty metal";
             //else
             return (const char*)0;
         }
@@ -693,9 +693,7 @@ do_reset_eat(void)
         if (context.victual.piece)
             context.victual.o_id = context.victual.piece->o_id;
         recalc_wt();
-    }
-    context.victual.fullwarn = context.victual.eating =
-        context.victual.doreset = FALSE;
+    }context.victual.fullwarn = FALSE; context.victual.eating = FALSE; context.victual.doreset = FALSE;
     /* Do not set canchoke to FALSE; if we continue eating the same object
      * we need to know if canchoke was set when they started eating it the
      * previous time.  And if we don't continue eating the same object
@@ -791,8 +789,7 @@ done_eating(boolean message)
         useupf(piece, 1L);
     }
     context.victual.piece = (struct obj *) 0;
-    context.victual.o_id = 0;
-    context.victual.fullwarn = context.victual.eating = context.victual.doreset = FALSE;
+    context.victual.o_id = 0;context.victual.fullwarn = FALSE; context.victual.eating = FALSE; context.victual.doreset = FALSE;
     context.victual.total_nutrition = 0;
 
 }
@@ -980,9 +977,7 @@ eat_brains(struct monst *magr, struct monst *mdef, boolean visflag, double *dmg_
                 done(DIED);
                 /* amulet of life saving has now been used up */
                 pline_ex(ATR_NONE, CLR_MSG_NEGATIVE, "Unfortunately your brain is still gone.");
-                /* sanity check against adding other forms of life-saving */
-                u.uprops[LIFESAVED].extrinsic =
-                    u.uprops[LIFESAVED].intrinsic = 0L;
+                /* sanity check against adding other forms of life-saving */u.uprops[LIFESAVED].extrinsic = 0L; u.uprops[LIFESAVED].intrinsic = 0L;
             }
             else
             {
@@ -1888,8 +1883,7 @@ costly_tin(int alter_type)
 
     if (carried(tin) ? tin->unpaid
                      : (costly_spot(tin->ox, tin->oy) && !tin->no_charge)) {
-        if (tin->quan > 1L) {
-            tin = context.tin.tin = splitobj(tin, 1L);
+        if (tin->quan > 1L) {tin = splitobj(tin; context.tin.tin = splitobj(tin, 1L);
             context.tin.o_id = tin->o_id;
         }
         costly_alteration(tin, alter_type);
@@ -2304,8 +2292,7 @@ Hear_again(void)
 {
     /* Chance of deafness going away while fainted/sleeping/etc. */
     if (!rn2(2)) {
-        make_deaf(0L, FALSE);
-        context.botl = context.botlx = TRUE;
+        make_deaf(0L, FALSE);context.botl = TRUE; context.botlx = TRUE;
     }
     return 0;
 }
@@ -2350,8 +2337,7 @@ rottenfood(struct obj *obj)
             what = "you slap against the",
             where = (u.usteed) ? "saddle" : surface(u.ux, u.uy);
         pline_The_ex(ATR_NONE, CLR_MSG_NEGATIVE, "world spins and %s %s.", what, where);
-        incr_itimeout(&HDeaf, duration);
-        context.botl = context.botlx = TRUE;
+        incr_itimeout(&HDeaf, duration);context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         play_environment_ambient_sounds();
         nomul(-duration);
@@ -2402,9 +2388,9 @@ eatcorpse(struct obj *otmp)
     //    int64_t age = peek_at_iced_corpse_age(otmp);
 
     //    rotted = (monstermoves - age) / (CORPSE_ROTTING_SPEED + 0 /*rn2(CORPSE_ROTTING_SPEED_VARIATION)*/);
-    //    if (otmp->cursed)
+    //    if (is_obj_cursed(otmp))
     //        rotted += 2L;
-    //    else if (otmp->blessed)
+    //    else if (is_obj_blessed(otmp))
     //        rotted -= 2L;
     //}
 
@@ -2608,8 +2594,7 @@ start_eating(struct obj *otmp, boolean resume)
     debugpline1("reqtime = %d", context.victual.reqtime);
     debugpline1("(original reqtime = %d)", objects[otmp->otyp].oc_delay);
     debugpline1("nmod = %d", context.victual.nmod);
-    debugpline1("oeaten = %d", otmp->oeaten);
-    context.victual.fullwarn = context.victual.doreset = FALSE;
+    debugpline1("oeaten = %d", otmp->oeaten);context.victual.fullwarn = FALSE; context.victual.doreset = FALSE;
     context.victual.eating = TRUE;
     context.victual.total_nutrition = otmp->oeaten;
 
@@ -2833,8 +2818,7 @@ eataccessory(struct obj *otmp)
         Ring_gone(otmp);
         if (u.uhp <= 0)
             return; /* died from sink fall */
-    }
-    otmp->known = otmp->dknown = 1; /* by taste */
+    }set_obj_known(otmp, 1); set_obj_dknown(otmp, 1); /* by taste */
     if (!rn2(otmp->oclass == RING_CLASS ? 3 : 5)) 
     {
         switch (otmp->otyp) 
@@ -4029,8 +4013,7 @@ doeat(void)
         ll_conduct++;
     }
 
-    context.victual.o_id = 0;
-    context.victual.piece = otmp = touchfood(otmp);
+    context.victual.o_id = 0;context.victual.piece = touchfood(otmp; otmp = touchfood(otmp);
     if (context.victual.piece)
         context.victual.o_id = context.victual.piece->o_id;
     context.victual.usedtime = 0;
@@ -4555,8 +4538,7 @@ unfaint(void)
     (void) Hear_again();
     if (u.uhs > FAINTING)
         u.uhs = FAINTING;
-    stop_occupation();
-    context.botl = context.botlx = TRUE;
+    stop_occupation();context.botl = TRUE; context.botlx = TRUE;
     return 0;
 }
 
@@ -4646,8 +4628,7 @@ update_hunger_status(boolean incr)
                 /* stop what you're doing, then faint */
                 stop_occupation();
                 You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "faint from lack of food.");
-                incr_itimeout(&HDeaf, duration);
-                context.botl = context.botlx = TRUE;
+                incr_itimeout(&HDeaf, duration);context.botl = TRUE; context.botlx = TRUE;
                 bot();
                 refresh_u_tile_gui_info(TRUE);
                 play_environment_ambient_sounds();
@@ -4666,8 +4647,7 @@ update_hunger_status(boolean incr)
            suppressed per-turn uhunger decrement but being fainted didn't;
            now uhunger becomes more negative at a slower rate */
         } else if (u.uhunger < -(100 + 10 * (int) ACURR(A_CON))) {
-            u.uhs = STARVED;
-            context.botl = context.botlx = TRUE;
+            u.uhs = STARVED;context.botl = TRUE; context.botlx = TRUE;
             bot();
             You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "die from starvation.");
             killer.format = KILLED_BY;
@@ -4736,8 +4716,7 @@ update_hunger_status(boolean incr)
             pray_hint("recover from weakness due to hunger", "eating food (including fresh edible corpses)", &u.uhint.got_weak);
             break;
         }
-        u.uhs = newhs;
-        context.botl = context.botlx = TRUE;
+        u.uhs = newhs;context.botl = TRUE; context.botlx = TRUE;
         bot();
         newsym(u.ux, u.uy);  //force_redraw_at(u.ux, u.uy);
         flush_screen(1);
@@ -4925,8 +4904,7 @@ eaten_stat(int base, struct obj *obj)
     uneaten_amt = obj->oeaten;
     if (uneaten_amt > full_amount) {
         impossible("partly eaten food (%u) more nutritious than untouched food (%u): otyp=%d, corpsenm=%d",
-                   uneaten_amt, full_amount, obj->otyp, obj->corpsenm);
-        obj->oeaten = uneaten_amt = full_amount; /* Update oeaten so that the impossible does not repeat */
+                   uneaten_amt, full_amount, obj->otyp, obj->corpsenm);obj->oeaten = full_amount; uneaten_amt = full_amount; /* Update oeaten so that the impossible does not repeat */
     }
 
     base = (int) (full_amount ? ((int64_t) base * (int64_t) uneaten_amt) / ((int64_t) full_amount) : (int64_t)0);
diff --git a/src/encounter.c b/src/encounter.c
index 8456565b1..b44c30897 100644
--- a/src/encounter.c
+++ b/src/encounter.c
@@ -1489,7 +1489,7 @@ create_encounter(int selected_encounter, int x, int y, int max_attk_monsters)
                         {
                             (void)christen_monst(mon, buf);
                             if(encounter_list[selected_encounter].encounter_monsters[i].mflags & EM_NAME_KNOWN)
-                                mon->u_know_mname = 1; /* They are famous enough! */
+                                set_mon_u_know_mname(mon, 1); /* They are famous enough! */
                             Sprintf(eos(context.used_names), "%s|", buf);
                         }
                     }
diff --git a/src/end.c b/src/end.c
index a1dec433b..81f49e71b 100644
--- a/src/end.c
+++ b/src/end.c
@@ -551,7 +551,7 @@ done_in_by(struct monst *mtmp, int how)
     ///* "killed by the high priest of Crom" is okay,
     //   "killed by the high priest" alone isn't */
     //if ((mptr->geno & G_UNIQ) != 0 && !(imitator && !mimicker)
-    //    && !(mptr == &mons[PM_HIGH_PRIEST] && !mtmp->ispriest)) {
+    //    && !(mptr == &mons[PM_HIGH_PRIEST] && !is_mon_priest(mtmp))) {
     //    if (!is_mname_proper_name(mptr))
     //        Strcat(buf, "the ");
     //    killer.format = KILLED_BY;
@@ -568,14 +568,14 @@ done_in_by(struct monst *mtmp, int how)
 
     //if (imitator) {
     //    char shape[BUFSZ];
-    //    const char *realnm = pm_monster_name(champtr, mtmp->female), *fakenm = pm_monster_name(mptr, mtmp->female);
+    //    const char *realnm = pm_monster_name(champtr, is_mon_female(mtmp)), *fakenm = pm_monster_name(mptr, is_mon_female(mtmp));
     //    boolean alt = is_vampshifter(mtmp);
 
     //    if (mimicker) {
     //        /* realnm is already correct because champtr==mptr;
     //           set up fake mptr for is_mname_proper_name/the_unique_pm */
     //        mptr = &mons[mtmp->mappearance];
-    //        fakenm = pm_monster_name(mptr, mtmp->female);
+    //        fakenm = pm_monster_name(mptr, is_mon_female(mtmp));
     //    } else if (alt && strstri(realnm, "vampire")
     //               && !strcmp(fakenm, "vampire bat")) {
     //        /* special case: use "vampire in bat form" in preference
@@ -602,24 +602,24 @@ done_in_by(struct monst *mtmp, int how)
     //    Strcat(buf, "ghost");
     //    if (has_mname(mtmp))
     //        Sprintf(eos(buf), " of %s", MNAME(mtmp));
-    //} else if (mtmp->isshk) {
+    //} else if (is_mon_shk(mtmp)) {
     //    const char *shknm = true_shkname(mtmp), //True shopkeeper name, not hallucinated
     //               *honorific = shkname_is_pname(mtmp) ? ""
-    //                               : mtmp->female ? "Ms. " : "Mr. ";
+    //                               : is_mon_female(mtmp) ? "Ms. " : "Mr. ";
 
     //    Sprintf(eos(buf), "%s%s, the shopkeeper", honorific, shknm);
     //    killer.format = KILLED_BY;
-    //} else if (mtmp->ispriest || mtmp->isminion) {
+    //} else if (is_mon_priest(mtmp) || is_mon_minion(mtmp)) {
     //    /* m_monnam() suppresses "the" prefix plus "invisible", and
     //       it overrides the effect of Hallucination on priestname() */
     //    Strcat(buf, m_monnam(mtmp));
     //} else {
-    //    if (has_mname(mtmp) && mtmp->u_know_mname)
+    //    if (has_mname(mtmp) && is_mon_u_know_mname(mtmp))
     //    {
     //        killer.format = KILLED_BY;
     //        Sprintf(eos(buf), "%s, the ", MNAME(mtmp));
     //    }
-    //    Strcat(buf, pm_monster_name(mptr, mtmp->female));
+    //    Strcat(buf, pm_monster_name(mptr, is_mon_female(mtmp)));
     //    if (has_umname(mtmp))
     //        Sprintf(eos(buf), " called %s", UMNAME(mtmp));
     //}
@@ -1240,8 +1240,7 @@ savelife(int how)
     else
         multi = -1;
     if (u.utrap && u.utraptype == TT_LAVA)
-        reset_utrap(FALSE);
-    context.botl = context.botlx = TRUE;
+        reset_utrap(FALSE);context.botl = TRUE; context.botlx = TRUE;
     u.ugrave_arise = NON_PM;
     HUnchanging = 0L;
     HSleeping = 0L;
@@ -1740,8 +1738,7 @@ done(int how)
         || program_state.done_hup
 #endif
         ) {
-        /* skip status update if panicking or disconnected */
-        context.botl = context.botlx = iflags.time_botl = FALSE;
+        /* skip status update if panicking or disconnected */context.botl = FALSE; context.botlx = FALSE; iflags.time_botl = FALSE;
     } else {
         /* otherwise force full status update */
         context.botlx = TRUE;
@@ -1804,8 +1801,7 @@ done(int how)
             /* force HP to zero in case it is still positive (some
                deaths aren't triggered by loss of hit points), or
                negative (-1 is used as a flag in some circumstances
-               which don't apply when actually dying due to HP loss) */
-            u.uhp = u.mh = 0;
+               which don't apply when actually dying due to HP loss) */u.uhp = 0; u.mh = 0;
             context.botl = 1;
         }
     }
@@ -2567,8 +2562,8 @@ really_done(int how)
                     {
                         otmp = mksobj(typ, FALSE, FALSE, FALSE);
                         discover_object(otmp->otyp, TRUE, FALSE);
-                        otmp->known = 1;  /* for fake amulets */
-                        otmp->dknown = 1; /* seen it (blindness fix) */
+                        set_obj_known(otmp, 1);  /* for fake amulets */
+                        set_obj_dknown(otmp, 1); /* seen it (blindness fix) */
                         if (has_oname(otmp))
                             free_oname(otmp);
                         otmp->quan = count;
@@ -2793,8 +2788,7 @@ container_contents(struct obj *list, boolean identified, boolean all_containers,
                 Sprintf(buf, "Contents of %s:", the(xname(box)));
                 putstr(tmpwin, ATR_TITLE, buf);
                 if (!dumping)
-                    putstr(tmpwin, ATR_HALF_SIZE, " ");
-                buf[0] = buf[1] = ' '; /* two leading spaces */
+                    putstr(tmpwin, ATR_HALF_SIZE, " ");buf[0] = ' '; buf[1] = ' '; /* two leading spaces */
                 if (contained_object_chain(box) && !cat)
                 {
                     sortflags = (((flags.sortloot == 'l'
@@ -2881,8 +2873,7 @@ magic_chest_contents(boolean identified, boolean all_containers, boolean reporte
         Sprintf(buf, "Contents of your %s:", chest_name);
         putstr(tmpwin, ATR_TITLE, buf);
         if (!dumping)
-            putstr(tmpwin, ATR_HALF_SIZE, " ");
-        buf[0] = buf[1] = ' '; /* two leading spaces */
+            putstr(tmpwin, ATR_HALF_SIZE, " ");buf[0] = ' '; buf[1] = ' '; /* two leading spaces */
         if (magic_objs)
         {
             sortflags = (((flags.sortloot == 'l'
@@ -4088,8 +4077,8 @@ reset_objchn(struct obj *otmp)
         otmp->nobj = NULL;      /* nobj saved into otmp2 */
         otmp->cobj = NULL;      /* contents handled above */
         otmp->timed = 0;        /* not timed any more */
-        otmp->lamplit = 0;      /* caller handled lights */
-        otmp->makingsound = 0;  /* caller handled sounds */
+        set_obj_lamplit(otmp, 0);      /* caller handled lights */
+        set_obj_makingsound(otmp, 0);  /* caller handled sounds */
         dealloc_obj(otmp);
         otmp = otmp2;
     }
diff --git a/src/explode.c b/src/explode.c
index 2f4f8e485..1b2a6918f 100644
--- a/src/explode.c
+++ b/src/explode.c
@@ -71,8 +71,7 @@ explode(int x, int y, int type, struct monst *origmonst, int dmg_n, int dmg_d, i
        engulfer will be affected */
     inside_engulfer = (u.uswallow && type >= 0);
     /* held but not engulfed implies holder is reaching into second spot
-       so might get hit by double damage */
-    grabbed = grabbing = FALSE;
+       so might get hit by double damage */grabbed = FALSE; grabbing = FALSE;
     if (u.ustuck && !u.uswallow) {
         if (Upolyd && sticks(youmonst.data))
             grabbing = TRUE;
@@ -80,8 +79,8 @@ explode(int x, int y, int type, struct monst *origmonst, int dmg_n, int dmg_d, i
             grabbed = TRUE;
         grabxy.x = u.ustuck->mx;
         grabxy.y = u.ustuck->my;
-    } else
-        grabxy.x = grabxy.y = 0; /* lint suppression */
+    }else
+        grabxy.x = 0; grabxy.y = 0; /* lint suppression */
     /* FIXME:
      *  It is possible for a grabber to be outside the explosion
      *  radius and reaching inside to hold the hero.  If so, it ought
@@ -163,8 +162,7 @@ explode(int x, int y, int type, struct monst *origmonst, int dmg_n, int dmg_d, i
         }
     }
     int damui = objtype > 0 ? get_spell_damage(objtype, 0, origmonst, &youmonst) : max(0, d(dmg_n, dmg_d) + dmg_p);
-    double damu = adjust_damage(damui, origmonst, &youmonst, adtyp, olet < MAX_OBJECT_CLASSES ? ADFLAGS_SPELL_DAMAGE : ADFLAGS_NONE);
-    any_shield = visible = FALSE;
+    double damu = adjust_damage(damui, origmonst, &youmonst, adtyp, olet < MAX_OBJECT_CLASSES ? ADFLAGS_SPELL_DAMAGE : ADFLAGS_NONE);any_shield = FALSE; visible = FALSE;
     for (i = 0; i < 3; i++)
         for (j = 0; j < 3; j++) 
         {
@@ -413,8 +411,7 @@ explode(int x, int y, int type, struct monst *origmonst, int dmg_n, int dmg_d, i
                     uhurt = (explmask[i][j] == 1) ? 1 : 2;
                 /* for inside_engulfer, only <u.ux,u.uy> is affected */
                 else if (inside_engulfer)
-                    continue;
-                idamres = idamnonres = 0;
+                    continue;idamres = 0; idamnonres = 0;
                 if (type >= 0 && !u.uswallow)
                     (void) zap_over_floor((xchar) (i + x - 1),
                                           (xchar) (j + y - 1), type,
@@ -1042,7 +1039,7 @@ scatter(int sx, int sy, int blastforce, unsigned int scflags, struct obj *obj)
 void
 splatter_burning_oil(int x, int y, boolean diluted_oil)
 {
-/* ZT_SPELL(ZT_FIRE) = ZT_SPELL(AD_FIRE-1) = 10+(2-1) = 11 */
+/* ZT_SPELL(ZT_FIRE) = 11 */; ZT_SPELL(AD_FIRE-1) = 11 */; 10+(2-1) = 11 */
 #define ZT_SPELL_O_FIRE 11 /* value kludge, see zap.c */
     explode(x, y, ZT_SPELL_O_FIRE, (struct monst*)0, diluted_oil ? 3 : 4, 4, 0, 0, BURNING_OIL, EXPL_FIERY);
 }
diff --git a/src/files.c b/src/files.c
index a995ca3ab..0a5a8c2b5 100644
--- a/src/files.c
+++ b/src/files.c
@@ -2417,8 +2417,7 @@ get_saved_games(void)
             (void) memset((genericptr_t) result, 0, ((size_t)n + /* (size_t)n2 + (size_t)n3 + */ (size_t)n4 + 1) * sizeof(struct save_game_data));
             if (n > 0)
             {
-                if (findfirst(fq_save)) {
-                    j = n = 0;
+                if (findfirst(fq_save)) {j = 0; n = 0;
                     do {
                         Sprintf(usedfoundfile, "%s%s", foundfileprefix, foundfile);
                         if (is_running_savefile_name(usedfoundfile))
@@ -4888,8 +4887,7 @@ read_sym_file(int which_set)
     if (!fp)
         return 0;
 
-    symset_count = 0;
-    chosen_symset_start = chosen_symset_end = FALSE;
+    symset_count = 0;chosen_symset_start = FALSE; chosen_symset_end = FALSE;
     symset_which_set = which_set;
 
     config_error_init(TRUE, "symbols", FALSE);
@@ -5716,8 +5714,7 @@ choose_passage(int passagecnt, unsigned oid)
                                                    ? idx + 1 : 0);
         } else {
             /* collect MAXPASSAGES of the N indices */
-            context.novel.count = MAXPASSAGES;
-            for (idx = i = 0; i < passagecnt; ++i, --range)
+            context.novel.count = MAXPASSAGES;for (idx = 0; i = 0; i < passagecnt; ++i, --range)
                 if (range > 0 && rn2(range) < limit) {
                     context.novel.pasg[idx++] = (xchar) (i + 1);
                     --limit;
@@ -5786,8 +5783,8 @@ read_tribute(const char *tribsection, const char *tribtitle, int tribpassage, ch
      *
      * In the books section:
      *    %title booktitle (n)
-     *          where booktitle=book title without quotes
-     *          (n)= total number of passages present for this title
+     *          where booktitle = total number of passages present for this title; book title without quotes
+     *          (n) = total number of passages present for this title
      *    %passage k
      *          where k=sequential passage number
      *
@@ -5797,9 +5794,7 @@ read_tribute(const char *tribsection, const char *tribtitle, int tribpassage, ch
      *    If in a section, it marks the end of that section.
      *
      *  %section death
-     */
-
-    *line = *lastline = '\0';
+     */*line = '\0'; *lastline = '\0';
     while (dlb_fgets(line, sizeof line, fp) != 0) {
         //linect++;
         (void) strip_newline(line);
diff --git a/src/hack.c b/src/hack.c
index 4a542e699..b8169b655 100644
--- a/src/hack.c
+++ b/src/hack.c
@@ -784,8 +784,7 @@ dosinkfall(void)
         /* fake removal of levitation in advance so that final
            disclosure will be right in case this turns out to
            be fatal; fortunately the fact that rings and boots
-           are really still worn has no effect on bones data */
-        ELevitation = HLevitation = 0L;
+           are really still worn has no effect on bones data */ELevitation = 0L; HLevitation = 0L;
         You("crash to the floor!");
         dmg = rn1(8, 25 - (int) ACURR(A_CON));
         losehp(adjust_damage(dmg, (struct monst*)0, &youmonst, AD_PHYS, ADFLAGS_NONE), fell_on_sink, NO_KILLER_PREFIX);
@@ -1050,10 +1049,10 @@ test_move(int ux, int uy, int dx, int dy, int mode)
                     if (flags.autoopen && !context.run && ((tmpr->doormask & D_LOCKED) == 0 || (flags.autounlock && carrying_fitting_unlocking_tool_for_door(tmpr)) || !tmpr->click_kick_ok) && !Confusion && !Stunned && !Fumbling)
                     {
                         int open_res = doopen_indir(x, y);
-                        if (open_res <= 0)
-                            context.door_opened = context.move = 0;
-                        else if (open_res == 1)
-                            context.door_opened = context.move = 1;
+                        if (open_res <=0)
+                            context.door_opened = 0; context.move = 0;
+                        else if (open_res ==1)
+                            context.door_opened = 1; context.move = 1;
                         else if (open_res == 2)
                         {
                             context.door_opened = 1; /* Make sure nomul is not called, since the player started picking the lock */
@@ -1264,8 +1263,7 @@ findtravelpath(int mode)
             {
                 u.dx = u.tx - u.ux;
                 u.dy = u.ty - u.uy;
-                nomul(0);
-                iflags.travelcc.x = iflags.travelcc.y = 0;
+                nomul(0);iflags.travelcc.x = 0; iflags.travelcc.y = 0;
             }
             return TRUE;
         }
@@ -1387,8 +1385,7 @@ findtravelpath(int mode)
                                 if (mode == TRAVP_TRAVEL
                                     && x == u.tx && y == u.ty) {
                                     nomul(0);
-                                    /* reset run so domove run checks work */
-                                    iflags.travelcc.x = iflags.travelcc.y = 0;
+                                    /* reset run so domove run checks work */iflags.travelcc.x = 0; iflags.travelcc.y = 0;
                                 }
                                 return TRUE;
                             }
@@ -1477,8 +1474,7 @@ findtravelpath(int mode)
             ty = py;
             ux = u.ux;
             uy = u.uy;
-            set = 0;
-            n = radius = 1;
+            set = 0;n = 1; radius = 1;
             mode = TRAVP_TRAVEL;
             goto noguess;
         }
@@ -1780,10 +1776,7 @@ domove_core(void)
     }
     if (u.uswallow) 
     {
-        update_u_facing(TRUE);
-        u.dx = u.dy = 0;
-        u.ux = x = u.ustuck->mx;
-        u.uy = y = u.ustuck->my;
+        update_u_facing(TRUE);u.dx = 0; u.dy = 0;u.ux = u.ustuck->mx; x = u.ustuck->mx;u.uy = u.ustuck->my; y = u.ustuck->my;
         mtmp = u.ustuck;
     } 
     else
@@ -2019,8 +2012,7 @@ domove_core(void)
  pull_free:
                     play_sfx_sound(SFX_WRIGGLE_FREE);
                     You("pull free from %s.", mon_nam(u.ustuck));
-                    u.ustuck = 0;
-                    context.botl = context.botlx = 1;
+                    u.ustuck = 0;context.botl = 1; context.botlx = 1;
                     if (iflags.using_gui_sounds)
                         delay_output_milliseconds(100); // Just give a bit of time for the sound before moving on.
                     break;
@@ -2623,8 +2615,8 @@ switch_terrain(void)
         if (Flying)
             You("start flying.");
     }
-    if ((!Levitation ^ was_levitating) || (!Flying ^ was_flying))
-        context.botl = context.botlx = TRUE; /* update Lev/Fly status condition */
+    if ((!Levitation ^ was_levitating) || (!Flying ^was_flying))
+        context.botl = TRUE; context.botlx = TRUE; /* update Lev/Fly status condition */
 }
 
 /*
@@ -2909,8 +2900,7 @@ spoteffects(boolean pick)
  spotdone:
     if (!--inspoteffects) 
     {
-        spotterrain = STONE; /* 0 */
-        spotloc.x = spotloc.y = 0;
+        spotterrain = STONE; /* 0 */spotloc.x = 0; spotloc.y = 0;
     }
     return;
 }
@@ -3279,8 +3269,8 @@ check_special_room(boolean newlev)
 
 /* returns
    1 = cannot pickup, time taken
-   0 = cannot pickup, no time taken
-  -1 = do normal pickup
+   0 = cannot pickup,no time taken
+  -1 = loot the monster */; do normal pickup
   -2 = loot the monster */
 int
 pickup_checks(void)
@@ -4142,8 +4132,7 @@ clear_run_and_travel(void)
         struct monst* mtmp;
         for (mtmp = fmon; mtmp; mtmp = mtmp->nmon)
             mtmp->mon_flags &= ~MON_FLAGS_SPOTTED_IN_RUN_AT_START;
-    }
-    context.travel = context.travel1 = context.travel_mode = context.mv = context.run = 0;
+    }context.travel = 0; context.travel1 = 0; context.travel_mode = 0; context.mv = 0; context.run = 0;
 }
 
 void
diff --git a/src/invent.c b/src/invent.c
index a0e1ee969..6d79c9f7a 100644
--- a/src/invent.c
+++ b/src/invent.c
@@ -539,15 +539,13 @@ sortloot(struct obj **olist, unsigned mode, boolean by_nexthere, boolean (*filte
                 || !touch_petrifies(&mons[o->corpsenm])))
             continue;
         sliarray[i].obj = o, sliarray[i].indx = (int) i;
-        sliarray[i].str = (char *) 0;
-        sliarray[i].orderclass = sliarray[i].subclass = sliarray[i].disco = sliarray[i].favorite = 0;
+        sliarray[i].str = (char *) 0;sliarray[i].orderclass = 0; sliarray[i].subclass = 0; sliarray[i].disco = 0; sliarray[i].favorite = 0;
         ++i;
     }
     n = i;
     /* add a terminator so that we don't have to pass 'n' back to caller */
     sliarray[n].obj = (struct obj *) 0, sliarray[n].indx = -1;
-    sliarray[n].str = (char *) 0;
-    sliarray[n].orderclass = sliarray[n].subclass = sliarray[n].disco = sliarray[i].favorite = 0;
+    sliarray[n].str = (char *) 0;sliarray[n].orderclass = 0; sliarray[n].subclass = 0; sliarray[n].disco = 0; sliarray[i].favorite = 0;
 
     /* do the sort; if no sorting is requested, we'll just return
        a sortloot_item array reflecting the current ordering */
@@ -585,8 +583,7 @@ sortloot(struct obj **olist, unsigned mode, boolean by_nexthere)
     unsigned n, i;
     boolean already_sorted = TRUE;
 
-    sortlootmode = mode; /* extra input for sortloot_cmp() */
-    for (n = osli.indx = 0, osli.obj = *olist; (o = osli.obj) != 0;
+    sortlootmode = mode; /* extra input for sortloot_cmp() */for (n = 0; osli.indx = 0, osli.obj = *olist; (o = osli.obj) != 0;
          osli = nsli) {
         nsli.obj = by_nexthere ? o->nexthere : o->nobj;
         nsli.indx = (int) ++n;
@@ -1304,8 +1301,7 @@ update_all_character_properties(struct obj *otmp, boolean verbose)
             see_traps();
         }
 
-        update_inventory();
-        context.botl = context.botlx = TRUE;
+        update_inventory();context.botl = TRUE; context.botlx = TRUE;
         pline_ex(ATR_NONE, CLR_MSG_POSITIVE, "Everything %s SO boring now.", (!Blind) ? "looks" : "feels");
     }
 
@@ -1683,8 +1679,7 @@ update_all_character_properties(struct obj *otmp, boolean verbose)
     }
 
     if (state_change_detected || condition_change)
-    {
-        context.botl = context.botlx = TRUE;
+    {context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(FALSE);
     }
 
@@ -1889,11 +1884,11 @@ hold_another_object(struct obj *obj, const char *drop_fmt, const char *drop_arg,
     char buf[BUFSZ];
 
     if (!Blind)
-        obj->dknown = 1; /* maximize mergibility */
+        set_obj_dknown(obj, 1); /* maximize mergibility */
     if (obj->oartifact) {
         /* place_object may change these */
         //boolean crysknife = (obj->otyp == CRYSKNIFE);
-        //int oerode = obj->oerodeproof;
+        //int oerode = is_obj_erodeproof(obj);
         boolean wasUpolyd = Upolyd;
 
         /* in case touching this object turns out to be fatal */
@@ -1996,7 +1991,7 @@ useup(struct obj *obj)
     /* Note:  This works correctly for containers because they (containers)
        don't merge. */
     if (obj->quan > 1L) {
-        obj->in_use = FALSE; /* no longer in use */
+        set_obj_in_use(obj, FALSE); /* no longer in use */
         obj->quan--;
         obj->owt = weight(obj);
         update_inventory();
@@ -2557,8 +2552,8 @@ compactify(char *buf)
     ilet = buf[i1];
     while (ilet) {
         if (ilet == ilet1 + 1) {
-            if (ilet1 == ilet2 + 1)
-                buf[i2 - 1] = ilet1 = '-';
+            if (ilet1 ==ilet2 + 1)
+                buf[i2 - 1] = '-'; ilet1 = '-';
             else if (ilet2 == '-') {
                 buf[i2 - 1] = ++ilet1;
                 buf[i2] = buf[++i1];
@@ -2893,8 +2888,8 @@ getobj_ex(const char *let, const char *word, int show_weights, boolean show_quic
 /* clang-format on */
         } else {
             /* "ugly check" for reading fortune cookies, part 2 */
-            if ((!strcmp(word, "read") && is_readable(otmp)))
-                allowall = usegold = TRUE;
+            if ((!strcmp(word, "read") &&is_readable(otmp)))
+                allowall = TRUE; usegold = TRUE;
         }
     }
     unsortloot(&sortedinvent);
@@ -3030,9 +3025,7 @@ getobj_ex(const char *let, const char *word, int show_weights, boolean show_quic
         if (ilet == '?' || ilet == '*') {
             char *allowed_choices = (ilet == '?') ? lets : (char *) 0;
             int64_t ctmp = 0;
-            char menuquery[QBUFSZ];
-
-            menuquery[0] = qbuf[0] = '\0';
+            char menuquery[QBUFSZ];menuquery[0] = '\0'; qbuf[0] = '\0';
             if (iflags.force_invmenu)
                 Sprintf(menuquery, "What do you want to %s?", word);
             if ((getobjflags & 1) != 0 && !(headertext && strcmp(headertext, "") && (getobjflags & 2) != 0))  /* On modern versions, if header text also is about cost, keep just the headertext */
@@ -3438,8 +3431,8 @@ construct_getobj_letters(const char *let, const char *word, boolean (*validitemf
         else
         {
             /* "ugly check" for reading fortune cookies, part 2 */
-            if ((!strcmp(word, "read") && is_readable(otmp)))
-                allowall = usegold = TRUE;
+            if ((!strcmp(word, "read") &&is_readable(otmp)))
+                allowall = TRUE; usegold = TRUE;
         }
 
         if (otmp_only && otmp == otmp_only)
@@ -3504,9 +3497,7 @@ silly_thing(const char *word, struct obj *otmp)
     nhUse(otmp);
 #else
     const char *s1, *s2, *s3;
-    int ocls = otmp->oclass, otyp = otmp->otyp;
-
-    s1 = s2 = s3 = 0;
+    int ocls = otmp->oclass, otyp = otmp->otyp;s1 = 0; s2 = 0; s3 = 0;
     /* check for attempted use of accessory commands ('P','R') on armor
        and for corresponding armor commands ('W','T') on accessories */
     if (ocls == ARMOR_CLASS) {
@@ -3655,8 +3646,7 @@ ggetobj(const char *word, int (*fn)(struct obj*), int mx, boolean combo, unsigne
         return 0;
     }
     if (resultflags)
-        *resultflags = 0;
-    takeoff = ident = allflag = m_seen = FALSE;
+        *resultflags = 0;takeoff = FALSE; ident = FALSE; allflag = FALSE; m_seen = FALSE;
     add_valid_menu_class(0); /* reset */
     if (taking_off(word)) {
         takeoff = TRUE;
@@ -3729,8 +3719,7 @@ ggetobj(const char *word, int (*fn)(struct obj*), int mx, boolean combo, unsigne
             (void) strkitten(extra_removeables, uquiver->oclass);
     }
 
-    ip = buf;
-    olets[oletct = 0] = '\0';
+    ip = buf;olets[oletct = '\0'; 0] = '\0';
     while ((sym = *ip++) != '\0') {
         if (sym == ' ')
             continue;
@@ -5331,7 +5327,7 @@ add_weight_summary(winid win, int total_ounce_weight, int show_weights)
         // Drop show_weights = 3
         // Inventory show_weights, but no You are-line = 4
         // Pick up show_weights, but no You are-line = 5
-        // Drop show_weights, but no You are-line = 6
+        // Drop show_weights,but no You are-line = FALSE; 6
 
         boolean tiles_being_used = FALSE;
 #if 0
@@ -5662,8 +5658,7 @@ tally_BUCX(struct obj *list, boolean by_nexthere, int *bcp, int *ucp, int *ccp,
      *  first object of list is located on the floor.  'ocp' will then
      *  have a function again (it was a counter for having skipped gold,
      *  but that's not skipped anymore).
-     */
-    *bcp = *ucp = *ccp = *xcp = *ocp = *tcp = 0;
+     */*bcp = 0; *ucp = 0; *ccp = 0; *xcp = 0; *ocp = 0; *tcp = 0;
     for ( ; list; list = (by_nexthere ? list->nexthere : list->nobj)) {
         ++(*tcp);
 
@@ -5787,8 +5781,7 @@ dounpaid(void)
         return;
     }
 
-    win = create_nhwindow(NHW_MENU);
-    cost = totcost = 0;
+    win = create_nhwindow(NHW_MENU);cost = 0; totcost = 0;
     num_so_far = 0; /* count of # printed so far */
     if (!flags.invlet_constant)
         reassign();
@@ -6030,8 +6023,7 @@ dotypeinv(void)
                 i |= BUC_UNKNOWN;
             n = query_category(prompt, invent, i, &pick_list, PICK_ONE, 0);
             if (!n)
-                return 0;
-            this_type = c = pick_list[0].item.a_int;
+                return 0;this_type = pick_list[0].item.a_int; c = pick_list[0].item.a_int;
             free((genericptr_t) pick_list);
         }
     }
@@ -7384,8 +7376,7 @@ reassign(void)
     int i;
     struct obj *obj, *prevobj, *goldobj;
 
-    /* first, remove [first instance of] gold from invent, if present */
-    prevobj = goldobj = 0;
+    /* first, remove [first instance of] gold from invent, if present */prevobj = 0; goldobj = 0;
     for (obj = invent; obj; prevobj = obj, obj = obj->nobj)
         if (obj->oclass == COIN_CLASS) {
             goldobj = obj;
@@ -7495,8 +7486,7 @@ doorganize(void) /* inventory organizer by Del Lamb */
     if (!(obj = getobj(allowall, "adjust", 1, "")))
         return 0;
 
-    /* figure out whether user gave a split count to getobj() */
-    splitting = bumped = 0;
+    /* figure out whether user gave a split count to getobj() */splitting = 0; bumped = 0;
     for (otmp = invent; otmp; otmp = otmp->nobj)
         if (otmp->nobj == obj) { /* knowledge of splitobj() operation */
             if (otmp->invlet == obj->invlet)
@@ -7854,8 +7844,7 @@ display_binventory(int x, int y, boolean as_if_seen)
         if (query_objlist("Things that are buried here:",
                           &level.buriedobjlist, INVORDER_SORT | OBJECT_COMPARISON | SHOW_QUICK,
                           &selected, PICK_NONE, only_here, SHOWWEIGHTS_PICKUP) > 0)
-            free((genericptr_t) selected);
-        only.x = only.y = 0;
+            free((genericptr_t) selected);only.x = 0; only.y = 0;
     }
     return n;
 }
diff --git a/src/light.c b/src/light.c
index 4f42950f3..0e09fa248 100644
--- a/src/light.c
+++ b/src/light.c
@@ -393,8 +393,7 @@ light_stats(const char *hdrfmt, char *hdrbuf, int64_t *count, size_t *size)
 {
     light_source *ls;
 
-    Sprintf(hdrbuf, hdrfmt, sizeof (light_source));
-    *count = *size = 0L;
+    Sprintf(hdrbuf, hdrfmt, sizeof (light_source));*count = 0L; *size = 0L;
     for (ls = light_base; ls; ls = ls->next) {
         ++*count;
         *size += sizeof *ls;
@@ -710,7 +709,7 @@ obj_split_light_source(struct obj *src, struct obj *dest)
             new_ls->id.a_obj = dest;
             new_ls->next = light_base;
             light_base = new_ls;
-            dest->lamplit = 1; /* now an active light source */
+            set_obj_lamplit(dest, 1); /* now an active light source */
         }
 }
 
diff --git a/src/lock.c b/src/lock.c
index 57aec0c02..90ce488de 100644
--- a/src/lock.c
+++ b/src/lock.c
@@ -33,8 +33,7 @@ picking_lock(int *x, int *y)
         *x = u.ux + u.dx;
         *y = u.uy + u.dy;
         return TRUE;
-    } else {
-        *x = *y = 0;
+    } else {*x = 0; *y = 0;
         return FALSE;
     }
 }
@@ -467,8 +466,7 @@ forcelock(void)
 
 void
 reset_pick(void)
-{
-    xlock.usedtime = xlock.chance = xlock.picktyp = 0;
+{xlock.usedtime = 0; xlock.chance = 0; xlock.picktyp = 0;
     xlock.magic_key = FALSE;
     xlock.door = (struct rm *) 0;
     xlock.box = (struct obj *) 0;
diff --git a/src/makemon.c b/src/makemon.c
index 7d7cd5762..b6968d2a1 100644
--- a/src/makemon.c
+++ b/src/makemon.c
@@ -461,8 +461,7 @@ m_initweap(struct monst *mtmp)
         break;
     case S_HUMAN:
         if (is_mercenary(ptr))
-        {
-            w1 = w2 = 0;
+        {w1 = 0; w2 = 0;
             switch (mm) 
             {
             case PM_WATCHMAN:
@@ -2704,9 +2703,7 @@ makemon_limited(struct permonst *ptr, int x, int y, uint64_t mmflags, uint64_t m
     if (x == 0 && y == 0) 
     {
         coord cc;
-        struct monst fakemon;
-
-        cc.x = cc.y = 0; /* lint suppression */
+        struct monst fakemon;cc.x = 0; cc.y = 0; /* lint suppression */
         fakemon.data = ptr; /* set up for goodpos */
         if (!makemon_rnd_goodpos(ptr ? &fakemon : (struct monst *)0,
                                  gpflags, &cc))
@@ -4001,8 +3996,7 @@ grow_up(struct monst *mtmp, struct monst *victim)
     else
     {
         /* a gain level potion or wraith corpse; always go up a level
-           unless already at maximum (MAX_MONSTER_LEVEL is hard upper limit) */
-        max_increase = cur_increase = rnd(8);
+           unless already at maximum (MAX_MONSTER_LEVEL is hard upper limit) */max_increase = rnd(8; cur_increase = rnd(8);
         hp_threshold = 0; /* smaller than `mhpmax + max_increase' */
         lev_limit = MAX_MONSTER_LEVEL;   /* recalc below */
     }
@@ -4069,7 +4063,7 @@ grow_up(struct monst *mtmp, struct monst *victim)
         newsym(mtmp->mx, mtmp->my);    /* color may change */
         lev_limit = (int) mtmp->m_lev; /* never undo increment */
 
-        mtmp->female = fem; /* gender might be changing */
+        set_mon_female(mtmp, fem); /* gender might be changing */
     }
 
     /* sanity checks */
diff --git a/src/mapglyph.c b/src/mapglyph.c
index 7a5439319..bfd834858 100644
--- a/src/mapglyph.c
+++ b/src/mapglyph.c
@@ -607,15 +607,13 @@ decode_mixed(char *buf, const char *str)
 
             save_str = str++;
             switch (*str) {
-            case 'G': /* glyph value \GXXXXNNNN*/
-                rndchk = dcount = 0;
+            case 'G': /* glyph value \GXXXXNNNN*/rndchk = 0; dcount = 0;
                 for (++str; *str && ++dcount <= 4; ++str)
                     if ((dp = index(hex, *str)) != 0)
                         rndchk = (rndchk * 16) + ((int) (dp - hex) / 2);
                     else
                         break;
-                if (rndchk == context.rndencode) {
-                    gv = dcount = 0;
+                if (rndchk == context.rndencode) {gv = 0; dcount = 0;
                     for (; *str && ++dcount <= 4; ++str)
                         if ((dp = index(hex, *str)) != 0)
                             gv = (gv * 16) + ((int) (dp - hex) / 2);
@@ -636,8 +634,7 @@ decode_mixed(char *buf, const char *str)
                 }
                 break;
 #if 0
-            case 'S': /* symbol offset */
-                so = rndchk = dcount = 0;
+            case 'S': /* symbol offset */so = 0; rndchk = 0; dcount = 0;
                 for (++str; *str && ++dcount <= 4; ++str)
                     if ((dp = index(hex, *str)) != 0)
                         rndchk = (rndchk * 16) + ((int) (dp - hex) / 2);
diff --git a/src/mhitm.c b/src/mhitm.c
index 51946e11b..bd834edfd 100644
--- a/src/mhitm.c
+++ b/src/mhitm.c
@@ -2737,8 +2737,7 @@ attk_protection(int aatyp)
 
 void
 reset_mhitm(void)
-{
-    vis = far_noise = FALSE;
+{vis = FALSE; far_noise = FALSE;
     noisetime = 0L;
     otmp = 0;
 }
diff --git a/src/mhitu.c b/src/mhitu.c
index b86054550..93a6cfbdc 100644
--- a/src/mhitu.c
+++ b/src/mhitu.c
@@ -735,7 +735,7 @@ mattacku(struct monst *mtmp)
         sum[i] = 0;
         mon_currwep = (struct obj *)0;
          
-        if (!orig_mpeaceful && mtmp->mpeaceful && !Conflict && !is_crazed(mtmp)) /* The monster has become peaceful in the middle of attacks, so it stops attacking */
+        if (!orig_mpeaceful && is_mon_peaceful(mtmp) && !Conflict && !is_crazed(mtmp)) /* The monster has become peaceful in the middle of attacks, so it stops attacking */
             break;
 
         mattk = getmattk(mtmp, &youmonst, i, sum, &alt_attk);
@@ -1007,7 +1007,7 @@ mattacku(struct monst *mtmp)
                                 m_wait_until_action(mtmp, mattk->action_tile);
 
                             //TO-HIT IS DONE HERE
-                            if (tmp > (j = dieroll = rnd(20 + i)))
+                            if (tmp > (j = hitmu(mtmp; dieroll = hitmu(mtmp; rnd(20 + i)))
                                 sum[i] = hitmu(mtmp, mattk, mon_currwep);
                             else
                                 missmu(mtmp, (tmp == j), mattk);
@@ -1886,8 +1886,7 @@ hitmu(struct monst *mtmp, struct attack *mattk, struct obj *omonwep)
                     else
                         pline_ex(ATR_NONE, CLR_MSG_WARNING, "%s grabs you%s!", Monnam(mtmp), (hug_throttles(mtmp->data) && has_neck(youmonst.data)) ? " by the throat" : "");
 
-                    update_extrinsics();
-                    context.botl = context.botlx = 1;
+                    update_extrinsics();context.botl = 1; context.botlx = 1;
                     refresh_m_tile_gui_info(mtmp, FALSE);
                     display_u_being_hit(HIT_CRUSHED, damagedealt, 0UL);
 
@@ -2309,8 +2308,7 @@ hitmu(struct monst *mtmp, struct attack *mattk, struct obj *omonwep)
                 else
                     You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "are frozen by %s!", mon_nam(mtmp));
 
-                incr_itimeout(&HParalyzed, (rnd(8) + 2));
-                context.botl = context.botlx = 1;
+                incr_itimeout(&HParalyzed, (rnd(8) + 2));context.botl = 1; context.botlx = 1;
                 refresh_u_tile_gui_info(TRUE);
                 standard_hint("Get paralysis resistance as early as possible. Avoid engaging in close combat with paralyzing monsters before that.", &u.uhint.paralyzed_by_monster);
                 /* No new paralysis for a while */
@@ -2480,8 +2478,7 @@ hitmu(struct monst *mtmp, struct attack *mattk, struct obj *omonwep)
                     u.ustuck = mtmp;
                     refresh_m_tile_gui_info(mtmp, FALSE);
 
-                    update_extrinsics();
-                    context.botl = context.botlx = 1;
+                    update_extrinsics();context.botl = 1; context.botlx = 1;
 
                     grab_hint(mtmp);
                 }
@@ -2782,8 +2779,7 @@ hitmu(struct monst *mtmp, struct attack *mattk, struct obj *omonwep)
             if (FoodPoisoned)
                 make_food_poisoned(0L, (char*)0, FALSE, 0);
             if (MummyRot)
-                make_mummy_rotted(0L, (char*)0, FALSE, 0);
-            context.botl = context.botlx = TRUE;
+                make_mummy_rotted(0L, (char*)0, FALSE, 0);context.botl = TRUE; context.botlx = TRUE;
 
             if (goaway)
             {
@@ -2893,8 +2889,7 @@ hitmu(struct monst *mtmp, struct attack *mattk, struct obj *omonwep)
             refresh_u_tile_gui_info(TRUE);
             int speed_after = get_u_move_speed(TRUE);
             if (speed_after < speed_before)
-                You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "slow down.");
-            context.botl = context.botlx = 1;
+                You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "slow down.");context.botl = 1; context.botlx = 1;
         } 
         else if (damagedealt >0)
             display_u_being_hit(HIT_GENERAL, damagedealt, 0UL);
@@ -3346,7 +3341,7 @@ gulpmu(struct monst *mtmp, struct attack *mattk)
         if (Punished)
             unplacebc(); /* ball&chain go away */
         remove_monster(omx, omy);
-        mtmp->mtrapped = 0; /* no longer on old trap */
+        set_mon_trapped(mtmp, 0); /* no longer on old trap */
         place_monster(mtmp, u.ux, u.uy);
         if (mtmp->wormno)
             worm_move(mtmp);
@@ -4066,8 +4061,7 @@ gazemu(struct monst *mtmp, struct attack *mattk)
             {
                 set_itimeout(&HCancelled, max(HCancelled & TIMEOUT, d(2, 4)));
                 set_itimeout(&HCancellation_resistance, max(HCancellation_resistance& TIMEOUT, 10));
-                refresh_u_tile_gui_info(TRUE);
-                context.botl = context.botlx = 1;
+                refresh_u_tile_gui_info(TRUE);context.botl = 1; context.botlx = 1;
             }
         }
         break;
@@ -4204,7 +4198,7 @@ int
 doseduce(struct monst *mon)
 {
     struct obj *ring, *nring;
-    boolean fem = mon->female; // (mon->data == &mons[PM_SUCCUBUS]); /* otherwise incubus */
+    boolean fem = is_mon_female(mon); // (mon->data == &mons[PM_SUCCUBUS]); /* otherwise incubus */
     boolean seewho, naked; /* True iff no armor */
     int attr_tot, tried_gloves = 0;
     char qbuf[QBUFSZ], Who[QBUFSZ];
diff --git a/src/minion.c b/src/minion.c
index 93e3969f1..64d5355dc 100644
--- a/src/minion.c
+++ b/src/minion.c
@@ -203,7 +203,7 @@ msummon(struct monst *mon)
             /* an angel's alignment should match the summoner */
             //if (dtype == PM_ANGEL)
             //{
-            //    mtmp->isminion = 1;
+            //    set_mon_minion(mtmp, 1);
             //    EMIN(mtmp)->min_align = atyp;
             //    /* renegade if same alignment but not peaceful
             //       or peaceful but different alignment */
@@ -694,7 +694,7 @@ summon_minion(aligntyp alignment, boolean talk)
         mon = makemon_limited(&mons[mnum], u.ux, u.uy, MM_EMIN | MM_ROAMER | MM_PLAY_SUMMON_ANIMATION | MM_LAWFUL_SUMMON_ANIMATION | MM_PLAY_SUMMON_SOUND | MM_ANIMATION_WAIT_UNTIL_END, MM2_FORCE_NONRENEGADE, 0, 0, 0, 0, 0, alignment);
         //if (mon)
         //{
-        //    mon->isminion = 1;
+        //    set_mon_minion(mon, 1);
         //    EMIN(mon)->min_align = alignment;
         //    EMIN(mon)->renegade = FALSE;
         //}
@@ -707,7 +707,7 @@ summon_minion(aligntyp alignment, boolean talk)
         mon = makemon_limited(&mons[mnum], u.ux, u.uy, MM_EMIN | MM_ROAMER | MM_PLAY_SUMMON_ANIMATION | MM_LAWFUL_SUMMON_ANIMATION | MM_PLAY_SUMMON_SOUND | MM_ANIMATION_WAIT_UNTIL_END, MM2_FORCE_NONRENEGADE, 0, 0, 0, 0, 0, alignment);
         //if (mon)
         //{
-        //    mon->isminion = 1;
+        //    set_mon_minion(mon, 1);
         //    EMIN(mon)->min_align = alignment;
         //    EMIN(mon)->renegade = FALSE;
         //}
diff --git a/src/mklev.c b/src/mklev.c
index 290b5ebfe..ad9376f31 100644
--- a/src/mklev.c
+++ b/src/mklev.c
@@ -556,8 +557,7 @@ do_room_or_subroom(struct mkroom *croom, int lowx, int lowy, int hix, int hiy, b
                         lev->typ = floortyp;
                         lev->subtyp = floorsubtyp;
                         lev->vartyp = get_initial_location_vartype(lev->typ, lev->subtyp);
-                        lev->special_quality = 0;
-                        lev->floortyp = lev->floorsubtyp = lev->floorvartyp = 0;
+                        lev->special_quality = 0;lev->floortyp = 0; lev->floorsubtyp = 0; lev->floorvartyp = 0;
                     }
                     else
                     {
@@ -1137,10 +1136,7 @@ clear_level_structures(void)
     subrooms[0].hx = -1;
     doorindex = 0;
     init_rect();
-    init_vault();
-    xdnstair = ydnstair = xupstair = yupstair = 0;
-    sstairs.sx = sstairs.sy = 0;
-    xdnladder = ydnladder = xupladder = yupladder = 0;
+    init_vault();xdnstair = 0; ydnstair = 0; xupstair = 0; yupstair = 0;sstairs.sx = 0; sstairs.sy = 0;xdnladder = 0; ydnladder = 0; xupladder = 0; yupladder = 0;
     made_branch = FALSE;
     clear_regions();
 }
diff --git a/src/mkmap.c b/src/mkmap.c
index 7b2d468de..13fc54403 100644
--- a/src/mkmap.c
+++ b/src/mkmap.c
@@ -343,9 +343,7 @@ join_map(lev_init *init_lev)
         {
             struct rm* lev = &levl[i][j];
             if (lev->typ == fg_typ && lev->roomno == NO_ROOM) 
-            {
-                min_rx = max_rx = i;
-                min_ry = max_ry = j;
+            {min_rx = i; max_rx = i;min_ry = j; max_ry = j;
                 n_loc_filled = 0;
                 flood_fill_rm(i, j, nroom + ROOMOFFSET, FALSE, FALSE);
                 if (n_loc_filled > 3) 
diff --git a/src/mkmaze.c b/src/mkmaze.c
index 56d66ddac..569bd7a42 100644
--- a/src/mkmaze.c
+++ b/src/mkmaze.c
@@ -388,13 +388,12 @@ baalz_fixup(void)
      * 'lastx - 1' and 'lasty - 1' for ending don't-clean column and row)
      * and the interior is protected against that (in wall_cleanup()).
      *
-     * Assumes level.flags.corrmaze is True, otherwise the bug legs will
+     * Assumes get_flag(level.flags.bitflags, LEVEL_BITFLAGS_CORRMAZE) is True, otherwise the bug legs will
      * have already been "cleaned" away by general wallification.
      */
 
     /* find low and high x for to-be-wallified portion of level */
-    y = ROWNO / 2;
-    for (lastx = x = 0; x < COLNO; ++x)
+    y = ROWNO / 2;for (lastx = 0; x = 0; x < COLNO; ++x)
         if ((levl[x][y].wall_info & W_NONDIGGABLE) != 0) {
             if (!lastx)
                 bughack.inarea.x1 = x + 1;
@@ -402,8 +401,7 @@ baalz_fixup(void)
         }
     bughack.inarea.x2 = ((lastx > bughack.inarea.x1) ? lastx : x) - 1;
     /* find low and high y for to-be-wallified portion of level */
-    x = bughack.inarea.x1;
-    for (lasty = y = 0; y < ROWNO; ++y)
+    x = bughack.inarea.x1;for (lasty = 0; y = 0; y < ROWNO; ++y)
         if ((levl[x][y].wall_info & W_NONDIGGABLE) != 0) {
             if (!lasty)
                 bughack.inarea.y1 = y + 1;
@@ -453,11 +451,7 @@ baalz_fixup(void)
 
     /* reset bughack region; set low end to <COLNO,ROWNO> so that
        within_bounded_region() in fix_wall_spines() will fail
-       most quickly--on its first test--when loading other levels */
-    bughack.inarea.x1 = bughack.delarea.x1 = COLNO;
-    bughack.inarea.y1 = bughack.delarea.y1 = ROWNO;
-    bughack.inarea.x2 = bughack.delarea.x2 = 0;
-    bughack.inarea.y2 = bughack.delarea.y2 = 0;
+       most quickly--on its first test--when loading other levels */bughack.inarea.x1 = COLNO; bughack.delarea.x1 = COLNO;bughack.inarea.y1 = ROWNO; bughack.delarea.y1 = ROWNO;bughack.inarea.x2 = 0; bughack.delarea.x2 = 0;bughack.inarea.y2 = 0; bughack.delarea.y2 = 0;
 }
 
 static boolean was_waterlevel; /* ugh... this shouldn't be needed */
@@ -602,8 +596,7 @@ fixup_special(void)
     } else if (Is_knox(&u.uz)) {
         /* using an unfilled morgue for rm id */
         croom = search_special(MORGUE);
-        /* avoid inappropriate morgue-related messages */
-        level.flags.graveyard = level.flags.has_morgue = 0;
+        /* avoid inappropriate morgue-related messages */set_flag(level.flags.bitflags, LEVEL_BITFLAGS_GRAVEYARD, 0); set_flag(level.flags.bitflags, LEVEL_BITFLAGS_HAS_MORGUE, 0);
         croom->rtype = OROOM; /* perhaps it should be set to VAULT? */
         /* stock the main vault */
         for (x = croom->lx; x <= croom->hx; x++)
@@ -776,7 +769,7 @@ stolen_booty(void)
     mtmp = makemon(&mons[PM_ORC_CAPTAIN], 0, 0, MM_NONAME | MM_ANGRY);
     if (mtmp) {
         mtmp = christen_monst(mtmp, gang);
-        mtmp->u_know_mname = TRUE; /* Not realistic, but let's do it */
+        set_mon_u_know_mname(mtmp, TRUE); /* Not realistic, but let's do it */
         shiny_orc_stuff(mtmp);
         migrate_orc(mtmp, ORC_LEADER);
     }
@@ -798,7 +791,7 @@ stolen_booty(void)
             if (mtmp->data != &mons[PM_ORC_CAPTAIN] && !(has_enpc(mtmp) && has_mname(mtmp)))
             {
                 mtmp = christen_orc(mtmp, gang, "");
-                mtmp->u_know_mname = TRUE; /* Not realistic, but let's do it */
+                set_mon_u_know_mname(mtmp, TRUE); /* Not realistic, but let's do it */
             }
         }
     }
@@ -851,13 +844,10 @@ maze_remove_deadends(xchar typ)
     dirok[0] = 0; /* lint suppression */
     for (x = 2; x < x_maze_max; x++)
         for (y = 2; y < y_maze_max; y++)
-            if (ACCESSIBLE(levl[x][y].typ) && (x % 2) && (y % 2)) {
-                idx = idx2 = 0;
+            if (ACCESSIBLE(levl[x][y].typ) && (x % 2) && (y % 2)) {idx = 0; idx2 = 0;
                 for (dir = 0; dir < 4; dir++) {
                     /* note: mz_move() is a macro which modifies
-                       one of its first two parameters */
-                    dx = dx2 = x;
-                    dy = dy2 = y;
+                       one of its first two parameters */dx = x; dx2 = x;dy = y; dy2 = y;
                     mz_move(dx, dy, dir);
                     if (!maze_inbounds(dx, dy)) {
                         idx2++;
@@ -970,13 +960,11 @@ create_maze(int corrwid, int wallthick)
                 tmpmap[x][y] = levl[x][y].typ;
             }
 
-        /* do the scaling */
-        rx = x = 2;
+        /* do the scaling */rx = 2; x = 2;
         while (rx < x_maze_max) {
             int mx = (x % 2) ? corrwid
                              : ((x == 2 || x == (rdx * 2)) ? 1
-                                                           : wallthick);
-            ry = y = 2;
+                                                           : wallthick);ry = 2; y = 2;
             while (ry < y_maze_max) {
                 int dx = 0, dy = 0;
                 int my = (y % 2) ? corrwid
@@ -1152,9 +1140,7 @@ makemaz(const char *s)
         {
             debugpline2("inv_pos: maze is too small! (%d x %d)",
                         x_maze_max, y_maze_max);
-        }
-
-        inv_pos.x = inv_pos.y = 0; /*{occupied() => invocation_pos()}*/
+        }inv_pos.x = 0; inv_pos.y = 0; /*{occupied() => invocation_pos()}*/
         do 
         {
             x = rn1(x_range, x_maze_min + INVPOS_X_MARGIN + 1);
@@ -1367,9 +1353,7 @@ bound_digging(void)
     int xmin, xmax, ymin, ymax;
 
     if (Is_earthlevel(&u.uz))
-        return; /* everything diggable here */
-
-    found = nonwall = FALSE;
+        return; /* everything diggable here */found = FALSE; nonwall = FALSE;
     for (xmin = 0; !found && xmin <= COLNO - 1; xmin++) {
         lev = &levl[xmin][0];
         for (y = 0; y <= ROWNO - 1; y++, lev++) {
@@ -1558,8 +1536,7 @@ movebubbles(void)
                                     sizeof(struct container));
 
                             while ((otmp = level.objects[x][y]) != 0) {
-                                remove_object(otmp);
-                                otmp->ox = otmp->oy = 0;
+                                remove_object(otmp);otmp->ox = 0; otmp->oy = 0;
                                 otmp->nexthere = olist;
                                 olist = otmp;
                             }
@@ -1591,8 +1568,7 @@ movebubbles(void)
                             else
                                 remove_monster(x, y);
 
-                            newsym(x, y); /* clean up old position */
-                            mon->mx = mon->my = 0;
+                            newsym(x, y); /* clean up old position */mon->mx = 0; mon->my = 0;
                         }
                         if (!u.uswallow && x == u.ux && y == u.uy) {
                             struct container *cons =
@@ -1715,8 +1691,7 @@ save_waterlevel(int fd, int mode)
 
 void
 reset_waterlevel(void)
-{
-    xmin = ymin = xmax = ymax = 0;
+{xmin = 0; ymin = 0; xmax = 0; ymax = 0;
     unsetup_waterlevel();
 }
 
@@ -1848,8 +1823,7 @@ unsetup_waterlevel(void)
     for (b = bbubbles; b; b = bb) {
         bb = b->next;
         free((genericptr_t) b);
-    }
-    bbubbles = ebubbles = (struct bubble *) 0;
+    }bbubbles = (struct bubble *; ebubbles = (struct bubble *) 0;
 }
 
 static void
diff --git a/src/mkobj.c b/src/mkobj.c
index acbce6aab..d21223488 100644
--- a/src/mkobj.c
+++ b/src/mkobj.c
@@ -859,8 +859,8 @@ splitobj(struct obj *obj, int64_t num)
     otmp->oextra = (struct oextra *) 0;
     otmp->o_id = nextoid(obj, otmp);
     otmp->timed = 0;                  /* not timed, yet */
-    otmp->lamplit = 0;                /* ditto */
-    otmp->makingsound = 0;            /* ditto */
+    set_obj_lamplit(otmp, 0);                /* ditto */
+    set_obj_makingsound(otmp, 0);            /* ditto */
     otmp->owornmask = 0L;             /* new object isn't worn */
     obj->quan -= num;
     obj->owt = weight(obj);
@@ -987,8 +987,7 @@ unsplitobj(struct obj *obj)
 /* reset splitobj()/unsplitobj() context */
 void
 clear_splitobjs(void)
-{
-    context.objsplit.parent_oid = context.objsplit.child_oid = 0;
+{context.objsplit.parent_oid = 0; context.objsplit.child_oid = 0;
 }
 
 /*
@@ -1082,7 +1081,7 @@ unknwn_contnr_contents(struct obj *obj)
  * an object which is different from what it started out as; the "I x"
  * command needs to display the original object.
  *
- * The caller is responsible for checking otmp->unpaid and
+ * The caller is responsible for checking is_obj_unpaid(otmp) and
  * costly_spot(u.ux, u.uy).  This function will make otmp no charge.
  *
  * Note that check_unpaid_usage() should be used instead for partial
@@ -1340,9 +1338,7 @@ costly_alteration(struct obj *obj, int alter_type)
     {
         impossible("invalid alteration type (%d)", alter_type);
         alter_type = 0;
-    }
-
-    ox = oy = 0;    /* lint suppression */
+    }ox = 0; oy = 0;    /* lint suppression */
     objroom = '\0'; /* ditto */
     if (carried(obj) || obj->where == OBJ_FREE)
     {
@@ -3763,9 +3757,7 @@ place_object(struct obj *otmp, int x, int y)
         level.objects[x][y] = otmp;
     }
 
-    /* set the new object's location */
-    otmp->ox = otmp->ox0 = x;
-    otmp->oy = otmp->oy0 = y;
+    /* set the new object's location */otmp->ox = x; otmp->ox0 = x;otmp->oy = y; otmp->oy0 = y;
 
     otmp->where = OBJ_FLOOR;
 
@@ -4452,7 +4444,7 @@ obj_sanity_check(void)
 
     /*
      * TODO:
-     *  Should check whether the obj->bypass and/or obj->nomerge bits
+     *  Should check whether the get_flag(obj->obj_bitflags, OBJ_BITFLAGS_BYPASS) and/or is_obj_nomerge(obj) bits
      *  are set.  Those are both used for temporary purposes and should
      *  be clear between moves.
      */
@@ -4634,9 +4626,7 @@ static void
 insane_object(struct obj *obj, const char *fmt, const char *mesg, struct monst *mon)
 {
     const char *objnm, *monnm;
-    char altfmt[BUFSZ];
-
-    objnm = monnm = "null!";
+    char altfmt[BUFSZ];objnm = "null!"; monnm = "null!";
     if (obj) {
         iflags.override_ID++;
         objnm = doname(obj);
diff --git a/src/mkroom.c b/src/mkroom.c
index c014f3ea2..5109aee6b 100644
--- a/src/mkroom.c
+++ b/src/mkroom.c
@@ -1200,7 +1200,7 @@ mkundead(coord *mm, boolean revive_corpses, int mm_flags)
         }
     }
     makemon_animation_wait_until_end();
-    level.flags.graveyard = TRUE; /* reduced chance for undead corpse */
+    set_flag(level.flags.bitflags, LEVEL_BITFLAGS_GRAVEYARD, TRUE); /* reduced chance for undead corpse */
 }
 
 static struct permonst *
diff --git a/src/mon.c b/src/mon.c
index 27df5f9ef..7f12c4f39 100644
--- a/src/mon.c
+++ b/src/mon.c
@@ -1335,10 +1332,10 @@ update_monster_timeouts(void)
                     switch (i)
                     {
                     case STONED:
-                        (void)munstone(mtmp, mtmp->delayed_killer_by_you); /* check if the monster has found something that helps */
+                        (void)munstone(mtmp, get_flag(mtmp->mon_bitflags, MON_BITFLAGS_DELAYED_KILLER_BY_YOU)); /* check if the monster has found something that helps */
                         break;
                     case SLIMED:
-                        (void)munslime(mtmp, mtmp->delayed_killer_by_you); /* check if the monster has found something that helps */
+                        (void)munslime(mtmp, get_flag(mtmp->mon_bitflags, MON_BITFLAGS_DELAYED_KILLER_BY_YOU)); /* check if the monster has found something that helps */
                         break;
                     case STRANGLED:
                         if (canseemon(mtmp) && !is_breathless(mtmp))
@@ -2489,8 +2486,7 @@ mfndpos_xy(struct monst *mon, xchar x, xchar y, coord *poss, int64_t *info, int6
     wallwalk = ((flag & (ALLOW_WALL)) != 0L);
     thrudoor = ((flag & (BUSTDOOR)) != 0L);
     poisongas_ok = ((is_not_living(mdat) || is_vampshifter(mon)
-                     || has_innate_breathless(mdat)) || resists_poison(mon));
-    in_poisongas = ((gas_reg = visible_region_at(x,y)) != 0
+                     || has_innate_breathless(mdat)) || resists_poison(mon));in_poisongas = visible_region_at(x; ((gas_reg = visible_region_at(x,y)) != 0
                     && gas_reg->glyph == gas_glyph);
 
     if (flag & ALLOW_DIG)
@@ -2499,8 +2495,7 @@ mfndpos_xy(struct monst *mon, xchar x, xchar y, coord *poss, int64_t *info, int6
 
         /* need to be specific about what can currently be dug */
         if (!needspick(mdat)) 
-        {
-            rockok = treeok = TRUE;
+        {rockok = TRUE; treeok = TRUE;
         } 
         else if ((mw_tmp = MON_WEP(mon)) && mw_tmp->cursed
                    && mon->weapon_strategy == NO_WEAPON_WANTED) 
@@ -3987,7 +3982,7 @@ monstone(struct monst *mdef)
     if (!DEADMONSTER(mdef))
         return;
 
-    mdef->mtrapped = 0; /* (see m_detach) */
+    set_mon_trapped(mdef, 0); /* (see m_detach) */
 
     if ((int) mdef->data->msize > MZ_TINY
         || !rn2(2 + ((int) (mdef->data->geno & G_FREQ) > 2))) {
@@ -4124,8 +4119,7 @@ unstuck(struct monst *mtmp)
                 mtmp->mspec_used = rnd(2);
         }
         u.ustuck = 0;
-        update_all_character_properties((struct obj*)0, TRUE);
-        context.botl = context.botlx = TRUE;
+        update_all_character_properties((struct obj*)0, TRUE);context.botl = TRUE; context.botlx = TRUE;
         play_environment_ambient_sounds();
     }
 }
@@ -4348,7 +4342,7 @@ xkilled(struct monst *mtmp, int xkill_flags)
 cleanup:
     /* punish bad behaviour */
     if (is_human(mdat) && !is_not_living(mdat) && !is_were(mdat)
-        && !(mtmp->ispriest && has_epri(mtmp) && EPRI(mtmp)->shralign == A_NONE) /* Killing Moloch's (high) priests do not make you murderer */
+        && !(is_mon_priest(mtmp) && has_epri(mtmp) && EPRI(mtmp)->shralign == A_NONE) /* Killing Moloch's (high) priests do not make you murderer */
         && (!always_hostile(mdat) && mtmp->mhostility <= 0)
         && (mndx < PM_ARCHAEOLOGIST || mndx > PM_WIZARD)
         && u.ualign.type != A_CHAOTIC) 
@@ -4661,9 +4655,7 @@ mnearto(struct monst *mtmp, xchar x, xchar y, boolean move_other, boolean has_ef
         if (othermon->wormno)
             remove_worm(othermon);
         else
-            remove_monster(x, y);
-
-        othermon->mx = othermon->my = 0; /* 'othermon' is not on the map */
+            remove_monster(x, y);othermon->mx = 0; othermon->my = 0; /* 'othermon' is not on the map */
     }
 
     newx = x;
@@ -5002,7 +4994,7 @@ wake_nearto(int x, int y, int distance)
         if (distance == 0 || dist2(mtmp->mx, mtmp->my, x, y) < distance) {
             /* sleep for N turns uses mtmp->mfrozen, but so does paralysis
                so we leave mfrozen monsters alone */
-            mtmp->msleeping = 0; /* wake indeterminate sleep */
+            set_mon_sleeping(mtmp, 0); /* wake indeterminate sleep */
             refresh_m_tile_gui_info(mtmp, FALSE);
             cnt++;
             if (!(mtmp->data->geno & G_UNIQ))
@@ -5186,7 +5178,7 @@ hideunder(struct monst *mtmp)
     if (is_u)
         u.uundetected = undetected;
     else
-        mtmp->mundetected = undetected ? TRUE : FALSE; /* Since mundetected is not a boolean (either a bitfield / unsigned int or uchar) */
+        set_mon_undetected(mtmp, undetected ? TRUE : FALSE); /* Since mundetected is not a boolean (either a bitfield / unsigned int or uchar) */
 
     newsym(x, y);
 
@@ -6966,7 +6958,7 @@ revert_mon_polymorph(struct monst *mtmp, boolean override_mextra, boolean polysp
             if (M_AP_TYPE(mtmp) && !is_mimic(&mons[mtraits->mnum]))
                 seemimic(mtmp); /* revert to normal monster */
 
-            //Copy data one by one rather than using *mtmp = *mtraits. About half of the data needs to be copied and half not.
+            //Copy data one by one rather than using *mtmp = &mons[mtraits->mnum]; *mtraits. About half of the data needs to be copied and half not.
             mtmp->data = &mons[mtraits->mnum];
             mtmp->mnum = mtraits->mnum;
             mtmp->subtype = mtraits->subtype;
@@ -7068,8 +7060,8 @@ revert_mon_polymorph(struct monst *mtmp, boolean override_mextra, boolean polysp
             //mtmp->mextra = mextra;
 
             //mtmp->mw = mw;
-            //mtmp->mx0 = mtmp->mx = mx;
-            //mtmp->my0 = mtmp->my = my;
+            //mtmp->mx0 = mx; mtmp->mx = mx;
+            //mtmp->my0 = my; mtmp->my = my;
             //mtmp->movement = 0;
             //mtmp->meating = meating;
             //mtmp->mtrapseen = mtrapseen;
diff --git a/src/monmove.c b/src/monmove.c
index 026a6ba71..99d61c071 100644
--- a/src/monmove.c
+++ b/src/monmove.c
@@ -1682,8 +1682,7 @@ m_move(struct monst *mtmp, int after)
         else if (likegold)
         {
             /* don't try to pick up anything else, but use the same loop */
-            uses_items = 0;
-            likegems = likeobjs = likemagic = likerock = conceals = 0;
+            uses_items = 0;likegems = 0; likeobjs = 0; likemagic = 0; likerock = 0; conceals = 0;
             goto look_for_obj;
         }
 
@@ -1780,9 +1779,7 @@ m_move(struct monst *mtmp, int after)
                     if (nx == mtrk->x && ny == mtrk->y)
                         if (rn2(4 * (cnt - j)))
                             goto nxti;
-            }
-
-            nearer = ((ndist = dist2(nx, ny, gx, gy)) < nidist);
+            }nearer = dist2(nx; ((ndist = dist2(nx, ny, gx, gy)) < nidist);
 
             if ((appr == 1 && nearer) || (appr == -1 && !nearer)
                 || (!appr && !rn2(++chcnt)) || !mmoved) 
@@ -2162,8 +2159,8 @@ m_move(struct monst *mtmp, int after)
 
         struct obj* objhere = o_at(mtmp->mx, mtmp->my);
         if (OBJ_AT(mtmp->mx, mtmp->my) && mon_wants_to_pick_up_obj(mtmp, objhere))
-            //mon_can_move(mtmp) && !mtmp->issummoned && mon_can_reach_floor(mtmp) && !onnopickup(mtmp->mx, mtmp->my, mtmp)
-            //&& !(couldsee(mtmp->mx, mtmp->my) && objhere && objhere->was_thrown)) //Do not pick up ammo or other stuff that the player shoots / throws (this checks just the first item, but that's probably good enough)
+            //mon_can_move(mtmp) && !get_flag(mtmp->mon_bitflags, MON_BITFLAGS_ISSUMMONED) && mon_can_reach_floor(mtmp) && !onnopickup(mtmp->mx, mtmp->my, mtmp)
+            //&& !(couldsee(mtmp->mx, mtmp->my) && objhere && get_flag(objhere->obj_bitflags, OBJ_BITFLAGS_WAS_THROWN))) //Do not pick up ammo or other stuff that the player shoots / throws (this checks just the first item, but that's probably good enough)
         {
             /* recompute the likes tests, in case we polymorphed
              * or if the "likegold" case got taken above */
@@ -2519,7 +2516,7 @@ m_findtravelpath(struct monst *mon, int mode, xchar *mon_dx_ptr, xchar *mon_dy_p
                 *mon_dx_ptr = target_x - mon->mx;
                 *mon_dy_ptr = target_y - mon->my;
                 //nomul(0);
-                //iflags.travelcc.x = iflags.travelcc.y = 0;
+                //iflags.travelcc.x = 0; iflags.travelcc.y = 0;
             }
             return TRUE;
         }
@@ -2657,7 +2654,7 @@ m_findtravelpath(struct monst *mon, int mode, xchar *mon_dx_ptr, xchar *mon_dy_p
                                     //nomul(0);
                                     /* reset run so domove run checks work */
                                     //context.run = 8;
-                                    //iflags.travelcc.x = iflags.travelcc.y = 0;
+                                    //iflags.travelcc.x = 0; iflags.travelcc.y = 0;
                                 }
                                 return TRUE;
                             }
@@ -2748,8 +2745,7 @@ m_findtravelpath(struct monst *mon, int mode, xchar *mon_dx_ptr, xchar *mon_dy_p
             ty = py;
             ux = mon->mx;
             uy = mon->my;
-            set = 0;
-            n = radius = 1;
+            set = 0;n = 1; radius = 1;
             mode = TRAVP_TRAVEL;
             goto noguess;
         }
diff --git a/src/mthrowu.c b/src/mthrowu.c
index 2f99f6fb3..4877b4703 100644
--- a/src/mthrowu.c
+++ b/src/mthrowu.c
@@ -128,8 +128,7 @@ thitu(int tlev, int dam, struct obj **objp, const char *name, struct monst *orig
                 You("are hit by something%s", exclam(dam));
             else
                 You("are hit by %s%s", onm, exclam(dam));
-            potionhit(&youmonst, &obj, POTHIT_OTHER_THROW);
-            *objp = obj = 0; /* potionhit() uses up the potion */
+            potionhit(&youmonst, &obj, POTHIT_OTHER_THROW);*objp = 0; obj = 0; /* potionhit() uses up the potion */
         } 
         else 
         {
@@ -345,8 +344,7 @@ monshoot(struct monst *mtmp, struct obj *otmp, struct obj *mwep)
                since we gave one above about throwing/shooting N missiles) */
             break; /* endmultishot(FALSE); */
     }
-    /* reset 'm_shot' */
-    m_shot.n = m_shot.i = 0;
+    /* reset 'm_shot' */m_shot.n = 0; m_shot.i = 0;
     m_shot.o = STRANGE_OBJECT;
     m_shot.s = FALSE;
 
@@ -1169,8 +1167,7 @@ thrwmm(struct monst *mtmp, struct monst *mtarg)
             target = mtarg;
             archer = mtmp;
             update_m_facing(mtmp, mtarg->mx - mtmp->mx, TRUE);
-            monshoot(mtmp, otmp, mwep); /* multishot shooting or throwing */
-            archer = target = (struct monst *) 0;
+            monshoot(mtmp, otmp, mwep); /* multishot shooting or throwing */archer = (struct monst *; target = (struct monst *) 0;
             nomul(0);
             return 1;
         }
diff --git a/src/muse.c b/src/muse.c
index 42977a26e..3fd79960d 100644
--- a/src/muse.c
+++ b/src/muse.c
@@ -185,8 +185,7 @@ precheck(struct monst *mon, struct obj *obj)
         {
             monkilled(mon, "", AD_MAGM, 0);
             return 1;
-        }
-        m.has_defense = m.has_offense = m.has_misc = 0;
+        }m.has_defense = 0; m.has_offense = 0; m.has_misc = 0;
         /* Only one needed to be set to 0 but the others are harmless */
     }
     return 0;
@@ -591,8 +590,7 @@ find_defensive(struct monst *mtmp)
                                    || passes_walls(mtmp->data)),
             diag_ok = !NODIAG(mtmp->mnum);
 
-        for (i = 0; i < 10; ++i) /* 10: 9 spots plus sentinel */
-            locs[i][0] = locs[i][1] = 0;
+        for (i = 0; i < 10; ++i) /* 10: 9 spots plus sentinel */locs[i][0] = 0; locs[i][1] = 0;
         /* collect viable spots; monster's <mx,my> comes first */
         locs[0][0] = x, locs[0][1] = y;
         i = 1;
diff --git a/src/music.c b/src/music.c
index 11c478245..bb3a50a34 100644
--- a/src/music.c
+++ b/src/music.c
@@ -108,7 +108,7 @@ put_monsters_to_sleep(int otyp, int distance)
             continue;
         if (distu(mtmp->mx, mtmp->my) < distance
             && sleep_monst(mtmp, (struct obj*)0, (struct monst*)0, duration, 0, FALSE)) {
-            //mtmp->msleeping = 1; /* 10d10 turns + wake_nearby to rouse */
+            //set_mon_sleeping(mtmp, 1); /* 10d10 turns + wake_nearby to rouse */
             slept_monst(mtmp);
         }
     }
@@ -943,9 +942,7 @@ do_play_instrument(struct obj *instr)
                 if (ok)
                 { /* There is a drawbridge near */
                     int tumblers, gears;
-                    boolean matched[5];
-
-                    tumblers = gears = 0;
+                    boolean matched[5];tumblers = 0; gears = 0;
                     for (x = 0; x < 5; x++)
                         matched[x] = FALSE;
 
diff --git a/src/objnam.c b/src/objnam.c
index 986024938..926e6b0d7 100644
--- a/src/objnam.c
+++ b/src/objnam.c
@@ -2164,8 +2162,7 @@ weapon_here:
         char* attrs = nextobuf();
         char* colors = nextobuf();
         memset(attrs, ATR_NONE, OBUFSZ - 1);
-        memset(colors, NO_COLOR, OBUFSZ - 1);
-        attrs[OBUFSZ - 1] = colors[OBUFSZ - 1] = 0;
+        memset(colors, NO_COLOR, OBUFSZ - 1);attrs[OBUFSZ - 1] = 0; colors[OBUFSZ - 1] = 0;
         char compbuf[BUFSZ] = "";
         print_comparison_stats(obj, compbuf, WIN_ERR, ATR_NONE, NO_COLOR, TRUE, FALSE, bp, attrs, colors);
         if (*compbuf)
@@ -2258,7 +2255,7 @@ doname_with_price_and_weight_first_and_comparison(struct obj *obj, boolean loads
     return doname_with_flags(obj, DONAME_WITH_PRICE | DONAME_WITH_WEIGHT_FIRST | (loadstonecorrectly ? DONAME_LOADSTONE_CORRECTLY : 0) | (comparison_stats ? DONAME_COMPARISON : 0), attrs_ptr, colors_ptr);
 }
 
-/* "some" instead of precise quantity if obj->dknown not set */
+/* "some" instead of precise quantity if is_obj_dknown(obj) not set */
 char *
 doname_vague_quan(struct obj *obj)
 {
@@ -2269,10 +2266,10 @@ doname_vague_quan(struct obj *obj)
      * because lookhere and pickup report the precise amount.
      * Picking the item up while blind also shows the precise amount
      * for inventory display, then dropping it while still blind leaves
-     * obj->dknown unset so the count reverts to "some" for farlook.
+     * is_obj_dknown(obj) unset so the count reverts to "some" for farlook.
      *
      * TODO: add obj->qknown flag for 'quantity known' on stackable
-     * items; it could overlay obj->cknown since no containers stack.
+     * items; it could overlay is_obj_cknown(obj) since no containers stack.
      */
     return doname_with_flags(obj, DONAME_VAGUE_QUAN, (char **)0, (char **)0);
 }
@@ -2419,9 +2416,8 @@ corpse_xname(struct obj *otmp, const char *adjective, unsigned cxn_flags)
             else if (the_unique_pm(&mons[omndx]) && !no_prefix)
                 the_prefix = TRUE;
         }
-    }
-    if (no_prefix)
-        the_prefix = any_prefix = FALSE;
+    }if (no_prefix)
+        the_prefix = FALSE; any_prefix = FALSE;
     else if (the_prefix)
         any_prefix = FALSE; /* mutually exclusive */
 
@@ -4220,8 +4206,7 @@ readobjnam(char *bp, struct obj *no_wish, boolean is_wiz_wish, boolean *removed_
 #define CONTAINER_EMPTY 1
 #define CONTAINER_SPINACH 2
     contents = CONTAINER_UNDEFINED;
-    oclass = 0;
-    actualn = dn = un = 0;
+    oclass = 0;actualn = 0; dn = 0; un = 0;
     wetness = 0;
 
     if (!bp)
@@ -4466,8 +4450,7 @@ readobjnam(char *bp, struct obj *no_wish, boolean is_wiz_wish, boolean *removed_
                 while (digit(*p))
                     p++;
             }
-            if (*p != ')') {
-                charges = rechrg = 0;
+            if (*p != ')') {charges = 0; rechrg = 0;
                 /* mis-matched parentheses; rest of string will be ignored
                  * [probably we should restore everything back to '('
                  * instead since it might be part of "named ..."]
@@ -4888,8 +4871,7 @@ readobjnam(char *bp, struct obj *no_wish, boolean is_wiz_wish, boolean *removed_
                 p -= j;
                 *p = 0;
                 if (p > bp && p[-1] == ' ')
-                    p[-1] = 0;
-                actualn = dn = bp;
+                    p[-1] = 0;actualn = bp; dn = bp;
                 goto srch;
             }
         }
@@ -4945,8 +4927,7 @@ retry:
 
     if (!BSTRCMPI(bp, p - 6, " stone") || !BSTRCMPI(bp, p - 4, " gem")) {
         p[!strcmpi(p - 4, " gem") ? -4 : -6] = '\0';
-        oclass = GEM_CLASS;
-        dn = actualn = bp;
+        oclass = GEM_CLASS;dn = bp; actualn = bp;
         goto srch;
     } else if (!strcmpi(bp, "looking glass")) {
         ; /* avoid false hit on "* glass" */
@@ -5871,7 +5850,7 @@ retry:
         else if(name == aname)
         {
             otmp = oname(otmp, name);
-            /* name==aname => wished for artifact (otmp->oartifact => got it) */
+            /* name==aname = > got it; > wished for artifact (otmp->oartifact = > got it) */
             if (otmp->oartifact) {
                 otmp->quan = 1L;
                 u.uconduct.wisharti++; /* KMH, conduct */
diff --git a/src/pager.c b/src/pager.c
index db6e89e75..1eb649d15 100644
--- a/src/pager.c
+++ b/src/pager.c
@@ -543,9 +543,7 @@ lookat(int x, int y, char *buf, char *simplebuf, char *extrabuf)
     struct monst* mtmp = (struct monst*)0;
     struct permonst* pm = (struct permonst*)0;
     int glyph;
-    boolean noarticle = FALSE;
-
-    buf[0] = simplebuf[0] = extrabuf[0] = '\0';
+    boolean noarticle = FALSE;buf[0] = '\0'; simplebuf[0] = '\0'; extrabuf[0] = '\0';
     if (!isok(x, y))
         return (struct permonst*)0;
 
@@ -981,8 +979,7 @@ checkfile(char *inp, struct permonst *pm, boolean user_typed_name, boolean witho
             alt = makesingular(dbase_str);
 
         pass1found_in_file = FALSE;
-        for (pass = !strcmp(alt, dbase_str) ? 0 : 1; pass >= 0; --pass) {
-            found_in_file = skipping_entry = FALSE;
+        for (pass = !strcmp(alt, dbase_str) ? 0 : 1; pass >= 0; --pass) {found_in_file = FALSE; skipping_entry = FALSE;
             txt_offset = 0L;
             if (dlb_fseek(fp, txt_offset, SEEK_SET) < 0 ) {
                 impossible("can't get to start of 'data' file");
diff --git a/src/pickup.c b/src/pickup.c
index 8094f3e75..6ac681f83 100644
--- a/src/pickup.c
+++ b/src/pickup.c
@@ -160,10 +160,7 @@ query_classes(char oclasses[], boolean *one_at_a_time, boolean *everything, cons
     char qbuf[QBUFSZ];
     char ebuf[QBUFSZ];
     boolean m_seen;
-    int itemcount, bcnt, ucnt, ccnt, xcnt, ocnt, tcnt;
-
-    oclasses[oclassct = 0] = '\0';
-    *one_at_a_time = *everything = m_seen = FALSE;
+    int itemcount, bcnt, ucnt, ccnt, xcnt, ocnt, tcnt;oclasses[oclassct = '\0'; 0] = '\0';*one_at_a_time = FALSE; *everything = FALSE; m_seen = FALSE;
     if (menu_on_demand)
         *menu_on_demand = 0;
     iletct = collect_obj_classes(ilets, objs, here, (boolean FDECL((*), (struct obj *))) 0, &itemcount);
@@ -210,10 +207,7 @@ query_classes(char oclasses[], boolean *one_at_a_time, boolean *everything, cons
         const char *where = 0;
         char sym, oc_of_sym, *p;
 
-    ask_again:
-        oclasses[oclassct = 0] = '\0';
-        *one_at_a_time = *everything = FALSE;
-        not_everything = filtered = FALSE;
+    ask_again:oclasses[oclassct = '\0'; 0] = '\0';*one_at_a_time = FALSE; *everything = FALSE;not_everything = FALSE; filtered = FALSE;
         if (applymode && index(ilets, 'a'))
         {
             Strcpy(inbuf, "a");
@@ -689,8 +682,7 @@ pickup(int what, boolean do_auto_in_bag)
 
     menu_pickup:
         n_tried = n;
-        debugprint_pos();
-        for (n_picked = i = 0; i < n; i++)
+        debugprint_pos();for (n_picked = 0; i = 0; i < n; i++)
         {
             uchar obj_gone = FALSE;
             res = pickup_object(pick_list[i].item.a_obj, pick_list[i].count, FALSE, do_auto_in_bag || (autopickup && flags.stash_on_autopickup && (!flags.pickup_thrown || !pick_list[i].item.a_obj->was_thrown)), &obj_gone);
@@ -1027,8 +1019,7 @@ autopick(struct obj *olist, int follow, menu_item **pick_list)
         check_costly = FALSE; /* only need to check once per autopickup */
     }
 
-    if (n) {
-        *pick_list = pi = (menu_item *) alloc(sizeof (menu_item) * (size_t)n);
+    if (n) {*pick_list = (menu_item *; pi = (menu_item *) alloc(sizeof (menu_item) * (size_t)n);
         for (n = 0, curr = olist; curr; curr = FOLLOW(curr, follow)) {
             if (autopick_testobj(curr, FALSE)) {
                 pi[n].item.a_obj = curr;
@@ -1265,8 +1256,7 @@ query_objlist(const char *qstr, struct obj **olist_p, int qflags, menu_item **pi
         int k;
 
         /* fix up counts:  -1 means no count used => pick all;
-           if fake_hero_object was picked, discard that choice */
-        for (i = k = 0, mi = *pick_list; i < n; i++, mi++) {
+           if fake_hero_object was picked, discard that choice */for (i = 0; k = 0, mi = *pick_list; i < n; i++, mi++) {
             if (mi->item.a_obj == &fake_hero_object)
                 continue;
             if (mi->count == -1L || mi->count > mi->item.a_obj->quan)
@@ -1589,9 +1579,7 @@ delta_cwt(struct obj *container, struct obj *obj)
     int owt, nwt;
 
     if (!Is_weight_changing_bag(container))
-        return obj->owt;
-
-    owt = nwt = container->owt;
+        return obj->owt;owt = container->owt; nwt = container->owt;
     /* find the object so that we can remove it */
     for (prev = contained_object_chain_ptr(container); *prev; prev = &(*prev)->nobj)
         if (*prev == obj)
@@ -1685,8 +1673,7 @@ carry_count(struct obj *obj, struct obj *container, int64_t count, boolean telek
          * don't merge.  -dean
          */
         for (qq = 1L; qq <= count; qq++) {
-            obj->quan = qq;
-            obj->owt = (unsigned) (ow = weight(obj));
+            obj->quan = qq;obj->owt = weight(obj; (unsigned) (ow = weight(obj));
             if (adjust_wt)
                 ow -= delta_cwt(container, obj);
             if (iw + ow >= 0)
@@ -1712,8 +1699,7 @@ carry_count(struct obj *obj, struct obj *container, int64_t count, boolean telek
             verb = telekinesis ? "acquire" : "lift";
         }
     } else {
-        /* lint suppression */
-        *obj_nambuf = *where = '\0';
+        /* lint suppression */*obj_nambuf = '\0'; *where = '\0';
         verb = "";
     }
     /* we can carry qq of them */
@@ -2238,8 +2224,7 @@ encumber_msg(void)
             You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "%s move a handspan with this load!",
                 newcap == 4 ? "can barely" : "can't even");
             break;
-        }
-        context.botl = context.botlx = TRUE;
+        }context.botl = TRUE; context.botlx = TRUE;
         standard_hint("Being burdened reduces your speed and thus your combat power. Try to remain unburdened at all times, if possible. Some magical bags reduce weight of items. Put items into such bags if you have them.", &u.uhint.got_burdened);
     } else if (oldcap > newcap) {
         switch (newcap) {
@@ -2256,8 +2241,7 @@ encumber_msg(void)
             You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "%s under your load.  Movement is still very hard.",
                 stagger(youmonst.data, "stagger"));
             break;
-        }
-        context.botl = context.botlx = TRUE;
+        }context.botl = TRUE; context.botlx = TRUE;
     }
 
     u.carrying_capacity_level = newcap;
@@ -3562,7 +3546,7 @@ in_container_core(struct obj *obj, boolean dobot)
             delay_output_milliseconds(ITEM_PICKUP_DROP_DELAY);
         }
 
-        obj->bypass = 0; /* It will now leave the inventory */
+        set_obj_bypass(obj, 0); /* It will now leave the inventory */
 
         /* gold in container always needs to be added to credit */
         if (floor_container && obj->oclass == COIN_CLASS)
@@ -3930,7 +3914,7 @@ observe_quantum_cat(struct obj *box, boolean makecat, boolean givemsg)
                           Monnam(livecat));
             }
             (void) christen_monst(livecat, sc);
-            livecat->u_know_mname = TRUE; /* It is famous! */
+            set_mon_u_know_mname(livecat, TRUE); /* It is famous! */
             if (deadcat)
             {
                 debugprint("observe_quantum_cat: %d", deadcat->otyp);
@@ -4217,7 +4201,7 @@ use_container(struct obj **objp, int held, boolean more_containers, int applymod
             explain_container_prompt(more_containers);
         } 
         else if (c == ':') 
-        { /* note: will set obj->cknown */
+        { /* note: will set is_obj_cknown(obj) */
             if (!current_container->cknown)
                 used = 1; /* gaining info */
             display_cinventory(current_container);
@@ -4276,8 +4260,7 @@ use_container(struct obj **objp, int held, boolean more_containers, int applymod
         && (!invent || (invent == current_container && !invent->nobj))) 
     {
         You("don't have anything%s to %s.", invent ? " else" : "",
-            stash_one ? "stash" : "put in");
-        loot_in = stash_one = FALSE;
+            stash_one ? "stash" : "put in");loot_in = FALSE; stash_one = FALSE;
     }
     else if (pickup_and_loot_in 
         && (!level.objects[u.ux][u.uy] || (level.objects[u.ux][u.uy] == current_container && !level.objects[u.ux][u.uy]->nexthere)))
@@ -5251,10 +5234,8 @@ tipcontainer(struct obj *box)
                 altarizing = IS_ALTAR(levl[ox][oy].typ),
                 cursed_mbag = (Is_mbag(box) && box->cursed);
         int held = carried(box);
-        int64_t loss = 0L;
-
-        if (u.uswallow)
-            highdrop = altarizing = FALSE;
+        int64_t loss = 0L;if (u.uswallow)
+            highdrop = FALSE; altarizing = FALSE;
         terse = !(highdrop || altarizing || costly_spot(box->ox, box->oy));
         box->cknown = 1;
         /* Terse formatting is
diff --git a/src/polyself.c b/src/polyself.c
index 8480bab55..d98941f77 100644
--- a/src/polyself.c
+++ b/src/polyself.c
@@ -139,8 +139,7 @@ float_vs_flight(void)
     if ((HLevitation || ELevitation) && stuck_in_floor)
         HBlocks_Levitation |= I_SPECIAL;
     else
-        HBlocks_Levitation &= ~I_SPECIAL;
-    context.botl = context.botlx = TRUE;
+        HBlocks_Levitation &= ~I_SPECIAL;context.botl = TRUE; context.botlx = TRUE;
 }
 
 /* for changing into form that's immune to strangulation */
@@ -153,8 +152,7 @@ check_strangling(boolean on)
            vulnerable form into another causes the counter to be reset */
         if (uamul && uamul->otyp == AMULET_OF_STRANGULATION
             && can_be_strangled(&youmonst)) {
-            Strangled = 6L;
-            context.botl = context.botlx = TRUE;
+            Strangled = 6L;context.botl = TRUE; context.botlx = TRUE;
             Your("%s %s your %s!", simpleonames(uamul),
                  Strangled ? "still constricts" : "begins constricting",
                  body_part(NECK)); /* "throat" */
@@ -164,8 +162,7 @@ check_strangling(boolean on)
     /* off -- maybe block strangling */
     } else {
         if (Strangled && !can_be_strangled(&youmonst)) {
-            Strangled = 0L;
-            context.botl = context.botlx = TRUE;
+            Strangled = 0L;context.botl = TRUE; context.botlx = TRUE;
             You("are no longer being strangled.");
         }
     }
@@ -186,9 +183,7 @@ polyman(const char *fmt, const char *arg)
         u.umonnum = u.umonster;
         flags.female = u.mfemale;
     }
-    set_uasmon();
-
-    u.mh = u.mhmax = u.basemhmax = 0;
+    set_uasmon();u.mh = 0; u.mhmax = 0; u.basemhmax = 0;
     u.mtimedone = 0;
     skinback(FALSE);
     u.uundetected = 0;
@@ -409,9 +404,7 @@ newman(void)
     if (Slimed) {
         Your_ex(ATR_NONE, CLR_MSG_WARNING, "body transforms, but there is still slime on you.");
         make_slimed(10L, (const char *) 0, KILLED_BY, "sliming", HINT_KILLED_SLIMED);
-    }
-
-    context.botl = context.botlx = TRUE;
+    }context.botl = TRUE; context.botlx = TRUE;
     see_monsters();
     (void) encumber_msg();
 
@@ -817,15 +810,9 @@ polymon(int mntmp)
      * Currently only strength gets changed.
      */
     //if (strongmonst(&mons[mntmp]))
-    //    ABASE(A_STR) = AMAX(A_STR) = STR18(100);
-
-
-    ABASE(A_STR) = AMAX(A_STR) = AMIN(A_STR) = (&mons[mntmp])->str; // STR18(100);
-    ABASE(A_DEX) = AMAX(A_DEX) = AMIN(A_DEX) = (&mons[mntmp])->dex;
-    ABASE(A_CON) = AMAX(A_CON) = AMIN(A_CON) = (&mons[mntmp])->con;
-    ABASE(A_INT) = AMAX(A_INT) = AMIN(A_INT) = (&mons[mntmp])->intl;
-//    ABASE(A_WIS) = AMAX(A_WIS) = AMIN(A_WIS) = (&mons[mntmp])->wis; // Wisdom does not change 
-    ABASE(A_CHA) = AMAX(A_CHA) = AMIN(A_CHA) = (&mons[mntmp])->cha;
+    //ABASE(A_STR) = STR18(100; AMAX(A_STR) = STR18(100);ABASE(A_STR) = (&mons[mntmp]; AMAX(A_STR) = (&mons[mntmp]; AMIN(A_STR) = (&mons[mntmp])->str; // STR18(100);ABASE(A_DEX) = (&mons[mntmp]; AMAX(A_DEX) = (&mons[mntmp]; AMIN(A_DEX) = (&mons[mntmp])->dex;ABASE(A_CON) = (&mons[mntmp]; AMAX(A_CON) = (&mons[mntmp]; AMIN(A_CON) = (&mons[mntmp])->con;ABASE(A_INT) = (&mons[mntmp]; AMAX(A_INT) = (&mons[mntmp]; AMIN(A_INT) = (&mons[mntmp])->intl;
+//ABASE(A_WIS) = (&mons[mntmp]; AMAX(A_WIS) = (&mons[mntmp]; AMIN(A_WIS) = (&mons[mntmp])->wis; //Wisdom does not change 
+    ABASE(A_CHA) = (&mons[mntmp]; AMAX(A_CHA) = (&mons[mntmp]; AMIN(A_CHA) = (&mons[mntmp])->cha;
 
     /* These are incorrect in the sense that they give monster race's maximum, not the current undrained maximum
     AMAX(A_STR) = monster_attribute_maximum(&mons[mntmp], A_STR);
@@ -1027,9 +1014,7 @@ polymon(int mntmp)
         You_ex(ATR_NONE, CLR_MSG_SUCCESS, "orient yourself on the web.");
         reset_utrap(TRUE);
     }
-    check_strangling(TRUE); /* maybe start strangling */
-
-    context.botl = context.botlx = TRUE;
+    check_strangling(TRUE); /* maybe start strangling */context.botl = TRUE; context.botlx = TRUE;
     vision_full_recalc = 1;
     see_monsters();
     (void) encumber_msg();
@@ -1678,9 +1663,7 @@ rehumanize(void)
         killer.format = KILLED_BY;
         done(DIED);
     }
-    nomul(0);
-
-    context.botl = context.botlx = TRUE;
+    nomul(0);context.botl = TRUE; context.botlx = TRUE;
     vision_full_recalc = 1;
     (void) encumber_msg();
     if (was_flying && !Flying && u.usteed)
diff --git a/src/potion.c b/src/potion.c
index 0c7738974..2269c8364 100644
--- a/src/potion.c
+++ b/src/potion.c
@@ -67,8 +67,7 @@ make_confused(int64_t xtime, boolean talk)
     set_itimeout(&HConfusion, xtime);
 
     if ((xtime && !old) || (!xtime && old))
-    {
-        context.botl = context.botlx = TRUE;
+    {context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
     }
 
@@ -105,8 +104,7 @@ make_stunned(int64_t xtime, boolean talk)
     set_itimeout(&HStun, xtime);
 
     if ((!xtime && old) || (xtime && !old))
-    {
-        context.botl = context.botlx = TRUE;
+    {context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
     }
 }
@@ -147,8 +145,7 @@ make_sick(int64_t xtime, const char *cause, boolean talk, int killerhintidx)
             if (talk)
                 You_feel_ex(ATR_NONE, CLR_MSG_NEGATIVE, "%s worse.", xtime <= Sick / 2L ? "much" : "even");
         }
-        set_itimeout(&Sick, xtime);
-        context.botl = context.botlx = TRUE;
+        set_itimeout(&Sick, xtime);context.botl = TRUE; context.botlx = TRUE;
         if (!old)
             refresh_u_tile_gui_info(TRUE);
     }
@@ -162,8 +159,7 @@ make_sick(int64_t xtime, const char *cause, boolean talk, int killerhintidx)
             else
                 You_feel_ex(ATR_NONE, CLR_MSG_POSITIVE, "cured.  What a relief!");
         }
-        Sick = 0L; /* set_itimeout(&Sick, 0L) */
-        context.botl = context.botlx = TRUE;
+        Sick = 0L; /* set_itimeout(&Sick, 0L) */context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
     }
 
@@ -222,8 +218,7 @@ make_food_poisoned(int64_t xtime, const char *cause, boolean talk, int killerhin
             if (talk)
                 You_feel_ex(ATR_NONE, CLR_MSG_NEGATIVE, "%s worse.", xtime <= FoodPoisoned / 2L ? "much" : "even");
         }
-        set_itimeout(&FoodPoisoned, xtime);
-        context.botl = context.botlx = TRUE;
+        set_itimeout(&FoodPoisoned, xtime);context.botl = TRUE; context.botlx = TRUE;
         if(!old)
             refresh_u_tile_gui_info(TRUE);
     }
@@ -237,8 +232,7 @@ make_food_poisoned(int64_t xtime, const char *cause, boolean talk, int killerhin
             else
                 You_feel_ex(ATR_NONE, CLR_MSG_POSITIVE, "cured.  What a relief!");
         }
-        FoodPoisoned = 0L;
-        context.botl = context.botlx = TRUE;
+        FoodPoisoned = 0L;context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
     }
 
@@ -301,9 +295,7 @@ make_mummy_rotted(int64_t xtime, const char *cause, boolean talk, int killerhint
         if (xtime > 0L)
             set_itimeout(&MummyRot, xtime);
         else
-            MummyRot |= FROM_ACQUIRED;
-
-        context.botl = context.botlx = TRUE;
+            MummyRot |= FROM_ACQUIRED;context.botl = TRUE; context.botlx = TRUE;
         if (!old)
             refresh_u_tile_gui_info(TRUE);
     }
@@ -318,8 +310,7 @@ make_mummy_rotted(int64_t xtime, const char *cause, boolean talk, int killerhint
                 You_feel_ex(ATR_NONE, CLR_MSG_POSITIVE, "cured.  What a relief!");
         }
         MummyRot = 0L;
-        context.mummyrot_advancement = 0;
-        context.botl = context.botlx = TRUE;
+        context.mummyrot_advancement = 0;context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
     }
 
@@ -354,8 +345,7 @@ make_slimed(int64_t xtime, const char *msg, int killedby, const char *killername
 #endif
     set_itimeout(&Slimed, xtime);
     if ((xtime != 0L) ^ (old != 0L)) 
-    {
-        context.botl = context.botlx = TRUE;
+    {context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         if (msg)
             pline_ex(ATR_NONE, !xtime ? CLR_MSG_POSITIVE : CLR_MSG_NEGATIVE, "%s", msg);
@@ -387,8 +377,7 @@ make_stoned(int64_t xtime, const char *msg, int killedby, const char *killername
 #endif
     set_itimeout(&Stoned, xtime);
     if ((xtime != 0L) ^ (old != 0L)) 
-    {
-        context.botl = context.botlx = TRUE;
+    {context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         if (msg)
             pline_ex(ATR_NONE, !xtime ? CLR_MSG_POSITIVE : CLR_MSG_NEGATIVE, "%s", msg);
@@ -416,8 +405,7 @@ make_vomiting(int64_t xtime, boolean talk)
         talk = FALSE;
 
     set_itimeout(&Vomiting, xtime);
-    refresh_u_tile_gui_info(TRUE);
-    context.botl = context.botlx = TRUE;
+    refresh_u_tile_gui_info(TRUE);context.botl = TRUE; context.botlx = TRUE;
     if (!xtime && old)
         if (talk)
             You_feel_ex(ATR_NONE, !xtime ? CLR_MSG_POSITIVE : CLR_MSG_NEGATIVE, "much less nauseated now.");
@@ -507,8 +495,7 @@ void
 toggle_blindness(void)
 {
     /* Note this is a kludge that works only with wielded weapons; */
-    /* blindness has just been toggled */
-    context.botl = context.botlx = TRUE;
+    /* blindness has just been toggled */context.botl = TRUE; context.botlx = TRUE;
     /* status conditions need update */
     vision_full_recalc = 1; /* vision has changed */
     /* this vision recalculation used to be deferred until moveloop(),
@@ -632,9 +619,7 @@ make_hallucinated(int64_t xtime, boolean talk, int64_t mask)
 
         /* for perm_inv and anything similar
         (eg. Qt windowport's equipped items display) */
-        update_inventory();
-
-        context.botl = context.botlx = TRUE;
+        update_inventory();context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         if (talk)
             pline_ex(ATR_NONE, !xtime ? CLR_MSG_POSITIVE : CLR_MSG_HALLUCINATED, message, verb);
@@ -654,8 +639,7 @@ make_deaf(int64_t xtime, boolean talk)
     set_itimeout(&HDeaf, xtime);
 
     if ((xtime != 0L) ^ (old != 0L)) 
-    {
-        context.botl = context.botlx = TRUE;
+    {context.botl = TRUE; context.botlx = TRUE;
         update_hearing_array_and_ambient_sounds();
         play_environment_ambient_sounds();
         refresh_u_tile_gui_info(TRUE);
@@ -807,7 +791,7 @@ dodrink(void)
     }
     if (!gone)
     {
-        otmp->in_use = TRUE; /* you've opened the stopper */
+        set_obj_in_use(otmp, TRUE); /* you've opened the stopper */
 
         if (u_item_use_flags() & ACTION_ITEM_USE_FLAGS_POTION)
         {
@@ -1320,8 +1300,7 @@ peffects(struct obj *otmp)
             else
                 Your_ex(ATR_NONE, CLR_MSG_POSITIVE, "%s are frozen to the %s!", makeplural(body_part(FOOT)),
                      surface(u.ux, u.uy));
-            incr_itimeout(&HParalyzed, duration);
-            context.botl = context.botlx = 1;
+            incr_itimeout(&HParalyzed, duration);context.botl = 1; context.botlx = 1;
             refresh_u_tile_gui_info(TRUE);
             standard_hint("Identify potions before drinking.", &u.uhint.drank_potion_of_paralysis_or_sleep);
         }
@@ -2100,9 +2079,7 @@ healup(int nhp, int nxtra, boolean curesick, boolean cureblind, boolean curehall
     if (cureconfusion)
     {
         make_confused(0L, TRUE);
-    }
-
-    context.botl = context.botlx = TRUE;
+    }context.botl = TRUE; context.botlx = TRUE;
     newsym_with_flags(u.ux, u.uy, NEWSYM_FLAGS_KEEP_OLD_EFFECT_MISSILE_ZAP_GLYPHS_AND_FLAGS);
     flush_screen(1);
     return;
@@ -2777,9 +2754,9 @@ potionbreathe(struct obj *obj, const char *introline)
         if (!isdiluted || !rn2(2))
         {
             if (Upolyd && u.mh < u.mhmax)
-                u.mh++, context.botl = context.botlx = 1;
+                u.mh++,context.botl = 1; context.botlx = 1;
             if (u.uhp < u.uhpmax)
-                u.uhp++, context.botl = context.botlx = 1;
+                u.uhp++,context.botl = 1; context.botlx = 1;
         }
         cureblind = TRUE;
         /*FALLTHRU*/
@@ -2893,8 +2870,7 @@ potionbreathe(struct obj *obj, const char *introline)
             Sprintf(dcbuf, "%s seems to be holding you.", Something);
             pline_ex1(ATR_NONE, CLR_MSG_WARNING, dcbuf);
             incr_itimeout(&HParalyzed, duration);
-            refresh_u_tile_gui_info(TRUE);
-            context.botl = context.botlx = 1;
+            refresh_u_tile_gui_info(TRUE);context.botl = 1; context.botlx = 1;
             standard_hint("You should acquire paralysis resistance as early as possible. Keep pets around to protect you while paralyzed.", &u.uhint.paralyzed_by_thrown_potion);
 #if 0
             nomul(-d(3 - 1 * bcsign(obj), 4)); // rnd(5));
@@ -3870,7 +3846,7 @@ dodip(void)
             else
             {
                 pline("Nothing seems to happen.");
-                obj->in_use = TRUE; /* goes poof instead */
+                set_obj_in_use(obj, TRUE); /* goes poof instead */
                 if (!objects[obj->otyp].oc_name_known && !objects[obj->otyp].oc_uname)
                     docall(obj, (char*)0);
                 debugprint("dodip7b: %d", obj->otyp);
@@ -4174,7 +4148,7 @@ dodip(void)
         else if ((!is_rustprone(obj) && !is_corrodeable(obj))
                    || is_ammo(obj) || (!obj->oeroded && !obj->oeroded2))
         {
-            /* uses up potion, doesn't set obj->greased */
+            /* uses up potion, doesn't set is_obj_greased(obj) */
             pline_ex(ATR_NONE, CLR_MSG_SUCCESS, "%s %s with an oily sheen.", Yname2(obj),
                   otense(obj, "gleam"));
         } 
@@ -4497,8 +4471,7 @@ split_mon(struct monst *mon, struct monst *mtmp)
                 mtmp2->mbasehpdrain = u.basemhdrain / 2;
                 u.basemhdrain -= mtmp2->mbasehpdrain;
                 updatemaxhp();
-                update_mon_maxhp(mtmp2);
-                context.botl = context.botlx = 1;
+                update_mon_maxhp(mtmp2);context.botl = 1; context.botlx = 1;
                 You("multiply%s!", reason);
             }
         }
@@ -4524,8 +4497,7 @@ split_mon(struct monst *mon, struct monst *mtmp)
 
 void
 reset_potion(void)
-{
-    nothing = unkn = 0;
+{nothing = 0; unkn = 0;
 }
 
 /*potion.c*/
diff --git a/src/pray.c b/src/pray.c
index 84f13ca4e..64d68dbc5 100644
--- a/src/pray.c
+++ b/src/pray.c
@@ -414,8 +414,7 @@ fix_worst_trouble(int trouble)
             You_ex1(ATR_NONE, CLR_MSG_POSITIVE, "are no longer being strangled.");
         else
             You_ex1(ATR_NONE, CLR_MSG_POSITIVE, "can breathe again.");
-        Strangled = 0;
-        context.botl = context.botlx = 1;
+        Strangled = 0;context.botl = 1; context.botlx = 1;
         break;
     case TROUBLE_LAVA:
         You_ex(ATR_NONE, CLR_MSG_POSITIVE, "are back on solid ground.");
@@ -428,8 +427,7 @@ fix_worst_trouble(int trouble)
         /*FALLTHRU*/
     case TROUBLE_HUNGRY:
         Your_ex(ATR_NONE, CLR_MSG_POSITIVE, "%s feels content.", body_part(STOMACH));
-        init_uhunger();
-        context.botl = context.botlx = 1;
+        init_uhunger();context.botl = 1; context.botlx = 1;
         break;
     case TROUBLE_SICK:
         play_sfx_sound(SFX_CURE_DISEASE);
@@ -455,8 +453,7 @@ fix_worst_trouble(int trouble)
            5 or less hit points, so make sure they're always
            boosted to be more than that */
         play_sfx_sound(SFX_FULL_HEALING);
-        You_feel_ex(ATR_NONE, CLR_MSG_POSITIVE, "much better.");
-        u.ubasehpdrain = u.basemhdrain = 0;
+        You_feel_ex(ATR_NONE, CLR_MSG_POSITIVE, "much better.");u.ubasehpdrain = 0; u.basemhdrain = 0;
         if (Upolyd) {
             u.basemhmax += rnd(5);
             if (u.basemhmax <= 5)
@@ -928,9 +925,7 @@ gcrownu(void)
 
     play_sfx_sound(SFX_PRAY_CROWNING);
 
-    godvoice(u.ualign.type, (char *) 0);
-
-    in_hand = in_hand2 = FALSE; /* lint suppression */
+    godvoice(u.ualign.type, (char *) 0);in_hand = FALSE; in_hand2 = FALSE; /* lint suppression */
     boolean vorpal_blade_already_exists = exist_artifact(LONG_SWORD, artiname(ART_VORPAL_BLADE));
     boolean excalibur_already_exists = exist_artifact(LONG_SWORD, artiname(ART_EXCALIBUR));
     boolean katana_already_exists = exist_artifact(KATANA, artiname(ART_KATANA_OF_MASAMUNE));
@@ -2204,8 +2179,7 @@ pleased(aligntyp g_align)
             /* superfluous; if hero was blinded we'd be handling trouble
                rather than issuing a pat-on-head */
             u.ucreamed = 0;
-            make_blinded(0L, TRUE);
-            context.botl = context.botlx = 1;
+            make_blinded(0L, TRUE);context.botl = 1; context.botlx = 1;
             dorefresh = TRUE;
             break;
         case 4: 
diff --git a/src/priest.c b/src/priest.c
index 9cd6d496a..01bd50154 100644
--- a/src/priest.c
+++ b/src/priest.c
@@ -799,7 +799,7 @@ priestname(struct monst *mon, char *pname)
     char whatcode = '\0';
     const char *what = do_hallu ? rndmonnam(&whatcode) : mon_monster_name(mon);
 
-    if (!mon->ispriest && !mon->isminion) /* should never happen...  */
+    if (!is_mon_priest(mon) && !is_mon_minion(mon)) /* should never happen...  */
         return strcpy(pname, what);       /* caller must be confused */
 
     *pname = '\0';
@@ -1126,10 +1122,7 @@ forget_smithy_entry(struct monst *smith)
     {
         impossible("attempting to manipulate smithy data for non-smith?");
         return;
-    }
-
-    esmi_p->intone_time = esmi_p->enter_time = esmi_p->peaceful_time =
-        esmi_p->hostile_time = 0L;
+    }esmi_p->intone_time = 0L; esmi_p->enter_time = 0L; esmi_p->peaceful_time = 0L; esmi_p->hostile_time = 0L;
 }
 
 /* called from check_special_room() when the player enters the smithy */
@@ -1159,8 +1152,7 @@ insmithy(int roomno)
                 canseemon(smith) ? Monnam(smith) : "A nearby voice");
             esmi_p->intone_time = moves + (int64_t)d(10, 500); /* ~2505 */
             esmi_p->enter_time = 0L;
-        }
-        msg1 = msg2 = 0;
+        }msg1 = 0; msg2 = 0;
 
         if (moves >= esmi_p->enter_time) 
         {
@@ -1385,11 +1376,11 @@ mk_roamer(struct permonst *ptr, aligntyp alignment, xchar x, xchar y, boolean pe
 
     //EMIN(roamer)->min_align = alignment;
     //EMIN(roamer)->renegade = (coaligned && !peaceful);
-    //roamer->ispriest = 0;
-    //roamer->isminion = 1;
+    //set_mon_priest(roamer, 0);
+    //set_mon_minion(roamer, 1);
     //roamer->mtrapseen = ~0; /* traps are known */
-    //roamer->mpeaceful = peaceful;
-    //roamer->msleeping = 0;
+    //set_mon_peaceful(roamer, peaceful);
+    //set_mon_sleeping(roamer, 0);
     //set_mhostility(roamer); /* peaceful may have changed */
 
     /* MORE TO COME */
@@ -1452,10 +1442,7 @@ ghod_hitsu(struct monst *priest)
     struct mkroom *troom;
 
     if (!roomno || !has_shrine(priest))
-        return;
-
-    ax = x = EPRI(priest)->shrpos.x;
-    ay = y = EPRI(priest)->shrpos.y;
+        return;ax = EPRI(priest; x = EPRI(priest)->shrpos.x;ay = EPRI(priest; y = EPRI(priest)->shrpos.y;
     troom = &rooms[roomno - ROOMOFFSET];
 
     if ((u.ux == x && u.uy == y) || !linedup(u.ux, u.uy, x, y, 1, FALSE, FALSE, FALSE, 0, FALSE, M_SHOOT_RANGE))
diff --git a/src/quest.c b/src/quest.c
index bdbb27e68..a35990f00 100644
--- a/src/quest.c
+++ b/src/quest.c
@@ -554,9 +554,8 @@ void
 achievement_gained(const char *achievement_name)
 {
     play_sfx_sound(SFX_ACHIEVEMENT);
-    custompline_ex_prefix(ATR_NONE, CLR_MSG_HINT, "ACHIEVEMENT", ATR_NONE, NO_COLOR, " - ", ATR_BOLD, CLR_MSG_HIGHLIGHT, 0U, "%s", achievement_name);
-    if(flags.showscore)
-        context.botl = context.botlx = 1; /* Make sure score get updated when visible on the status bar */
+    custompline_ex_prefix(ATR_NONE, CLR_MSG_HINT, "ACHIEVEMENT", ATR_NONE, NO_COLOR, " - ", ATR_BOLD, CLR_MSG_HIGHLIGHT, 0U, "%s", achievement_name);if(flags.showscore)
+        context.botl = 1; context.botlx = 1; /* Make sure score get updated when visible on the status bar */
 }
 
 /*quest.c*/
diff --git a/src/read.c b/src/read.c
index 692b59039..7bdb255fe 100644
--- a/src/read.c
+++ b/src/read.c
@@ -706,8 +706,8 @@ recharge(struct obj *obj, int curse_bless, boolean verbose, const char *title, b
 
         /*
          * Recharging might cause wands to explode.
-         *      v = number of previous recharges
-         *            v = percentage chance to explode on this attempt
+         *      v = cumulative odds for exploding; number of previous recharges
+         *            v = cumulative odds for exploding; percentage chance to explode on this attempt
          *                    v = cumulative odds for exploding
          *      0 :   0       0
          *      1 :   0.29    0.29
@@ -1986,7 +1985,7 @@ seffects(struct obj *sobj, boolean *effect_happened_ptr, struct monst *targetmon
         //    else
         //    {
         //        play_sfx_sound(SFX_ENCHANT_ITEM_VIBRATE_AND_DESTROY);
-        //        otmp->in_use = TRUE;
+        //        set_obj_in_use(otmp, TRUE);
         //        pline_multi_ex_popup(ATR_NONE, CLR_MSG_NEGATIVE, no_multiattrs, multicolor_buffer, "Evaporation", is_serviced_spell,
         //            "%s violently %s%s%s for a while, then %s.", Yname2(otmp),
         //            otense(otmp, Blind ? "vibrate" : "glow"),
@@ -2039,11 +2038,11 @@ seffects(struct obj *sobj, boolean *effect_happened_ptr, struct monst *targetmon
         //   ever changed to care about curse/bless status of armor] */
         //if (s < 0)
         //    costly_alteration(otmp, COST_DECHNT);
-        //if (scursed && !otmp->cursed)
+        //if (scursed && !is_obj_cursed(otmp))
         //    curse(otmp);
-        //else if (sblessed && !otmp->blessed)
+        //else if (sblessed && !is_obj_blessed(otmp))
         //    bless(otmp);
-        //else if (!scursed && otmp->cursed)
+        //else if (!scursed && is_obj_cursed(otmp))
         //    uncurse(otmp);
         //
         //if (s) 
@@ -2053,9 +2052,9 @@ seffects(struct obj *sobj, boolean *effect_happened_ptr, struct monst *targetmon
         //    updateabon();
         //    updatemaxen();
         //    updatemaxhp();
-        //    known = otmp->known;
+        //    known = is_obj_known(otmp);
         //    /* update shop bill to reflect new higher price */
-        //    if (s > 0 && otmp->unpaid)
+        //    if (s > 0 && is_obj_unpaid(otmp))
         //        alter_cost(otmp, 0L);
         //}
 
@@ -2471,8 +2468,7 @@ seffects(struct obj *sobj, boolean *effect_happened_ptr, struct monst *targetmon
         int candidates, res, results, vis_results;
 
         if (u.uswallow) {
-            candidates = 1;
-            results = vis_results = maybe_tame(u.ustuck, sobj, otyp != SCR_TAMING ? &youmonst : (struct monst*)0);
+            candidates = 1;results = maybe_tame(u.ustuck; vis_results = maybe_tame(u.ustuck, sobj, otyp != SCR_TAMING ? &youmonst : (struct monst*)0);
         }
         else {
             int i, j, bd = confused ? 5 : 1;
@@ -2529,8 +2525,7 @@ seffects(struct obj *sobj, boolean *effect_happened_ptr, struct monst *targetmon
             }
 
             /* note: maybe_tame() can return either positive or
-               negative values, but not both for the same scroll */
-            candidates = results = vis_results = 0;
+               negative values, but not both for the same scroll */candidates = 0; results = 0; vis_results = 0;
             for (i = -bd; i <= bd; i++)
                 for (j = -bd; j <= bd; j++)
                 {
@@ -2569,8 +2564,7 @@ seffects(struct obj *sobj, boolean *effect_happened_ptr, struct monst *targetmon
         int i, j, bd, res = 0;
         struct monst* mtmp;
 
-        bd = (int)objects[otyp].oc_spell_radius;
-        candidates = results = vis_results = 0;
+        bd = (int)objects[otyp].oc_spell_radius;candidates = 0; results = 0; vis_results = 0;
 
         for (i = -bd; i <= bd; i++)
             for (j = -bd; j <= bd; j++)
@@ -2626,8 +2620,7 @@ seffects(struct obj *sobj, boolean *effect_happened_ptr, struct monst *targetmon
         int i, j, bd, res = 0;
         struct monst* mtmp;
 
-        bd = (int)objects[otyp].oc_spell_radius;
-        candidates = results = vis_results = 0;
+        bd = (int)objects[otyp].oc_spell_radius;candidates = 0; results = 0; vis_results = 0;
 
         for (i = -bd; i <= bd; i++)
         {
@@ -3152,8 +3143,7 @@ seffects(struct obj *sobj, boolean *effect_happened_ptr, struct monst *targetmon
         int maxslots = u.max_weapon_slots;
         lose_weapon_skill(u.ulevel - 1);
         u.max_weapon_slots = maxslots;
-        u.weapon_slots = u.max_weapon_slots;
-        context.botl = context.botlx = 1;
+        u.weapon_slots = u.max_weapon_slots;context.botl = 1; context.botlx = 1;
         update_can_advance_any_skill();
 
         if (Hallucination) /* Ommmmmm! */
@@ -3651,7 +3641,7 @@ wand_explode(struct obj *obj, int chg)
     }
     /* inflict damage and destroy the wand */
     dmg = d(n, k);
-    obj->in_use = TRUE; /* in case losehp() is fatal (or --More--^C) */
+    set_obj_in_use(obj, TRUE); /* in case losehp() is fatal (or --More--^C) */
     if (obj->oartifact)
     {
         play_sfx_sound(SFX_VANISHES_IN_PUFF_OF_SMOKE);
@@ -3876,8 +3866,7 @@ do_class_genocide(void)
 
         class = name_to_monclass(buf, (int *) 0);
         if (class == 0 && (i = name_to_mon(buf)) != NON_PM)
-            class = mons[i].mlet;
-        immunecnt = gonecnt = goodcnt = 0;
+            class = mons[i].mlet;immunecnt = 0; gonecnt = 0; goodcnt = 0;
 
         for (i = LOW_PM; i < NUM_MONSTERS; i++) 
         {
@@ -4367,9 +4356,7 @@ create_particular_parse(char *str, struct _create_particular_data *d)
     d->monclass = MAX_MONSTER_CLASSES;
     d->which = urole.monsternum; /* an arbitrary index into mons[] */
     d->fem = -1; /* gender not specified */
-    d->randmonst = FALSE;
-    d->maketame = d->makepeaceful = d->makehostile = FALSE;
-    d->sleeping = d->saddled = d->invisible = FALSE;
+    d->randmonst = FALSE;d->maketame = FALSE; d->makepeaceful = FALSE; d->makehostile = FALSE;d->sleeping = FALSE; d->saddled = FALSE; d->invisible = FALSE;
 
     if ((tmpp = strstri(bufp, "saddled ")) != 0) {
         d->saddled = TRUE;
diff --git a/src/restore.c b/src/restore.c
index 6ceb97948..0b1b992f1 100644
--- a/src/restore.c
+++ b/src/restore.c
@@ -184,7 +184,7 @@ inven_inuse(boolean quietly)
                     ))
                 )
             {
-                otmp->in_use = 0; /* Likely memory corruption; prevent destruction of any critical items */
+                set_obj_in_use(otmp, 0); /* Likely memory corruption; prevent destruction of any critical items */
                 char dbuf[BUFSZ * 2];
                 Sprintf(dbuf, "A mysterious force prevents finishing off %s...", the(xname(otmp)));
                 if (!quietly)
@@ -281,8 +281,7 @@ restobj(int fd, struct obj *otmp)
     mread(fd, (genericptr_t) otmp, sizeof(struct obj));
 
     /* next object pointers are invalid; otmp->cobj needs to be left
-       as is--being non-null is key to restoring container contents */
-    otmp->nobj = otmp->nexthere = (struct obj *) 0;
+       as is--being non-null is key to restoring container contents */otmp->nobj = (struct obj *; otmp->nexthere = (struct obj *) 0;
     /* non-null oextra needs to be reconstructed */
     if (otmp->oextra) {
         otmp->oextra = newoextra();
@@ -375,7 +374,7 @@ restobjchn(int fd, boolean ghostly, boolean frozen)
 
             otmp->cobj = restobjchn(fd, ghostly, Is_IceBox(otmp));
             /* restore container back pointers */
-            for (otmp3 = otmp->cobj; otmp3; otmp3 = otmp3->nobj)
+            for (otmp3 = otmp->cobj; otmp3;otmp3 = otmp; otmp3->nobj)
                 otmp3->ocontainer = otmp;
         } 
         else if (SchroedingersBox(otmp)) 
@@ -552,7 +551,7 @@ restmonchn(int fd, boolean ghostly)
             struct obj *obj;
             mtmp->minvent = restobjchn(fd, ghostly, FALSE);
             /* restore monster back pointer */
-            for (obj = mtmp->minvent; obj; obj = obj->nobj)
+            for (obj = mtmp->minvent; obj;obj = mtmp; obj->nobj)
                 obj->ocarry = mtmp;
         }
         if (mtmp->mw) {
@@ -706,7 +705,7 @@ restgamestate(int fd, unsigned int *stuckid, unsigned int *steedid)
     iflags.deferred_X = (newgameflags.explore && !discover);
     if (newgameflags.debug) {
         /* authorized by startup code; wizard mode exists and is allowed */
-        wizard = TRUE, discover = iflags.deferred_X = FALSE;
+        wizard = TRUE,discover = FALSE; iflags.deferred_X = FALSE;
     } else if (wizard) {
         /* specified by save file; check authorization now */
         set_playmode();
@@ -737,8 +736,7 @@ restgamestate(int fd, unsigned int *stuckid, unsigned int *steedid)
 #ifdef CLIPPING
     cliparound(u.ux, u.uy, TRUE);
 #endif
-    if (u.uhp <= 0 && (!Upolyd || u.mh <= 0)) {
-        u.ux = u.uy = 0; /* affects pline() [hence You()] */
+    if (u.uhp <= 0 && (!Upolyd || u.mh <= 0)) {u.ux = 0; u.uy = 0; /* affects pline() [hence You()] */
         You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "were not healthy enough to survive restoration.");
         /* wiz1_level.dlevel is used by mklev.c to see if lots of stuff is
          * uninitialized, so we only have to set it and not the other stuff.
@@ -1890,8 +1886,7 @@ restore_menu(winid bannerwin)
     {
         *plname = '\0';
         saved = get_saved_games(); /* array of character names */
-        repeat = TRUE;
-        ch = ret = 0;
+        repeat = TRUE;ch = 0; ret = 0;
 
         if (firsttime && (!saved || !saved[0].playername))
             return 2; /* start new game straight; there's no menu to return to */
diff --git a/src/rumors.c b/src/rumors.c
index 743c6569b..e3f6650bf 100644
--- a/src/rumors.c
+++ b/src/rumors.c
@@ -122,8 +122,8 @@ getrumor(int truth, char *rumor_buf, boolean exclude_cookie)
             }
             /*
              *  input:      1    0   -1
-             *   rn2 \ +1  2=T  1=T  0=F
-             *   adj./ +0  1=T  0=F -1=F
+             *   rn2 \+1  2 = F; T  1 = F; T  0 = F
+             *   adj./+0  1 = F; T  0 = F; F -1 = F
              */
             switch (adjtruth = truth + rn2(2)) {
             case 2: /*(might let a bogus input arg sneak thru)*/
diff --git a/src/save.c b/src/save.c
index 37c8f20d4..be834a53d 100644
--- a/src/save.c
+++ b/src/save.c
@@ -303,8 +303,7 @@ dosave0(boolean quietly)
      * in-core data structures, since all we're doing is copying.
      * This also avoids at least one nasty core dump.
      */
-    uz_save = u.uz;
-    u.uz.dnum = u.uz.dlevel = 0;
+    uz_save = u.uz;u.uz.dnum = 0; u.uz.dlevel = 0;
     /* these pointers are no longer valid, and at least u.usteed
      * may mislead place_monster() on other levels
      */
@@ -1226,8 +1225,8 @@ saveobjchn(int fd, struct obj *otmp, int mode)
             otmp->nobj = NULL;      /* nobj saved into otmp2 */
             otmp->cobj = NULL;      /* contents handled above */
             otmp->timed = 0;        /* not timed any more */
-            otmp->lamplit = 0;      /* caller handled lights */
-            otmp->makingsound = 0;  /* caller handled sounds */
+            set_obj_lamplit(otmp, 0);      /* caller handled lights */
+            set_obj_makingsound(otmp, 0);  /* caller handled sounds */
             dealloc_obj(otmp);
         }
         otmp = otmp2;
diff --git a/src/shk.c b/src/shk.c
index 308be5d69..273325847 100644
--- a/src/shk.c
+++ b/src/shk.c
@@ -1006,7 +1005,7 @@ obfree(struct obj *obj, struct obj *merge)
     if ((bp = onbill(obj, shkp, FALSE)) != 0) {
         if (!merge) {
             bp->useup = 1;
-            obj->unpaid = 0; /* only for doinvbill */
+            set_obj_unpaid(obj, 0); /* only for doinvbill */
             add_to_billobjs(obj);
             return;
         }
@@ -1335,8 +1334,7 @@ dopay(void)
 
     /* Find how many shk's there are, how many are in
      * sight, and are you in a shop room with one.
-     */
-    nxtm = resident = 0;
+     */nxtm = 0; resident = 0;
     for (shkp = next_shkp(fmon, FALSE); shkp;
          shkp = next_shkp(shkp->nmon, FALSE)) {
         sk++;
@@ -1725,7 +1723,7 @@ dopayobj(struct monst *shkp, struct bill_x *bp, struct obj **obj_p, int which, b
         quan = obj->quan;
     }
     //obj->quan = quan;        /* to be used by doname() */
-    //obj->unpaid = 0;         /* ditto */
+    //set_obj_unpaid(obj, 0);         /* ditto */
     iflags.payobj_special_quan = quan;
     iflags.suppress_price++; /* affects containers */
     ltmp = bp->price * quan;
@@ -1776,7 +1774,7 @@ dopayobj(struct monst *shkp, struct bill_x *bp, struct obj **obj_p, int which, b
     if (buy != PAY_BUY) {
         /* restore unpaid object to original state */
         //obj->quan = save_quan;
-        //obj->unpaid = 1;
+        //set_obj_unpaid(obj, 1);
         iflags.payobj_special_quan = 0;
         iflags.suppress_price--;
         return buy;
@@ -1788,7 +1786,7 @@ dopayobj(struct monst *shkp, struct bill_x *bp, struct obj **obj_p, int which, b
                   consumed ? "paid for %s at a cost of %ld gold piece%s.%s"
                            : "bought %s for %ld gold piece%s.%s",
                   ltmp, "");
-    obj->unpaid = 0; /* It is now finally unpaid in reality! */
+    set_obj_unpaid(obj, 0); /* It is now finally unpaid in reality! */
     iflags.payobj_special_quan = 0;
     //obj->quan = save_quan; /* restore original count */
     /* quan => amount just bought, save_quan => remaining unpaid count */
@@ -1841,8 +1839,7 @@ paybill(int croaked, boolean silently)
         petrified since shk shouldn't be able to grab inventory
         which has been shut inside a statue] */
 
-    /* this is where inventory will end up if any shk takes it */
-    repo.location.x = repo.location.y = 0;
+    /* this is where inventory will end up if any shk takes it */repo.location.x = 0; repo.location.y = 0;
     repo.shopkeeper = 0;
 
     /*
@@ -1854,8 +1851,7 @@ paybill(int croaked, boolean silently)
      * 6) first shk on monster list (last resort; unlikely, since
      * any nonlocal shk will probably be in the owed category
      * and almost certainly be in the angry category).
-     */
-    resident = creditor = hostile = localshk = (struct monst *) 0;
+     */resident = (struct monst *; creditor = (struct monst *; hostile = (struct monst *; localshk = (struct monst *) 0;
     for (mtmp = next_shkp(fmon, FALSE); mtmp;
          mtmp = next_shkp(mtmp2, FALSE)) {
         mtmp2 = mtmp->nmon;
@@ -2106,7 +2102,7 @@ find_oid(unsigned id)
     mmtmp[1] = migrating_mons;
     mmtmp[2] = mydogs; /* for use during level changes */
     for (i = 0; i < 3; i++)
-        for (mon = mmtmp[i]; mon; mon = mon->nmon)
+        for (mon = mmtmp[i]; mon;mon = o_on(id; mon->nmon)
             if ((obj = o_on(id, mon->minvent)) != 0)
                 return obj;
 
@@ -2867,7 +2861,7 @@ addtobill(struct obj *obj, boolean ininv, boolean dummy, boolean silent)
             add_one_tobill(obj, dummy, shkp);
         if (cltmp)
             bill_box_content(obj, ininv, dummy, shkp);
-        picked_container(obj); /* reset contained obj->no_charge */
+        picked_container(obj); /* reset contained is_obj_no_charge(obj) */
 
         ltmp += cltmp;
 
@@ -3615,8 +3607,8 @@ sellobj(struct obj *obj, xchar x, xchar y)
             /* Moved to above to allow saving during question */
             //if (container)
             //    dropped_container(obj, shkp, FALSE);
-            //if (!obj->unpaid)
-            //    obj->no_charge = 1;
+            //if (!is_obj_unpaid(obj))
+            //    set_obj_no_charge(obj, 1);
             //subfrombill(obj, shkp);
             break;
         case 'a':
@@ -4415,9 +4407,8 @@ shk_move(struct monst *shkp)
             if (((!eshkp->robbed && !eshkp->billct && !eshkp->debit) || avoid)
                 && GDIST(omx, omy) < 3) {
                 if (!badinv && !onlineu(omx, omy))
-                    return 0;
-                if (satdoor)
-                    appr = gx = gy = 0;
+                    return 0;if (satdoor)
+                    appr = 0; gx = 0; gy = 0;
             }
         }
     }
@@ -4578,9 +4569,7 @@ makekops(coord *mm)
 {
     static const short k_mndx[4] = { PM_KEYSTONE_KOP, PM_KOP_SERGEANT,
                                      PM_KOP_LIEUTENANT, PM_KOP_KAPTAIN };
-    int k_cnt[4], cnt, mndx, k;
-
-    k_cnt[0] = cnt = abs(depth(&u.uz)) + rnd(5);
+    int k_cnt[4], cnt, mndx, k;k_cnt[0] = abs(depth(&u.uz; cnt = abs(depth(&u.uz)) + rnd(5);
     k_cnt[1] = (cnt / 3) + 1; /* at least one sarge */
     k_cnt[2] = (cnt / 6);     /* maybe a lieutenant */
     k_cnt[3] = (cnt / 9);     /* and maybe a kaptain */
diff --git a/src/shknam.c b/src/shknam.c
index 89d72f704..89466b3e0 100644
--- a/src/shknam.c
+++ b/src/shknam.c
@@ -537,9 +537,7 @@ shkveg(void)
 {
     int i, j, maxprob, prob;
     char oclass = FOOD_CLASS;
-    int ok[NUM_OBJECTS];
-
-    j = maxprob = 0;
+    int ok[NUM_OBJECTS];j = 0; maxprob = 0;
     ok[0] = 0; /* lint suppression */
     for (i = bases[(int) oclass]; i < NUM_OBJECTS; ++i) {
         if (objects[i].oc_class != oclass)
@@ -870,10 +867,7 @@ shkinit(const struct shclass *shp, struct mkroom *sroom)
     assign_level(&eshkp->shoplevel, &u.uz);
     eshkp->shd = doors[sh];
     eshkp->shk.x = sx;
-    eshkp->shk.y = sy;
-    eshkp->robbed = eshkp->credit = eshkp->debit = eshkp->loan = 0L;
-    eshkp->following = eshkp->surcharge = eshkp->dismiss_kops = FALSE;
-    eshkp->billct = eshkp->visitct = 0;
+    eshkp->shk.y = sy;eshkp->robbed = 0L; eshkp->credit = 0L; eshkp->debit = 0L; eshkp->loan = 0L;eshkp->following = FALSE; eshkp->surcharge = FALSE; eshkp->dismiss_kops = FALSE;eshkp->billct = 0; eshkp->visitct = 0;
     eshkp->bill_p = (struct bill_x *) 0;
     eshkp->customer[0] = '\0';
     int64_t shk_money = 
diff --git a/src/sit.c b/src/sit.c
index f70d19009..e9310e6aa 100644
--- a/src/sit.c
+++ b/src/sit.c
@@ -298,8 +298,7 @@ dosit(void)
                 //make_sick(0L, (char *) 0, FALSE);
                 //make_food_poisoned(0L, (char*)0, FALSE);
                 //make_mummy_rotted(0L, (char*)0, FALSE);
-                heal_legs(0);
-                context.botl = context.botlx = 1;
+                heal_legs(0);context.botl = 1; context.botlx = 1;
                 break;
             case 5:
                 take_gold();
diff --git a/src/sounds.c b/src/sounds.c
index 04df578a2..7ac8c2767 100644
--- a/src/sounds.c
+++ b/src/sounds.c
@@ -779,7 +779,7 @@ dosounds(void)
                 "convulsive ravings.", /* Apollo at Delphi */
                 "snoring snakes.",     /* AEsculapius at Epidaurus */
                 "someone say \"No more woodchucks!\"",
-                "a loud ZOT!" /* both rec.humor.oracle */
+                "a loud ZOT!" /* both get_flag(rec.humor.flags.bitflags, MAPSEEN_BITFLAGS_ORACLE) */
             };
             static enum sfx_sound_types ora_sound[5] = {
                 SFX_LEVEL_STRANGE_WIND, SFX_LEVEL_CONVULSIVE_RAVINGS, SFX_LEVEL_SNORING_SNAKES,
@@ -3245,7 +3245,7 @@ dochatmon(struct monst *mtmp)
             chatnum++;
         }
 
-        if (is_tame(mtmp) && invent && is_peaceful(mtmp)) /*  && !mtmp->issummoned */
+        if (is_tame(mtmp) && invent && is_peaceful(mtmp)) /*  && !get_flag(mtmp->mon_bitflags, MON_BITFLAGS_ISSUMMONED) */
         {
             Sprintf(available_chat_list[chatnum].name, "Give items to %s", noittame_mon_nam(mtmp));
             available_chat_list[chatnum].function_ptr = &do_chat_pet_giveitems;
@@ -3302,7 +3302,7 @@ dochatmon(struct monst *mtmp)
             }
         }
 
-        if (is_tame(mtmp) && mtmp->minvent && is_peaceful(mtmp)) /*  && !mtmp->issummoned */
+        if (is_tame(mtmp) && mtmp->minvent && is_peaceful(mtmp)) /*  && !get_flag(mtmp->mon_bitflags, MON_BITFLAGS_ISSUMMONED) */
         {
             Sprintf(available_chat_list[chatnum].name, "Uncurse or bless %s items", s_suffix(noittame_mon_nam(mtmp)));
             available_chat_list[chatnum].function_ptr = &do_chat_uncurse_items;
@@ -3357,7 +3357,7 @@ dochatmon(struct monst *mtmp)
             }
         }
 
-        if (is_tame(mtmp) && mtmp->minvent && can_operate_objects(mtmp->data) && attacktype(mtmp->data, AT_WEAP) && is_peaceful(mtmp)) /*  && !mtmp->issummoned */
+        if (is_tame(mtmp) && mtmp->minvent && can_operate_objects(mtmp->data) && attacktype(mtmp->data, AT_WEAP) && is_peaceful(mtmp)) /*  && !get_flag(mtmp->mon_bitflags, MON_BITFLAGS_ISSUMMONED) */
         {
             if (select_hwep(mtmp, TRUE, 0, 0))
             {
@@ -4480,8 +4480,7 @@ dochatmon(struct monst *mtmp)
                         available_chat_category_titles[available_chat_list[j].category], MENU_UNSELECTED, menu_heading_info());
                 }
 
-                any = zeroany;
-                any.a_char = available_chat_list[j].charnum = 'a' + j;
+                any = zeroany;any.a_char = 'a' + j; available_chat_list[j].charnum = 'a' + j;
                 if (available_chat_list[j].using_menu_color || available_chat_list[j].using_special_symbols)
                 {
                     int mcolor = available_chat_list[j].using_menu_color ? available_chat_list[j].color : NO_COLOR;
diff --git a/src/soundset.c b/src/soundset.c
index 68ac7930d..ab71ebec9 100644
--- a/src/soundset.c
+++ b/src/soundset.c
@@ -18366,8 +18366,7 @@ update_hearing_array(int mode)
         return;
 
     int hear_distance = get_max_hearing_distance();
-    /* Fill the array */
-    hearing_array[u.ux][u.uy] = prev_hearing_array[u.ux][u.uy] = 1.0f;
+    /* Fill the array */hearing_array[u.ux][u.uy] = 1.0f; prev_hearing_array[u.ux][u.uy] = 1.0f;
 
     int r, i, y, prev_y, x, prev_x;
     for (r = 1; r <= hear_distance; r++)
@@ -18953,8 +18952,7 @@ sound_stats(const char *hdrfmt, char *hdrbuf, int64_t *count, size_t *size)
 {
     sound_source* ss;
 
-    Sprintf(hdrbuf, hdrfmt, sizeof(sound_source));
-    *count = *size = 0L;
+    Sprintf(hdrbuf, hdrfmt, sizeof(sound_source));*count = 0L; *size = 0L;
     for (ss = sound_base; ss; ss = ss->next) {
         ++* count;
         *size += sizeof * ss;
@@ -19286,7 +19284,7 @@ obj_split_sound_source(struct obj *src, struct obj *dest)
             new_ss->id.a_obj = dest;
             new_ss->next = sound_base;
             sound_base = new_ss;
-            dest->makingsound = 1; /* now an active sound source */
+            set_obj_makingsound(dest, 1); /* now an active sound source */
         }
 }
 
diff --git a/src/sp_lev.c b/src/sp_lev.c
index f9cc7fda6..06efa4c5a 100644
--- a/src/sp_lev.c
+++ b/src/sp_lev.c
@@ -313,8 +313,7 @@ splev_stack_done(struct splevstack *st)
             }
         }
         Free(st->stackdata);
-        st->stackdata = NULL;
-        st->depth = st->depth_alloc = 0;
+        st->stackdata = NULL;st->depth = 0; st->depth_alloc = 0;
         Free(st);
     }
 }
@@ -734,9 +733,7 @@ shuffle_alignments(void)
 static void
 count_features(void)
 {
-    xchar x, y;
-
-    level.flags.nfountains = level.flags.nsinks = 0;
+    xchar x, y;level.flags.nfountains = 0; level.flags.nsinks = 0;
     for (y = 0; y < ROWNO; y++)
         for (x = 0; x < COLNO; x++) {
             int typ = levl[x][y].typ;
@@ -841,7 +838,7 @@ link_doors_rooms(void)
             if (IS_DOOR(levl[x][y].typ) || levl[x][y].typ == SDOOR) {
                 /* in case this door was a '+' or 'S' from the
                    MAP...ENDMAP section without an explicit DOOR
-                   directive, set/clear levl[][].horizontal for it */
+                   directive, set/clear is_lev_horizontal(x, y) for it */
                 set_door_orientation(x, y);
 
                 for (tmpi = 0; tmpi < nroom; tmpi++) {
@@ -974,8 +971,7 @@ get_location(schar *x, schar *y, int humidity, struct mkroom *croom)
                 }
             if (!(humidity & NO_LOC_WARN)) {
                 impossible("get_location:  can't find a place!");
-            } else {
-                *x = *y = -1;
+            } else {*x = -1; *y = -1;
             }
         }
     }
@@ -987,8 +983,7 @@ found_it:
             /*warning("get_location:  (%d,%d) out of bounds", *x, *y);*/
             *x = x_maze_max;
             *y = y_maze_max;
-        } else {
-            *x = *y = -1;
+        } else {*x = -1; *y = -1;
         }
     }
 }
@@ -1025,8 +1020,7 @@ get_unpacked_coord(int64_t loc, int defhumidity)
 {
     static unpacked_coord c;
 
-    if (loc & SP_COORD_IS_RANDOM) {
-        c.x = c.y = -1;
+    if (loc & SP_COORD_IS_RANDOM) {c.x = -1; c.y = -1;
         c.is_random = 1;
         c.getloc_flags = (loc & ~SP_COORD_IS_RANDOM);
         if (!c.getloc_flags)
@@ -1229,9 +1223,8 @@ create_room(xchar x, xchar y, xchar w, xchar h, xchar xal, xchar yal, xchar rtyp
             hx = r1->hx;
             hy = r1->hy;
             lx = r1->lx;
-            ly = r1->ly;
-            if (vault)
-                dx = dy = 1;
+            ly = r1->ly;if (vault)
+                dx = 1; dy = 1;
             else {
                 dx = 2 + rn2((hx - lx > 28) ? 12 : 8);
                 dy = 2 + rn2(4);
@@ -1468,8 +1461,7 @@ create_door(room_door *dd, struct mkroom *broom)
             if (!isok(x + 1, y) || IS_ROCK(levl[x + 1][y].typ))
                 goto redoloop;
             goto outdirloop;
-        default:
-            x = y = 0;
+        default:x = 0; y = 0;
             panic("create_door: No wall for door!");
             return;
             //goto outdirloop;
@@ -1836,7 +1828,7 @@ create_monster(monster *m, struct mkroom *croom)
         if (m->name.str)
         {
             mtmp = christen_monst(mtmp, m->name.str);
-            mtmp->u_know_mname = 1; /* On special levels you know all generated monsters -- maybe should be specified later */
+            set_mon_u_know_mname(mtmp, 1); /* On special levels you know all generated monsters -- maybe should be specified later */
         }
 
         mtmp->mon_flags |= MON_FLAGS_SPLEVEL_RESIDENT; /* Created specifically for the special level; don't remove in bones if staying on the level */
@@ -2669,7 +2659,7 @@ create_object(object *o, struct mkroom *croom)
             {
                 otmp->speflags |= SPEFLAGS_MINES_PRIZE;
                 /* prevent stacking; cleared when achievement is recorded */
-                //otmp->nomerge = 1;
+                //set_obj_nomerge(otmp, 1);
                 if (++mines_prize_count > 1)
                     impossible(prize_warning, "mines end");
             }
@@ -2679,14 +2669,14 @@ create_object(object *o, struct mkroom *croom)
             if (otmp->otyp == iflags.soko_prize_type1)
             {
                 otmp->speflags |= SPEFLAGS_SOKO_PRIZE1;
-                //otmp->nomerge = 1; /* redundant; Sokoban prizes don't stack */
+                //set_obj_nomerge(otmp, 1); /* redundant; Sokoban prizes don't stack */
                 if (++soko_prize_count > 1)
                     impossible(prize_warning, "sokoban end");
             } 
             else if (otmp->otyp == iflags.soko_prize_type2) 
             {
                 otmp->speflags |= SPEFLAGS_SOKO_PRIZE2;
-                //otmp->nomerge = 1; /* redundant; Sokoban prizes don't stack */
+                //set_obj_nomerge(otmp, 1); /* redundant; Sokoban prizes don't stack */
                 if (++soko_prize_count > 1)
                     impossible(prize_warning, "sokoban end");
             }
@@ -3168,8 +3158,7 @@ search_door(struct mkroom *croom, xchar *x, xchar *y, xchar wall, int cnt)
         xx = croom->lx - 1;
         yy = croom->ly;
         break;
-    default:
-        dx = dy = xx = yy = 0;
+    default:dx = 0; dy = 0; xx = 0; yy = 0;
         panic("search_door: Bad wall!");
         return FALSE;
         break;
@@ -3646,9 +3635,7 @@ fill_empty_maze(void)
 {
     int mapcountmax, mapcount, mapfact;
     xchar x, y;
-    coord mm;
-
-    mapcountmax = mapcount = (x_maze_max - 2) * (y_maze_max - 2);
+    coord mm;mapcountmax = (x_maze_max - 2; mapcount = (x_maze_max - 2) * (y_maze_max - 2);
     mapcountmax = mapcountmax / 2;
 
     for (x = 2; x < x_maze_max; x++)
@@ -5540,8 +5527,7 @@ selection_rndcoord(struct opvar *ov, schar *x, schar *y, boolean removeit)
                     }
                     c--;
                 }
-    }
-    *x = *y = -1;
+    }*x = -1; *y = -1;
     return 0;
 }
 
@@ -6181,7 +6167,7 @@ sel_set_door(int dx, int dy, genericptr_t arg, genericptr_t arg2, genericptr_t a
 
     dmask |= dflags;
 
-    set_door_orientation(x, y); /* set/clear levl[x][y].horizontal */
+    set_door_orientation(x, y); /* set/clear is_lev_horizontal(x, y) */
     levl[x][y].subtyp = subtyp;
     levl[x][y].vartyp = get_initial_location_vartype(levl[x][y].typ, levl[x][y].subtyp);
     levl[x][y].doormask = dmask;
@@ -7019,9 +7005,7 @@ spo_region(struct sp_coder *coder)
     troom->needjoining = joined;
 
     if (irregular) 
-    {
-        min_rx = max_rx = dx1;
-        min_ry = max_ry = dy1;
+    {min_rx = dx1; max_rx = dx1;min_ry = dy1; max_ry = dy1;
         smeq[nroom] = nroom;
         flood_fill_rm(dx1, dy1, nroom + ROOMOFFSET, (boolean)OV_i(rlit), TRUE);
         add_room(min_rx, min_ry, max_rx, max_ry, FALSE, (schar)OV_i(rtype), TRUE, usedfloortype, usedfloorsubtype, roommontype, roomtileset, decotyp);
@@ -7865,8 +7849,7 @@ sp_level_coder(sp_lev *lvl)
     icedpools = FALSE;
     /* achievement tracking; static init would suffice except we need to
        reset if #wizmakemap is used to recreate mines' end or sokoban end;
-       once either level is created, these values can be forgotten */
-    mines_prize_count = soko_prize_count = 0;
+       once either level is created, these values can be forgotten */mines_prize_count = 0; soko_prize_count = 0;
 
     if (wizard) {
         char *met = nh_getenv("SPCODER_MAX_RUNTIME");
@@ -8606,7 +8589,7 @@ sp_level_coder(sp_lev *lvl)
      * purpose.
      */
 
-    //if (!level.flags.corrmaze)
+    //if (!get_flag(level.flags.bitflags, LEVEL_BITFLAGS_CORRMAZE))
     wallification(1, 0, COLNO - 1, ROWNO - 1);
 
     count_features();
@@ -8676,9 +8659,7 @@ void
 reset_splev(void)
 {
     selection_flood_check_func = 0;
-    floodfillchk_match_under_typ = 0;
-    xsize = ysize = 0;
-    xstart = ystart = 0;
+    floodfillchk_match_under_typ = 0;xsize = 0; ysize = 0;xstart = 0; ystart = 0;
     memset((genericptr_t)&SpLev_Map, 0, sizeof(SpLev_Map));
     ralign[0] = AM_CHAOTIC;
     ralign[1] = AM_NEUTRAL;
diff --git a/src/spell.c b/src/spell.c
index e65ac3ea2..ff33d0533 100644
--- a/src/spell.c
+++ b/src/spell.c
@@ -529,8 +528,7 @@ learn(void)
         if (book->spestudied > MAX_SPELL_STUDY)
         {
             play_sfx_sound(SFX_SPELL_TOO_FAINT);
-            pline_ex(ATR_NONE, CLR_MSG_FAIL, "This spellbook is too faint to be read any more.");
-            book->otyp = booktype = SPE_BLANK_PAPER;
+            pline_ex(ATR_NONE, CLR_MSG_FAIL, "This spellbook is too faint to be read any more.");book->otyp = SPE_BLANK_PAPER; booktype = SPE_BLANK_PAPER;
             book->material = objects[book->otyp].oc_material;
             book->owt = weight(book);
             /* reset spestudied as if polymorph had taken place */
@@ -570,8 +568,7 @@ learn(void)
         if (book->spestudied >= MAX_SPELL_STUDY) {
             /* pre-used due to being the product of polymorph */
             play_sfx_sound(SFX_SPELL_TOO_FAINT);
-            pline_ex(ATR_NONE, CLR_MSG_FAIL, "This spellbook is too faint to read even once.");
-            book->otyp = booktype = SPE_BLANK_PAPER;
+            pline_ex(ATR_NONE, CLR_MSG_FAIL, "This spellbook is too faint to read even once.");book->otyp = SPE_BLANK_PAPER; booktype = SPE_BLANK_PAPER;
             book->material = objects[book->otyp].oc_material;
             book->owt = weight(book);
             /* reset spestudied as if polymorph had taken place */
@@ -2645,10 +2642,10 @@ spelleffects(int spell, boolean atme, struct monst *targetmonst, boolean *stop_r
         return res;
     } 
     //else {
-        /* If hero is a wizard, their current intelligence
+        /* If hero is a wizard,their current intelligence
              * (bonuses + temporary + current)
              * affects hunger reduction in casting a spell.
-             * 1. int = 17-18 no reduction
+             * 1. int = 16    1/4 hungr; 17-18 no reduction
              * 2. int = 16    1/4 hungr
              * 3. int = 15    1/2 hungr
              * 4. int = 1-14  normal reduction
@@ -2744,7 +2741,7 @@ spelleffects(int spell, boolean atme, struct monst *targetmonst, boolean *stop_r
     pseudo.otyp = spellid(spell);
     pseudo.oclass = SPBOOK_CLASS;
     pseudo.quan = 20L; /* do not let useup get it */
-    //pseudo->blessed = pseudo->cursed = 0;
+    //set_obj_blessed(pseudo, 0); set_obj_cursed(pseudo, 0);
     /*
      * Find the skill the hero has in a spell type category.
      * See spell_skilltype for categories.
@@ -2951,8 +2948,7 @@ spelleffects(int spell, boolean atme, struct monst *targetmonst, boolean *stop_r
         if (objects[otyp].oc_dir != NODIR)
         {
             if (atme) 
-            {
-                u.dx = u.dy = u.dz = 0;
+            {u.dx = 0; u.dy = 0; u.dz = 0;
             } 
             else if (!getdir((char *) 0))
             {
@@ -3132,7 +3128,7 @@ spelleffects(int spell, boolean atme, struct monst *targetmonst, boolean *stop_r
         special_effect_wait_until_action(0);
         if (!Blocks_Clairvoyance) {
             if (role_skill >= P_SKILLED)
-                pseudo.blessed = 1; /* detect monsters as well as map */
+                set_obj_blessed(&(pseudo), 1); /* detect monsters as well as map */
             do_vicinity_map(&pseudo);
         /* at present, only one thing blocks clairvoyance */
         } else if (uarmh && uarmh->otyp == CORNUTHAUM)
diff --git a/src/steed.c b/src/steed.c
index 09ffb03fe..8fa19e2f4 100644
--- a/src/steed.c
+++ b/src/steed.c
@@ -304,7 +304,7 @@ mount_steed(struct monst *mtmp)
         play_sfx_sound(SFX_GENERAL_CURRENTLY_UNABLE_TO_DO);
         Your_ex(ATR_NONE, CLR_MSG_FAIL, "%s are in no shape for riding.", makeplural(body_part(LEG)));
         if (force && wizard && yn_query("Heal your legs?") == 'y')
-            HWounded_legs = EWounded_legs = 0L;
+            HWounded_legs = 0L; EWounded_legs = 0L;
         else
             return (FALSE);
     }
@@ -529,8 +529,7 @@ mount_steed(struct monst *mtmp)
     */
     u.usteed = mtmp;
     remove_monster(mtmp->mx, mtmp->my);
-    teleds(mtmp->mx, mtmp->my, TRUE, FALSE);
-    context.botl = context.botlx = TRUE;
+    teleds(mtmp->mx, mtmp->my, TRUE, FALSE);context.botl = TRUE; context.botlx = TRUE;
     return TRUE;
 }
 
@@ -867,8 +866,7 @@ dismount_steed(int reason)
         vision_full_recalc = 1;
     } 
     /* polearms behave differently when not mounted */
-    update_unweapon();
-    context.botl = context.botlx = TRUE;
+    update_unweapon();context.botl = TRUE; context.botlx = TRUE;
     /*
     if (uwep && is_pole(uwep))
         unweapon = TRUE;
diff --git a/src/teleport.c b/src/teleport.c
index 618de55df..b2ec2a66b 100644
--- a/src/teleport.c
+++ b/src/teleport.c
@@ -317,8 +317,7 @@ teleds(int nux, int nuy, boolean allow_drag, boolean keep_effect_glyphs)
         youmonst.m_ap_type = M_AP_NOTHING;
     }
 
-    if (u.uswallow) {
-        u.uswldtim = u.uswallow = 0;
+    if (u.uswallow) {u.uswldtim = 0; u.uswallow = 0;
         if (Punished && !ball_active) {
             /* ensure ball placement, like unstuck */
             ball_active = TRUE;
@@ -806,9 +805,7 @@ dotelecmd(void)
 
     /* normal mode; ignore 'm' prefix if it was given */
     if (!wizard)
-        return dotele(FALSE);
-
-    added = hidden = NOOP_SPELL;
+        return dotele(FALSE);added = NOOP_SPELL; hidden = NOOP_SPELL;
     save_HTele = HTeleportation, save_ETele = ETeleportation;
     if (!iflags.menu_requested)
     {
@@ -886,12 +883,10 @@ dotelecmd(void)
             HTeleportation |= I_SPECIAL; /* confer intrinsic teleportation */
             hidden = tport_spell(HIDE_SPELL); /* hide teleport-away */
             break;
-        case 's':
-            HTeleportation = ETeleportation = 0L; /* suppress intrinsic */
+        case 's':HTeleportation = 0L; ETeleportation = 0L; /* suppress intrinsic */
             added = tport_spell(ADD_SPELL); /* add teleport-away */
             break;
-        case 't':
-            HTeleportation = ETeleportation = 0L; /* suppress intrinsic */
+        case 't':HTeleportation = 0L; ETeleportation = 0L; /* suppress intrinsic */
             hidden = tport_spell(HIDE_SPELL); /* hide teleport-away */
             break;
         case 'w':
@@ -1274,8 +1269,7 @@ random_levtport:
                 /* take unpaid inventory items off of shop bills */
                 in_mklev = TRUE; /* suppress map update */
                 u_left_shop(u.ushops0, TRUE);
-                /* you're now effectively out of the shop */
-                *u.ushops0 = *u.ushops = '\0';
+                /* you're now effectively out of the shop */*u.ushops0 = '\0'; *u.ushops = '\0';
                 in_mklev = FALSE;
             }
             if (newlev <= -10) {
@@ -1661,7 +1655,7 @@ rloc(struct monst *mtmp, boolean suppress_impossible)
         return TRUE;
     }
 
-    if (mtmp->iswiz && mtmp->mx) { /* Wizard, not just arriving */
+    if (is_mon_wiz(mtmp) && mtmp->mx) { /* Wizard, not just arriving */
         if (!In_W_tower(u.ux, u.uy, &u.uz))
             x = xupstair, y = yupstair;
         else if (!xdnladder) /* bottom level of tower */
diff --git a/src/timeout.c b/src/timeout.c
index 06b6ff4c0..8827aa296 100644
--- a/src/timeout.c
+++ b/src/timeout.c
@@ -146,8 +146,7 @@ sick_dialogue(void)
 
     if (updategui)
     {
-        refresh_u_tile_gui_info(TRUE);
-        context.botl = context.botlx = 1;
+        refresh_u_tile_gui_info(TRUE);context.botl = 1; context.botlx = 1;
     }
 
 }
@@ -206,8 +205,7 @@ food_poisoned_dialogue(void)
 
     if (updategui)
     {
-        refresh_u_tile_gui_info(TRUE);
-        context.botl = context.botlx = 1;
+        refresh_u_tile_gui_info(TRUE);context.botl = 1; context.botlx = 1;
     }
 }
 
@@ -1043,8 +1041,7 @@ nh_timeout(void)
                 break;
             case DEAF:
                 set_itimeout(&HDeaf, 1L);
-                make_deaf(0L, TRUE);
-                context.botl = context.botlx = TRUE;
+                make_deaf(0L, TRUE);context.botl = TRUE; context.botlx = TRUE;
                 if (!Deaf)
                     stop_occupation();
                 break;
@@ -1463,9 +1460,9 @@ nh_timeout(void)
         || (ACURR(A_INT) != oldint)
         || (ACURR(A_WIS) != oldwis)
         || (ACURR(A_CHA) != oldcha)
-        || (old_move != get_u_move_speed(TRUE))
+        || (old_move !=get_u_move_speed(TRUE))
         )
-        context.botl = context.botlx = 1;
+        context.botl = 1; context.botlx = 1;
 
 
 }
@@ -1474,8 +1471,7 @@ void
 fall_asleep(int how_long, boolean wakeup_msg)
 {
     stop_occupation();
-    incr_itimeout(&HSleeping, abs(how_long));
-    context.botl = context.botlx = 1;
+    incr_itimeout(&HSleeping, abs(how_long));context.botl = 1; context.botlx = 1;
     refresh_u_tile_gui_info(TRUE);
 
     if (wakeup_msg)
@@ -1538,9 +1534,7 @@ hatch_egg(anything *arg, int64_t timeout)
     egg = arg->a_obj;
     /* sterilized while waiting */
     if (egg->corpsenm == NON_PM)
-        return FALSE;
-
-    mon = mon2 = (struct monst *) 0;
+        return FALSE;mon = (struct monst *; mon2 = (struct monst *) 0;
     mnum = big_to_little(egg->corpsenm);
     /* The identity of one's father is learned, not innate */
     yours = (egg->speflags & SPEFLAGS_YOURS) != 0;
@@ -2029,8 +2023,7 @@ burn_object(anything *arg, int64_t timeout)
         (void) Shk_Your(whose, obj);
     } else {
         canseeit = FALSE;
-    }
-    need_newsym = need_invupdate = FALSE;
+    }need_newsym = FALSE; need_invupdate = FALSE;
 
     boolean warncolor = FALSE;
     /* obj->age is the age remaining at this point.  */
@@ -2442,7 +2435,7 @@ begin_burn(struct obj *obj, boolean already_lit)
         /* Infinite burn */
         do_timer = FALSE;
         do_lamplit = TRUE;
-        //obj->lamplit = 1;
+        //set_obj_lamplit(obj, 1);
 
         if (obj->otyp == MAGIC_CANDLE)
         {
@@ -2455,12 +2448,12 @@ begin_burn(struct obj *obj, boolean already_lit)
         switch (obj->otyp) 
         {
         case MAGIC_LAMP:
-            //obj->lamplit = 1;
+            //set_obj_lamplit(obj, 1);
             do_timer = FALSE;
             do_lamplit = TRUE;
             break;
         case MAGIC_CANDLE:
-            //obj->lamplit = 1;
+            //set_obj_lamplit(obj, 1);
             do_lamplit = TRUE;
             if (obj->special_quality == SPEQUAL_MAGIC_CANDLE_UNUSED)
                 obj->special_quality = SPEQUAL_MAGIC_CANDLE_PARTLY_USED;
@@ -2523,7 +2516,7 @@ begin_burn(struct obj *obj, boolean already_lit)
     if (do_timer) 
     {
         if (start_timer(turns, TIMER_OBJECT, BURN_OBJECT, obj_to_any(obj))) {
-            //obj->lamplit = 1;
+            //set_obj_lamplit(obj, 1);
             do_lamplit = TRUE;
             obj->age -= turns;
             if (carried(obj) && !already_lit)
@@ -2531,7 +2524,7 @@ begin_burn(struct obj *obj, boolean already_lit)
         }
         else 
         {
-            //obj->lamplit = 0;
+            //set_obj_lamplit(obj, 0);
             do_lamplit = FALSE;
         }
     } 
@@ -2884,8 +2877,7 @@ do_storms(void)
         /* Inside a cloud during a thunder storm is deafening. */
         /* Even if already deaf, we sense the thunder's vibrations. */
         pline_ex(ATR_NONE, CLR_MSG_ATTENTION, "Kaboom!!!  Boom!!  Boom!!");
-        incr_itimeout(&HDeaf, rn1(20, 30));
-        context.botl = context.botlx = TRUE;
+        incr_itimeout(&HDeaf, rn1(20, 30));context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         play_environment_ambient_sounds();
         if (!u.uinvulnerable) {
@@ -3004,14 +2996,12 @@ wiz_timeout_queue(void)
     /* Timed properies:
      * check every one; the majority can't obtain temporary timeouts in
      * normal play but those can be forced via the #wizintrinsic command.
-     */
-    count = longestlen = 0;
+     */count = 0; longestlen = 0;
     for (i = 0; (propname = propertynames[i].prop_name) != 0; ++i) {
         p = propertynames[i].prop_num;
         intrinsic = u.uprops[p].intrinsic;
         if (intrinsic & TIMEOUT) {
-            ++count;
-            if ((ln = (int) strlen(propname)) > longestlen)
+            ++count;if ((ln = ln; (int) strlen(propname)) > longestlen)
                 longestlen = ln;
         }
         if (specindx == 0 && p == FIRE_IMMUNITY)
@@ -3761,8 +3751,7 @@ timer_stats(const char *hdrfmt, char *hdrbuf, int64_t *count, size_t *size)
 {
     timer_element *te;
 
-    Sprintf(hdrbuf, hdrfmt, (int64_t) sizeof (timer_element));
-    *count = *size = 0L;
+    Sprintf(hdrbuf, hdrfmt, (int64_t) sizeof (timer_element));*count = 0L; *size = 0L;
     for (te = timer_base; te; te = te->next) {
         ++*count;
         *size += sizeof *te;
@@ -4456,8 +4445,7 @@ property_expiry_message(int propidx, boolean was_flying)
         break;
     case FLYING:
         /* timed Flying is via #wizintrinsic only */
-        if (was_flying && !Flying) {
-            context.botl = context.botlx = 1;
+        if (was_flying && !Flying) {context.botl = 1; context.botlx = 1;
             You_ex(ATR_NONE, CLR_MSG_ATTENTION, "land.");
             spoteffects(TRUE);
         }
diff --git a/src/topten.c b/src/topten.c
index 204abbc16..b1c8211b3 100644
--- a/src/topten.c
+++ b/src/topten.c
@@ -231,8 +231,7 @@ readentry(FILE *rfile, struct toptenentry *tt)
 #endif
 
 #ifdef UPDATE_RECORD_IN_PLACE
-    /* note: input below must read the record's terminating newline */
-    final_fpos = tt->fpos = ftell(rfile);
+    /* note: input below must read the record's terminating newline */final_fpos = ftell(rfile; tt->fpos = ftell(rfile);
 #endif
 #define TTFIELDS 13
     if (fscanf(rfile, fmt, &tt->ver_major, &tt->ver_minor, &tt->patchlevel,
@@ -257,8 +256,7 @@ readentry(FILE *rfile, struct toptenentry *tt)
         if (tt->ver_major < 3 || (tt->ver_major == 3 && tt->ver_minor < 3)) {
             int i;
 
-            if (sscanf(inbuf, fmt32, tt->plrole, tt->plgend, s1, s2) == 4) {
-                tt->plrole[1] = tt->plgend[1] = '\0'; /* read via %c */
+            if (sscanf(inbuf, fmt32, tt->plrole, tt->plgend, s1, s2) == 4) {tt->plrole[1] = '\0'; tt->plgend[1] = '\0'; /* read via %c */
                 copynchars(tt->name, s1, (int) (sizeof tt->name) - 1);
                 copynchars(tt->death, s2, (int) (sizeof tt->death) - 1);
             } else
@@ -837,9 +835,7 @@ topten(int how, time_t when)
 
     /* assure minimum number of points */
     if (t0->points < sysopt.pointsmin)
-        t0->points = 0;
-
-    t1 = tt_head = newttentry();
+        t0->points = 0;t1 = newttentry(; tt_head = newttentry();
     tprev = 0;
     /* rank0: -1 undefined, 0 not_on_list, n n_th on list */
     for (rank = 1;;) {
@@ -970,13 +966,7 @@ topten(int how, time_t when)
         truncate_file(rfile);
 #else
         /* use sentinel record rather than relying on truncation */
-        t1->points = 0L; /* terminates file when read back in */
-        t1->ver_major = t1->ver_minor = t1->patchlevel = 0;
-        t1->uid = t1->deathdnum = t1->deathlev = 0;
-        t1->maxlvl = t1->hp = t1->maxhp = t1->deaths = 0;
-        t1->plrole[0] = t1->plrace[0] = t1->plgend[0] = t1->plalign[0] = '-';
-        t1->plrole[1] = t1->plrace[1] = t1->plgend[1] = t1->plalign[1] = 0;
-        t1->birthdate = t1->deathdate = yyyymmdd((time_t) 0L);
+        t1->points = 0L; /* terminates file when read back in */t1->ver_major = 0; t1->ver_minor = 0; t1->patchlevel = 0;t1->uid = 0; t1->deathdnum = 0; t1->deathlev = 0;t1->maxlvl = 0; t1->hp = 0; t1->maxhp = 0; t1->deaths = 0;t1->plrole[0] = '-'; t1->plrace[0] = '-'; t1->plgend[0] = '-'; t1->plalign[0] = '-';t1->plrole[1] = 0; t1->plrace[1] = 0; t1->plgend[1] = 0; t1->plalign[1] = 0;t1->birthdate = yyyymmdd((time_t; t1->deathdate = yyyymmdd((time_t) 0L);
         Strcpy(t1->name, "@");
         Strcpy(t1->death, "<eod>\n");
         writeentry(rfile, t1);
@@ -1288,9 +1278,7 @@ prscore(int argc, char **argv)
         playerct = --argc;
         players = (const char **) ++argv;
     }
-    raw_print("");
-
-    t1 = tt_head = newttentry();
+    raw_print("");t1 = newttentry(; tt_head = newttentry();
     for (rank = 1;; rank++) {
         readentry(rfile, t1);
         if (t1->points == 0)
diff --git a/src/trap.c b/src/trap.c
index c6c2db3a6..d4b99b257 100644
--- a/src/trap.c
+++ b/src/trap.c
@@ -419,9 +419,7 @@ maketrap(int x, int y, int typ, int permonstid, uint64_t mkflags)
         ttmp->ty = y;
     }
     /* [re-]initialize all fields except ntrap (handled below) and <tx,ty> */
-    ttmp->vl = zero_vl;
-    ttmp->launch.x = ttmp->launch.y = -1; /* force error if used before set */
-    ttmp->dst.dnum = ttmp->dst.dlevel = -1;
+    ttmp->vl = zero_vl;ttmp->launch.x = -1; ttmp->launch.y = -1; /* force error if used before set */ttmp->dst.dnum = -1; ttmp->dst.dlevel = -1;
     ttmp->madeby_u = (mkflags & MKTRAPFLAG_MADE_BY_U) != 0;
     ttmp->madeby_mon = (mkflags & MKTRAPFLAG_MADE_BY_MON) != 0;
     ttmp->once = 0;
@@ -441,8 +439,8 @@ maketrap(int x, int y, int typ, int permonstid, uint64_t mkflags)
         int tavail[12], tpick[12], tcnt = 0, k;
         struct trap *t;
 
-        for (k = 0; k < 12; ++k)
-            tavail[k] = tpick[k] = 0;
+        for (k = 0; k < 12;++k)
+            tavail[k] = 0; tpick[k] = 0;
         for (t = ftrap; t; t = t->ntrap)
             if (t->ttyp == SQKY_BOARD && t != ttmp)
                 tavail[t->tnote] = 1;
@@ -2022,8 +2020,7 @@ steedintrap(struct trap *trap, struct obj *otmp)
         return 0;
     tt = trap->ttyp;
     steed->mx = u.ux;
-    steed->my = u.uy;
-    trapkilled = steedhit = FALSE;
+    steed->my = u.uy;trapkilled = FALSE; steedhit = FALSE;
 
     switch (tt) {
     case ARROW_TRAP:
@@ -2581,9 +2578,7 @@ mkroll_launch(struct trap *ttmp, xchar x, xchar y, short otyp, int64_t ocount)
             --distance;
     }
     if (!success) {
-        /* create the trap without any ammo, launch pt at trap location */
-        cc.x = bcc.x = x;
-        cc.y = bcc.y = y;
+        /* create the trap without any ammo, launch pt at trap location */cc.x = x; bcc.x = x;cc.y = y; bcc.y = y;
     } else {
         otmp = mksobj(otyp, TRUE, FALSE, FALSE);
         otmp->quan = ocount;
@@ -3692,8 +3687,7 @@ mselftouch(struct monst *mon, const char *arg, boolean byplayer)
 void
 float_up(void)
 {
-    play_movement_sound(&youmonst, CLIMBING_TYPE_STAIRS_UP);
-    context.botl = context.botlx = TRUE;
+    play_movement_sound(&youmonst, CLIMBING_TYPE_STAIRS_UP);context.botl = TRUE; context.botlx = TRUE;
     if (u.utrap) {
         if (u.utraptype == TT_PIT) {
             reset_utrap(FALSE);
@@ -3810,8 +3804,7 @@ float_down(int64_t hmask, int64_t emask)
                               : "ground"); /* TT_INFLOOR */
         (void) encumber_msg(); /* carrying capacity might have changed */
         return 0;
-    }
-    context.botl = context.botlx = TRUE;
+    }context.botl = TRUE; context.botlx = TRUE;
     nomul(0); /* stop running or resting */
     if (Blocks_Flying) {
         /* controlled flight no longer overridden by levitation */
@@ -4145,8 +4138,7 @@ domagictrap(void)
         if (!Deaf) 
         {
             You_hear_ex(ATR_NONE, CLR_MSG_NEGATIVE, "a deafening roar!");
-            incr_itimeout(&HDeaf, rn1(20, 30));
-            context.botl = context.botlx = TRUE;
+            incr_itimeout(&HDeaf, rn1(20, 30));context.botl = TRUE; context.botlx = TRUE;
             refresh_u_tile_gui_info(TRUE);
             play_environment_ambient_sounds();
         } 
@@ -4154,8 +4146,7 @@ domagictrap(void)
         {
             /* magic vibrations still hit you */
             You_feel_ex(ATR_NONE, CLR_MSG_NEGATIVE, "rankled.");
-            incr_itimeout(&HDeaf, rn1(5, 15));
-            context.botl = context.botlx = TRUE;
+            incr_itimeout(&HDeaf, rn1(5, 15));context.botl = TRUE; context.botlx = TRUE;
             refresh_u_tile_gui_info(TRUE);
             play_environment_ambient_sounds();
         }
@@ -4774,8 +4764,7 @@ water_damage_chain(struct obj *obj, boolean here)
     struct obj *otmp;
 
     /* initialize acid context: so far, neither seen (dknown) potions of
-       acid nor unseen have exploded during this water damage sequence */
-    acid_ctx.dkn_boom = acid_ctx.unk_boom = 0;
+       acid nor unseen have exploded during this water damage sequence */acid_ctx.dkn_boom = 0; acid_ctx.unk_boom = 0;
     acid_ctx.ctx_valid = TRUE;
 
     for (; obj; obj = otmp) {
@@ -4783,8 +4772,7 @@ water_damage_chain(struct obj *obj, boolean here)
         water_damage(obj, (char *) 0, FALSE);
     }
 
-    /* reset acid context */
-    acid_ctx.dkn_boom = acid_ctx.unk_boom = 0;
+    /* reset acid context */acid_ctx.dkn_boom = 0; acid_ctx.unk_boom = 0;
     acid_ctx.ctx_valid = FALSE;
 }
 
@@ -4931,8 +4919,7 @@ drown(void)
         if (!is_pool(u.ux, u.uy))
             return TRUE;
     }
-    crawl_ok = FALSE;
-    x = y = 0; /* lint suppression */
+    crawl_ok = FALSE;x = 0; y = 0; /* lint suppression */
     /* if sleeping, wake up now so that we don't crawl out of water
        while still asleep; we can't do that the same way that waking
        due to combat is handled; note unmul() clears u.usleep */
@@ -5086,7 +5073,7 @@ succeed_untrap(int ttyp, boolean helping_another)
 
     //boolean res = FALSE;
     //int probability = 0;
-    int usedskilllevel = max(P_UNSKILLED, min(P_GRAND_MASTER, P_SKILL_LEVEL(P_DISARM_TRAP) + (Enhanced_untrap ? 1 : 0))); /* (Role_if(PM_ROGUE) && u.uhave.questart) */
+    int usedskilllevel = max(P_UNSKILLED, min(P_GRAND_MASTER, P_SKILL_LEVEL(P_DISARM_TRAP) + (Enhanced_untrap ? 1 : 0))); /* (Role_if(PM_ROGUE) && get_flag(u.uhave.bitflags, UHAVE_BITFLAGS_QUESTART)) */
 
     //probability = untrap_probability(ttmp->ttyp, usedskilllevel, &youmonst);
     if (Fumbling & !rn2(2))
@@ -5397,9 +5384,7 @@ move_into_trap(struct trap *ttmp)
 {
     int bc = 0;
     xchar x = ttmp->tx, y = ttmp->ty, bx, by, cx, cy;
-    boolean unused;
-
-    bx = by = cx = cy = 0; /* lint suppression */
+    boolean unused;bx = 0; by = 0; cx = 0; cy = 0; /* lint suppression */
     /* we know there's no monster in the way, and we're not trapped */
     if (!Punished
         || drag_ball(x, y, &bc, &bx, &by, &cx, &cy, &unused, TRUE))
@@ -6988,8 +6973,7 @@ chest_trap(struct obj *obj, int bodypart, boolean disarm)
             if (!Free_action)
             {
                 play_sfx_sound(SFX_CAUSE_PARALYSIS);
-                incr_itimeout(&HParalyzed, d(5, 6));
-                context.botl = context.botlx = 1;
+                incr_itimeout(&HParalyzed, d(5, 6));context.botl = 1; context.botlx = 1;
                 refresh_u_tile_gui_info(TRUE);
                 pline_ex(ATR_NONE, CLR_MSG_NEGATIVE, "Suddenly you are frozen in place!");
 #if 0
@@ -7454,8 +7438,7 @@ lava_effects(void)
             {
                 if (usurvive)
                 {
-                    const char* hclr = hcolor_multi_buf2(NH_DARK_RED);
-                    multicolor_buffer[1] = multicolor_buffer[3] = multicolor_buffer[2];
+                    const char* hclr = hcolor_multi_buf2(NH_DARK_RED);multicolor_buffer[1] = multicolor_buffer[2]; multicolor_buffer[3] = multicolor_buffer[2];
                     pline_multi_ex(ATR_NONE, CLR_MSG_WARNING, no_multiattrs, multicolor_buffer, "%s glows a %s%s%s, but remains intact.",
                         The(xname(obj)), "strange ", hclr, " light");
                 }
@@ -7632,7 +7615,7 @@ maybe_finish_sokoban(void)
                clear the sokoban_rules flag so that luck penalties for
                things like breaking boulders or jumping will no longer
                be given, and restrictions on diagonal moves are lifted */
-            Sokoban = 0; /* clear gl.level.flags.sokoban_rules */
+            Sokoban = 0; /* clear gl.get_flag(level.flags.bitflags, LEVEL_BITFLAGS_SOKOBAN_RULES) */
             /*
              * TODO: give some feedback about solving the sokoban puzzle
              * (perhaps say "congratulations" in Japanese?).
diff --git a/src/u_init.c b/src/u_init.c
index f547c4e6e..be6f4e05c 100644
--- a/src/u_init.c
+++ b/src/u_init.c
@@ -930,9 +924,7 @@ u_init(void)
     u.umortality = 0;
     u.ugrave_arise = NON_PM;
     u.facing_right = 0;
-    u.action = ACTION_TILE_NO_ACTION;
-
-    u.umonnum = u.umonster = urole.monsternum;
+    u.action = ACTION_TILE_NO_ACTION;u.umonnum = urole.monsternum; u.umonster = urole.monsternum;
     u.ulycn = NON_PM;
     set_uasmon();
     flags.female = flags.female;
@@ -942,9 +934,7 @@ u_init(void)
     u.ulevel = 0; /* set up some of the initial attributes */
 
     u.ubasehpmax = newhp();
-    u.ubaseenmax = newpw();
-
-    u.ubasehpdrain = u.basemhdrain = u.ubaseendrain = 0;
+    u.ubaseenmax = newpw();u.ubasehpdrain = 0; u.basemhdrain = 0; u.ubaseendrain = 0;
 
     u.uhp_fraction = 0;
     u.mh_fraction = 0;
@@ -955,8 +945,7 @@ u_init(void)
     nocreate3 = STRANGE_OBJECT;
     nocreate4 = STRANGE_OBJECT;
 
-    adjabil(0, 1);
-    u.ulevel = u.ulevelmax = 1;
+    adjabil(0, 1);u.ulevel = 1; u.ulevelmax = 1;
 
     init_attr(SUM_INIT_ATTRIBUTES); /* init attribute values */
     max_rank_sz(); /* set max str size for class ranks */
@@ -970,9 +959,7 @@ u_init(void)
     init_uhunger();
     for (i = 0; i <= MAXSPELL; i++)
         spl_book[i].sp_id = NO_SPELL;
-    u.uprayer_timeout = Role_if(PM_PRIEST) ? 150 : 300; /* no prayers just yet */
-    u.ualignbase[A_CURRENT] = u.ualignbase[A_ORIGINAL] = u.ualign.type =
-        aligns[flags.initalign].value;
+    u.uprayer_timeout = Role_if(PM_PRIEST) ? 150 : 300; /* no prayers just yet */u.ualignbase[A_CURRENT] = aligns[flags.initalign].value; u.ualignbase[A_ORIGINAL] = aligns[flags.initalign].value; u.ualign.type = aligns[flags.initalign].value;
 
 #if defined(BSD) && !defined(POSIX_TYPES)
     (void) time((int64_t *) &ubirthday);
@@ -1704,8 +1691,7 @@ ini_inv(const struct trobj *trop)
                         debugpline3("ini_inv: substituting %s for %s%s",
                             OBJ_NAME(objects[inv_subs[i].subs_otyp]),
                             (trop->trotyp == UNDEF_TYP) ? "random " : "",
-                            OBJ_NAME(objects[otyp]));
-                        otyp = obj->otyp = inv_subs[i].subs_otyp;
+                            OBJ_NAME(objects[otyp]));otyp = inv_subs[i].subs_otyp; obj->otyp = inv_subs[i].subs_otyp;
                         obj->material = objects[otyp].oc_material;
                     }
             }
@@ -2583,8 +2565,7 @@ back_from_race:
                     if ((windowprocs.wincap2 & WC2_SPECIAL_SYMBOLS) != 0 && !wizard
                         && (i == ROLE_BARBARIAN || i == ROLE_VALKYRIE))
                     {
-                        Strcat(anbuf, " &rec;");
-                        usingSpecialSymbols = didUseRecommended = TRUE;
+                        Strcat(anbuf, " &rec;");usingSpecialSymbols = TRUE; didUseRecommended = TRUE;
                     }
                     if (roles[i].desciption && *roles[i].desciption)
                     {
@@ -2718,8 +2699,7 @@ back_from_gender:
                         if ((windowprocs.wincap2 & WC2_SPECIAL_SYMBOLS) != 0 && !wizard && 
                             (i == RACE_GNOLL || (flags.initrole == ROLE_VALKYRIE && i == RACE_DWARF)))
                         {
-                            Strcat(buf, " &rec;");
-                            usingSpecialSymbols = didUseRecommended = TRUE;
+                            Strcat(buf, " &rec;");usingSpecialSymbols = TRUE; didUseRecommended = TRUE;
                         }
                         if (races[i].desciption && *races[i].desciption)
                         {
@@ -3312,8 +3292,7 @@ back_from_align:
             /* start fresh, but bypass "shall I pick everything for you?"
                step; any partial role selection via config file, command
                line, or name suffix is discarded this time */
-            pick4u = 'n';
-            flags.initrole = flags.initrace = flags.initgend = flags.initalign = ROLE_NONE;
+            pick4u = 'n';flags.initrole = ROLE_NONE; flags.initrace = ROLE_NONE; flags.initgend = ROLE_NONE; flags.initalign = ROLE_NONE;
             goto back_from_race;
             break;
         case 1: /* 'y' or Space or Return/Enter */
diff --git a/src/uhitm.c b/src/uhitm.c
index 3e5537036..1bd57a259 100644
--- a/src/uhitm.c
+++ b/src/uhitm.c
@@ -565,9 +564,8 @@ attack(struct monst *mtmp)
             nomul(0);
             context.move = FALSE;
             return TRUE;
-        }
-        else
-            unweapon1 = unweapon2 = FALSE;
+        }else
+            unweapon1 = FALSE; unweapon2 = FALSE;
     }
 
     boolean has_right_weapon = (uwep && is_weapon(uwep));
@@ -4183,8 +4180,7 @@ hmonas(struct monst *mon)
                     mattk = &alt_attk;
                 }
                 /* change damage to 1d1; not strangling but still
-                   doing [minimal] physical damage to victim's body */
-                mattk->damn = mattk->damd = 1;
+                   doing [minimal] physical damage to victim's body */mattk->damn = 1; mattk->damd = 1;
                 /* don't give 'unconcerned' feedback if there is extra damage
                    or if it is nearly destroyed or if creature doesn't have
                    the mental ability to be concerned in the first place */
@@ -4522,8 +4518,7 @@ passive(struct monst *mon, struct obj *weapon, boolean mhit, int malive, uchar a
                     } else {
                         play_sfx_sound(SFX_ACQUIRE_PARALYSIS);
                         You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "are frozen by %s gaze!", s_suffix(mon_nam(mon)));
-                        incr_itimeout(&HParalyzed, (ACURR(A_WIS) > 12 || rn2(4)) ? basedmg : 127);
-                        context.botl = context.botlx = 1;
+                        incr_itimeout(&HParalyzed, (ACURR(A_WIS) > 12 || rn2(4)) ? basedmg : 127);context.botl = 1; context.botlx = 1;
                         refresh_u_tile_gui_info(TRUE);
                         standard_hint("Do not hit floating eyes in melee unless you wear a blindfold or a towel. Use ranged weapons against them.", &u.uhint.paralyzed_by_floating_eye);
                     }
@@ -4539,8 +4534,7 @@ passive(struct monst *mon, struct obj *weapon, boolean mhit, int malive, uchar a
             } else { /* gelatinous cube */
                 play_sfx_sound(SFX_ACQUIRE_PARALYSIS);
                 You_ex(ATR_NONE, CLR_MSG_NEGATIVE, "are frozen by %s!", mon_nam(mon));
-                incr_itimeout(&HParalyzed, basedmg);
-                context.botl = context.botlx = 1;
+                incr_itimeout(&HParalyzed, basedmg);context.botl = 1; context.botlx = 1;
                 refresh_u_tile_gui_info(TRUE);
                 standard_hint("Get paralysis resistance as early as possible. Use ranged weapons to attack monsters with paralyzing passive defense.", &u.uhint.paralyzed_by_cube);
             }
diff --git a/src/vault.c b/src/vault.c
index 2ebab31aa..069269a89 100644
--- a/src/vault.c
+++ b/src/vault.c
@@ -141,7 +144,7 @@ restfakecorr(struct monst *grd)
 {
     /* it seems you left the corridor - let the guard disappear */
     if (clear_fcorr(grd, FALSE)) {
-        grd->isgd = 0; /* dmonsfree() should delete this mon */
+        set_mon_gd(grd, 0); /* dmonsfree() should delete this mon */
         mongone(grd);
     }
 }
@@ -179,7 +182,7 @@ grddead(struct monst *grd)
         dispose = clear_fcorr(grd, TRUE);
     }
     if (dispose)
-        grd->isgd = 0; /* for dmonsfree() */
+        set_mon_gd(grd, 0); /* for dmonsfree() */
     return dispose;
 }
 
@@ -806,8 +809,7 @@ gd_move(struct monst *grd)
             u_carry_gold = FALSE, newspot = FALSE, see_guard;
 
     if (!on_level(&(egrd->gdlevel), &u.uz))
-        return -1;
-    nx = ny = m = n = 0;
+        return -1;nx = 0; ny = 0; m = 0; n = 0;
 
     if (semi_dead || !grd->mx || egrd->gddone) 
     {
@@ -1037,8 +1039,7 @@ gd_move(struct monst *grd)
         {
             if ((nx == x || ny == y) && (nx != x || ny != y)
                 && isok(nx, ny))
-            {
-                typ = (crm = &levl[nx][ny])->typ;
+            {typ = &levl[nx][ny]; (crm = &levl[nx][ny])->typ;
                 //subtyp = crm->subtyp;
                 if (!IS_STWALL(typ) && !IS_POOL(typ)) 
                 {
@@ -1084,7 +1085,6 @@ gd_move(struct monst *grd)
         nx += dx;
     else
         ny += dy;
-
     while ((typ = (crm = &levl[nx][ny])->typ) != STONE) 
     {
         /* in view of the above we must have IS_WALL(typ) or typ == POOL */
diff --git a/src/vision.c b/src/vision.c
index b5af780c4..c2ead85e8 100644
--- a/src/vision.c
+++ b/src/vision.c
@@ -310,9 +310,7 @@ rogue_vision(char **next, char *rmin, char *rmax)
     /* If in a lit room, we are able to see to its boundaries. */
     /* If dark, set COULD_SEE so various spells work -dlc */
     if (rnum >= 0) {
-        for (zy = rooms[rnum].ly - 1; zy <= rooms[rnum].hy + 1; zy++) {
-            rmin[zy] = start = rooms[rnum].lx - 1;
-            rmax[zy] = stop = rooms[rnum].hx + 1;
+        for (zy = rooms[rnum].ly - 1; zy <= rooms[rnum].hy + 1; zy++) {rmin[zy] = rooms[rnum].lx - 1; start = rooms[rnum].lx - 1;rmax[zy] = rooms[rnum].hx + 1; stop = rooms[rnum].hx + 1;
 
             for (zx = start; zx <= stop; zx++) {
                 if (rooms[rnum].rlit) {
@@ -480,9 +478,9 @@ new_angle(struct rm *lev, unsigned char *sv, int row, int col)
  *        impacted by vision occur during the same move [make_blinded()]
  *
  * Control flag = 1.  An adjacent vision recalculation.  The hero has moved
- * one square.  Knowing this, it might be possible to optimize the vision
+ * one square.  Knowing this,it might be possible to optimize the vision
  * recalculation using the current knowledge.  This is presently unimplemented
- * and is treated as a control = 0 call.
+ * and is treated as a control = 2.  Turn off the vision system.  Nothing new will be; 0 call.
  *
  *      + Right after the hero moves. [domove()]
  *
@@ -747,8 +745,7 @@ vision_recalc(int control)
      *      do you know?  You have to check the closest adjacent position.
      *      Even so, that is not entirely correct.  But it seems close
      *      enough for now.
-     */
-    colbump[u.ux] = colbump[u.ux + 1] = 1;
+     */colbump[u.ux] = 1; colbump[u.ux + 1] = 1;
     for (row = 0; row < ROWNO; row++) 
     {
         dy = u.uy - row;
@@ -836,7 +833,7 @@ vision_recalc(int control)
                  * The hero can now see that it is not lit, so change waslit
                  * and update the location.
                  */
-                lev->waslit = 0; /* remember lit condition */
+                set_rm_waslit(lev, 0); /* remember lit condition */
                 newsym(col, row);
 
             /*
@@ -863,8 +860,7 @@ vision_recalc(int control)
             }
 
         } /* end for col . . */
-    }     /* end for row . .  */
-    colbump[u.ux] = colbump[u.ux + 1] = 0;
+    }     /* end for row . .  */colbump[u.ux] = 0; colbump[u.ux + 1] = 0;
 
 skip:
     /* This newsym() caused a crash delivering msg about failure to open
@@ -2561,7 +2557,7 @@ left_side(int row, int left_mark, int right, char *limits)
     int lim_min;
 
 #ifdef GCC_WARN
-    rowp = row_min = row_max = 0;
+    rowp = 0; row_min = 0; row_max = 0;
 #endif
     nrow = row + step;
     deeper = good_row(nrow) && (!limits || (*limits >= *(limits + 1)));
diff --git a/src/weapon.c b/src/weapon.c
index 65e9265d4..1680e7713 100644
--- a/src/weapon.c
+++ b/src/weapon.c
@@ -3011,7 +3011,7 @@ enhance_weapon_skill(void)
     do 
     {
         /* find longest available skill name, count those that can advance */
-        to_advance = eventually_advance = maxxed_cnt = 0;
+        to_advance = 0; eventually_advance = 0; maxxed_cnt = 0;
         longest = 0;
         for (i = 0; i < P_NUM_SKILLS; i++)
         {
@@ -3599,7 +3599,8 @@ enhance_weapon_skill(void)
             free((genericptr_t) selected);
             skill_advance(n);
             /* check for more skills able to advance, if so then .. */
-            for (n = i = 0; i < P_NUM_SKILLS; i++) 
+            n = 0;
+            for (i = 0; i < P_NUM_SKILLS; i++)
             {
                 if (can_advance(i, speedy)) 
                 {
@@ -3622,13 +3623,12 @@ update_can_advance_any_skill(void)
     for (i = 0; i < P_NUM_SKILLS; i++) {
         if (can_advance(i, FALSE)) {
             if(!u.canadvanceskill)
-                context.botl = context.botlx = TRUE;
+                context.botl = TRUE; context.botlx = TRUE;
             u.canadvanceskill = TRUE;
             return;
         }
-    }
-    if (u.canadvanceskill)
-        context.botl = context.botlx = TRUE;
+    }if (u.canadvanceskill)
+        context.botl = TRUE; context.botlx = TRUE;
 
     u.canadvanceskill = FALSE;
     return;
diff --git a/src/were.c b/src/were.c
index 98ff59f1a..8f8366258 100644
--- a/src/were.c
+++ b/src/were.c
@@ -247,8 +247,7 @@ you_unwere(boolean purify)
     else if (is_were(youmonst.data) && !u.mtimedone)
         u.mtimedone = rn1(200, 200); /* 40% of initial were change */
 
-    refresh_u_tile_gui_info(TRUE);
-    context.botl = context.botlx = 1;
+    refresh_u_tile_gui_info(TRUE);context.botl = 1; context.botlx = 1;
 }
 
 /* lycanthropy is being caught or cured, but no shape change is involved */
@@ -257,9 +256,7 @@ set_ulycn(int which)
 {
     u.ulycn = which;
     /* add or remove lycanthrope's innate intrinsics (Drain_resistance) */
-    set_uasmon();
-
-    context.botl = context.botlx = 1;
+    set_uasmon();context.botl = 1; context.botlx = 1;
 }
 
 /*were.c*/
diff --git a/src/wield.c b/src/wield.c
index 0ac984b76..64ef74c9c 100644
--- a/src/wield.c
+++ b/src/wield.c
@@ -318,8 +316,7 @@ ready_weapon(struct obj *wep, int64_t mask)
 void
 setuqwep(struct obj *obj)
 {
-    setworncore(obj, W_QUIVER, TRUE);
-    context.botl = context.botlx = TRUE;
+    setworncore(obj, W_QUIVER, TRUE);context.botl = TRUE; context.botlx = TRUE;
     status_reassess();
     /* no extra handling needed; this used to include a call to
        update_inventory() but that's already performed by setworn() */
@@ -1180,8 +1177,7 @@ dowieldquiver(void)
     /* Since the quiver isn't in your hands, don't check cantwield(), */
     /* will_weld(), touch_petrifies(), etc. */
     multi = 0;
-    /* forget last splitobj() before calling getobj() with ALLOW_COUNT */
-    context.objsplit.child_oid = context.objsplit.parent_oid = 0;
+    /* forget last splitobj() before calling getobj() with ALLOW_COUNT */context.objsplit.child_oid = 0; context.objsplit.parent_oid = 0;
 
     /* Prompt for a new quiver: "What do you want to ready?"
        (Include gems/stones as likely candidates if either primary
@@ -1653,9 +1649,7 @@ wield_tool(struct obj *obj, const char *verb)
             otmp->speflags &= ~SPEFLAGS_PREVIOUSLY_WIELDED;
 
         if(setprevwepflag && oldwep)
-            oldwep->speflags |= SPEFLAGS_PREVIOUSLY_WIELDED;
-
-        context.botl = context.botlx = TRUE;
+            oldwep->speflags |= SPEFLAGS_PREVIOUSLY_WIELDED;context.botl = TRUE; context.botlx = TRUE;
         status_reassess();
     }
 
@@ -1704,8 +1698,7 @@ dotwoweapon(void)
         u.twoweap = 1;
     }
 
-    update_hand_unweapon(2);
-    context.botl = context.botlx = TRUE;
+    update_hand_unweapon(2);context.botl = TRUE; context.botlx = TRUE;
     newsym(u.ux, u.uy);
     flush_screen(1);
     update_inventory();
diff --git a/src/wizard.c b/src/wizard.c
index c8d89813a..b3c97d4af 100644
--- a/src/wizard.c
+++ b/src/wizard.c
@@ -297,7 +297,7 @@ strategy(struct monst *mtmp)
         if ((strat = target_on(M3_WANTSAMUL, mtmp)) != STRAT_NONE)
             return strat;
 
-    if (u.uevent.invoked) { /* priorities change once gate opened */
+    if (get_flag(u.uevent.bitflags, UEVENT_BITFLAGS_INVOKED)) { /* priorities change once gate opened */
         if ((strat = target_on(M3_WANTSARTI, mtmp)) != STRAT_NONE)
             return strat;
         if ((strat = target_on(M3_WANTSBOOK, mtmp)) != STRAT_NONE)
@@ -812,8 +811,7 @@ resurrect(void)
         }
     }
 
-    if (mtmp) {
-        mtmp->mtame = mtmp->mpeaceful = 0; /* paranoia */
+    if (mtmp) {mtmp->mtame = 0; set_mon_peaceful(mtmp, 0); /* paranoia */
         set_mhostility(mtmp);
         newsym(mtmp->mx, mtmp->my);
         check_boss_fight(mtmp);
diff --git a/src/worn.c b/src/worn.c
index 903a69d0c..4c98a7fe3 100644
--- a/src/worn.c
+++ b/src/worn.c
@@ -255,8 +255,7 @@ setworncore(struct obj *obj, int64_t mask, boolean verbose_and_update_stats)
                     learnring(oobj, FALSE);
                 }
             }
-        }
-        context.botl = context.botlx = 1;
+        }context.botl = 1; context.botlx = 1;
         if (needbecomecursedmsg)
         {
             if (Blind)
@@ -346,8 +345,7 @@ setnotworncore(struct obj *obj, boolean verbose)
         {
             //Nonobservable ring
             learnring(obj, FALSE);
-        }
-        context.botl = context.botlx = 1;
+        }context.botl = 1; context.botlx = 1;
     }
     update_inventory();
     return obj_gone;
diff --git a/src/zap.c b/src/zap.c
index db0970fa7..b8dd24c48 100644
--- a/src/zap.c
+++ b/src/zap.c
@@ -2497,8 +2500,7 @@ get_obj_location(struct obj *obj, xchar *xp, xchar *yp, int locflags)
                     return TRUE;
                 }
     }
-    }
-    *xp = *yp = 0;
+    }*xp = 0; *yp = 0;
     return FALSE;
 }
 
@@ -2775,9 +2776,7 @@ revive(struct obj *corpse, boolean by_hero, int animateintomon, boolean replaceu
     {
         impossible("Attempting to revive %s?", xname(corpse));
         return (struct monst *) 0;
-    }
-
-    x = y = 0;
+    }x = 0; y = 0;
 
     if (corpse->where != OBJ_CONTAINED)
     {
@@ -2899,8 +2898,7 @@ revive(struct obj *corpse, boolean by_hero, int animateintomon, boolean replaceu
                 /* change shape to match the corpse */
                 (void) newcham(mtmp, mptr, subtype, FALSE, FALSE);
             } else if (mtmp->data->mlet == S_LESSER_UNDEAD) 
-            {
-                mtmp->mhp = mtmp->mhpmax = 100;
+            {mtmp->mhp = 100; mtmp->mhpmax = 100;
                 mtmp->mprops[VERY_FAST] |= M_INTRINSIC_ACQUIRED;
             }
         }
@@ -3350,8 +3348,7 @@ revive_from_inventory(struct monst *mon)
     int res = 0;
 
     youseeit = (mon == &youmonst) ? TRUE : canseemon(mon);
-    otmp2 = (mon == &youmonst) ? invent : mon->minvent;
-    owner[0] = corpse[0] = '\0'; /* lint suppression */
+    otmp2 = (mon == &youmonst) ? invent : mon->minvent;owner[0] = '\0'; corpse[0] = '\0'; /* lint suppression */
 
     while ((otmp = otmp2) != 0) {
         otmp2 = otmp->nobj;
@@ -4340,7 +4337,7 @@ bhito(struct obj *obj, struct obj *otmp, struct monst *origmonst)
          * The bypass bit on all objects is reset each turn, whenever
          * context.bypasses is set.
          *
-         * We check the obj->bypass bit above AND context.bypasses
+         * We check the is_obj_bypass(obj) bit above AND context.bypasses
          * as a safeguard against any stray occurrence left in an obj
          * struct someplace, although that should never happen.
          */
@@ -6370,7 +6366,7 @@ backfire(struct obj *otmp)
 {
     int dmg;
 
-    otmp->in_use = TRUE; /* in case losehp() is fatal */
+    set_obj_in_use(otmp, TRUE); /* in case losehp() is fatal */
     play_sfx_sound(SFX_EXPLOSION_MAGICAL);
     pline_ex(ATR_NONE, CLR_MSG_NEGATIVE, "%s suddenly explodes!", The(xname(otmp)));
     play_special_effect_at(SPECIAL_EFFECT_SMALL_FIERY_EXPLOSION, 0, u.ux, u.uy, FALSE);
@@ -7022,8 +7018,7 @@ zapyourself(struct obj *obj, boolean ordinary)
         damage = 0;
         //boolean was_fast = Fast;
         boolean was_very_fast = Very_fast;
-        incr_itimeout(&HVery_fast, duration);
-        context.botl = context.botlx = TRUE;
+        incr_itimeout(&HVery_fast, duration);context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         if (Very_fast && !was_very_fast && !Ultra_fast && !Super_fast && !Lightning_fast)
         {
@@ -7057,8 +7052,7 @@ zapyourself(struct obj *obj, boolean ordinary)
     case SPE_MASS_SLOW:
         damage = 0;
         boolean was_slowed = Slowed;
-        incr_itimeout(&HSlowed, duration);
-        context.botl = context.botlx = TRUE;
+        incr_itimeout(&HSlowed, duration);context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         if (Slowed && !was_slowed)
         {
@@ -7071,8 +7065,7 @@ zapyourself(struct obj *obj, boolean ordinary)
     case SPE_HASTE_MONSTER:
         damage = 0;
         boolean was_very_fast = Very_fast;
-        incr_itimeout(&HVery_fast, obj->oclass == WAND_CLASS ? rn1(10, 100 + 60 * bcsign(obj)) : duration);
-        context.botl = context.botlx = TRUE;
+        incr_itimeout(&HVery_fast, obj->oclass == WAND_CLASS ? rn1(10, 100 + 60 * bcsign(obj)) : duration);context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         if (Very_fast && !was_very_fast && !Ultra_fast && !Super_fast && !Lightning_fast)
         {
@@ -7087,8 +7080,7 @@ zapyourself(struct obj *obj, boolean ordinary)
         if (!check_ability_resistance_success(&youmonst, A_WIS, save_adj))
         {
             boolean was_paralyzed = Paralyzed_or_immobile;
-            incr_itimeout(&HParalyzed, duration);
-            context.botl = context.botlx = TRUE;
+            incr_itimeout(&HParalyzed, duration);context.botl = TRUE; context.botlx = TRUE;
             refresh_u_tile_gui_info(TRUE);
             if (Paralyzed_or_immobile && !was_paralyzed)
             {
@@ -7109,8 +7101,7 @@ zapyourself(struct obj *obj, boolean ordinary)
         if (!check_ability_resistance_success(&youmonst, A_WIS, save_adj))
         {
             boolean was_paralyzed = Paralyzed_or_immobile;
-            incr_itimeout(&HUndead_immobility, duration);
-            context.botl = context.botlx = TRUE;
+            incr_itimeout(&HUndead_immobility, duration);context.botl = TRUE; context.botlx = TRUE;
             refresh_u_tile_gui_info(TRUE);
             if (Paralyzed_or_immobile && !was_paralyzed)
             {
@@ -7129,8 +7120,7 @@ zapyourself(struct obj *obj, boolean ordinary)
     case SPE_SILENCE:
         damage = 0;
         boolean was_silenced = Silenced;
-        incr_itimeout(&HSilenced, duration);
-        context.botl = context.botlx = TRUE;
+        incr_itimeout(&HSilenced, duration);context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
         if (Silenced && !was_silenced)
         {
@@ -7568,7 +7559,7 @@ zapyourself(struct obj *obj, boolean ordinary)
         do
         {
             didmerge = FALSE;
-            for (otmp = invent; !didmerge && otmp; otmp = otmp->nobj)
+            for (otmp = invent; !didmerge && otmp;otmp = otmp->nobj; otmp->nobj)
                 for (onxt = otmp->nobj; onxt; onxt = onxt->nobj)
                     if (merged(&otmp, &onxt))
                     {
@@ -7850,8 +7841,7 @@ cancel_monst(struct monst *mdef, struct obj *obj, boolean youattack, boolean all
 
 
         /* Add cancellation debuff */
-        incr_itimeout(&HCancelled, duration);
-        context.botl = context.botlx = TRUE;
+        incr_itimeout(&HCancelled, duration);context.botl = TRUE; context.botlx = TRUE;
         refresh_u_tile_gui_info(TRUE);
 
         if (Upolyd) { /* includes lycanthrope in creature form */
@@ -8006,9 +7996,7 @@ zap_updown(struct obj *obj)
         hit_only_one = 4; /* 2- 6 targets based on BUC status */
 
     /* some wands have special effects other than normal bhitpile */
-    /* drawbridge might change <u.ux,u.uy> */
-    x = xx = u.ux;     /* <x,y> is zap location */
-    y = yy = u.uy;     /* <xx,yy> is drawbridge (portcullis) position */
+    /* drawbridge might change <u.ux,u.uy> */x = u.ux; xx = u.ux;     /* <x,y> is zap location */y = u.uy; yy = u.uy;     /* <xx,yy> is drawbridge (portcullis) position */
     ttmp = t_at(x, y); /* trap if there is one */
 
     switch (obj->otyp) {
@@ -9492,7 +9480,7 @@ zhitm(struct monst *mon, int type, struct obj *origobj, struct monst *origmonst,
         {
             save_adj = -4;
         }
-        sleep_eff = sleep_monst(mon, origobj, origmonst, !origobj ? rn1(5, 8) : duration, save_adj, NOTELL); // Duration 0 = permanent sleep
+        sleep_eff = sleep_monst(mon, origobj, origmonst, !origobj ? rn1(5, 8) : duration, save_adj, NOTELL); //Duration 0 = sleep_eff; permanent sleep
         sho_hit_eff = sleep_eff;
         if (out_flags_ptr && sleep_eff)
             *out_flags_ptr |= ZHITM_FLAGS_SLEEP;
@@ -10103,8 +10091,8 @@ is_buzztype_eyestalk(int type)
  * type == -10 to -19 : monster casting spell
  * type == -20 to -29 : monster breathing at you
  * type == -30 to -39 : monster firing an eyestalk
- * type == -40 to -49 : monster shooting a wand
- * called with dx = dy = 0 with vertical bolts
+ * type == -40 to -49 :monster shooting a wand
+ * called with dx = 0 with vertical bolts; dy = 0 with vertical bolts
  */
 void
 dobuzz(int type, struct obj *origobj, struct monst *origmonst, int dmgdice, int dicesize, int dmgplus, xchar sx, xchar sy, int dx, int dy, boolean say)
@@ -11438,10 +11425,8 @@ destroy_one_item(struct obj *obj, int osym, int dmgtyp, boolean forcedestroy)
     boolean physical_damage;
     int obj_sound_type = OBJECT_SOUND_TYPE_BREAK;
 
-    physical_damage = FALSE;
-    xresist = skip = 0;
-    /* lint suppression */
-    dmg = dindx = 0;
+    physical_damage = FALSE;xresist = 0; skip = 0;
+    /* lint suppression */dmg = 0; dindx = 0;
     quan = 0L;
 
     switch (dmgtyp) 
@@ -11464,8 +11449,7 @@ destroy_one_item(struct obj *obj, int osym, int dmgtyp, boolean forcedestroy)
             skip++;
             if (!Blind)
             {
-                const char* hclr = hcolor_multi_buf2(NH_DARK_RED);
-                multicolor_buffer[1] = multicolor_buffer[3] = multicolor_buffer[2];
+                const char* hclr = hcolor_multi_buf2(NH_DARK_RED);multicolor_buffer[1] = multicolor_buffer[2]; multicolor_buffer[3] = multicolor_buffer[2];
                 pline_multi_ex(ATR_NONE, CLR_MSG_ATTENTION, no_multiattrs, multicolor_buffer, "%s glows a %s%s%s, but remains intact.",
                     The(xname(obj)), "strange ", hclr, " light");
             }
@@ -11755,8 +11738,7 @@ destroy_mitem(struct monst *mtmp, int osym, int dmgtyp)
                 skip++;
                 if (vis)
                 {
-                    const char* hclr = hcolor_multi_buf2(NH_DARK_RED);
-                    multicolor_buffer[1] = multicolor_buffer[3] = multicolor_buffer[2];
+                    const char* hclr = hcolor_multi_buf2(NH_DARK_RED);multicolor_buffer[1] = multicolor_buffer[2]; multicolor_buffer[3] = multicolor_buffer[2];
                     pline_multi_ex(ATR_NONE, CLR_MSG_ATTENTION, no_multiattrs, multicolor_buffer, "%s glows a %s%s%s, but remains intact.",
                         The(distant_name(obj, xname)), "strange ", hclr, " light");
                 }
@@ -11824,8 +11806,7 @@ destroy_mitem(struct monst *mtmp, int osym, int dmgtyp)
             break;
         }
         if (!skip) 
-        {
-            for (i = cnt = 0L; i < quan; i++)
+        {for (i = 0L, cnt = 0L; i < quan; i++)
                 if (!rn2(3))
                     cnt++;
 
diff --git a/win/win32/xpl/GnollHackX/GnollHackX/GHConstants.cs b/win/win32/xpl/GnollHackX/GnollHackX/GHConstants.cs
index ac209ec6c..7ae0bb673 100644
--- a/win/win32/xpl/GnollHackX/GnollHackX/GHConstants.cs
+++ b/win/win32/xpl/GnollHackX/GnollHackX/GHConstants.cs
@@ -1,4 +1,4 @@
-﻿using System;
+using System;
 using System.Collections.Generic;
 using System.Globalization;
 using System.Reflection;
diff --git a/win/win32/xpl/libshare/libproc.c b/win/win32/xpl/libshare/libproc.c
index ffdd87b97..bdf2dc7ed 100644
--- a/win/win32/xpl/libshare/libproc.c
+++ b/win/win32/xpl/libshare/libproc.c
@@ -405,8 +405,7 @@ void lib_add_extended_menu(winid wid, int glyph, const ANY_P* identifier, char a
 }
 
 void lib_end_menu_ex(winid wid, const char* prompt, const char* subtitle)
-{
-    *_buf = *_buf2 = 0;
+{*_buf = 0; *_buf2 = 0;
     if (prompt)
         write_text2buf_utf8(_buf, UTF8BUFSZ, prompt);
     if (subtitle)
@@ -737,9 +736,7 @@ static char _defs[16] = { 0 };
 
 char lib_yn_function_ex(int style, int attr, int color, int glyph, const char* title, const char* question, const char* choices, char def, const char* resp_desc, const char* introline, uint64_t ynflags)
 {
-    reset_found_this_turn(); /* Otherwise, after user input, the animation might play again upon flush_screen */
-
-    *_qbuf = *_qtbuf = *_buf = *_buf2 = *_ibuf = 0;
+    reset_found_this_turn(); /* Otherwise, after user input, the animation might play again upon flush_screen */*_qbuf = 0; *_qtbuf = 0; *_buf = 0; *_buf2 = 0; *_ibuf = 0;
     if(question)
         write_text2buf_utf8(_qbuf, UTF8QBUFSZ, question);
     if (title)
@@ -758,9 +755,7 @@ char lib_yn_function_ex(int style, int attr, int color, int glyph, const char* t
 
 void lib_getlin_ex(int style, int attr, int color, const char* question, char* input, const char* placeholder, const char* linesuffix, const char* introline)
 {
-    reset_found_this_turn(); /* Otherwise, after user input, the animation might play again upon flush_screen */
-
-    *_qbuf = *_buf = *_buf2 = *_ibuf = *_utf8buf = 0;
+    reset_found_this_turn(); /* Otherwise, after user input, the animation might play again upon flush_screen */*_qbuf = 0; *_buf = 0; *_buf2 = 0; *_ibuf = 0; *_utf8buf = 0;
     if (question)
         write_text2buf_utf8(_qbuf, UTF8QBUFSZ, question);
     if (placeholder)
@@ -883,8 +878,7 @@ void lib_preference_update(const char* pref)
 {
     lib_callbacks.callback_preference_update(pref);
     if (!strcmp(pref, "statuslines"))
-    {
-        context.botl = context.botlx = 1;
+    {context.botl = 1; context.botlx = 1;
     }
 }
 
@@ -1332,8 +1326,7 @@ void lib_set_animation_timer_interval(unsigned int param)
 }
 
 int lib_open_special_view(struct special_view_info info)
-{
-    *_buf = *_buf2 = 0;
+{*_buf = 0; *_buf2 = 0;
     if (info.text)
         write_text2buf_utf8(_buf, UTF8BUFSZ, info.text);
     if (info.title)
@@ -1511,8 +1504,7 @@ lib_clear_context_menu(void)
 
 void
 lib_add_context_menu(int cmd_def_char, int cmd_cur_char, int style, int glyph, const char* cmd_text, const char* target_text, int attr, int color)
-{
-    *_buf = *_buf2 = 0;
+{*_buf = 0; *_buf2 = 0;
     if (cmd_text)
         write_text2buf_utf8(_buf, sizeof(_buf), cmd_text);
     if (target_text)
@@ -1543,8 +1535,7 @@ lib_display_floating_text(int x, int y, const char* text, int style, int attr, i
 
 void
 lib_display_screen_text(const char* text, const char* supertext, const char* subtext, int style, int attr, int color, uint64_t tflags)
-{
-    *_utf8buf = *_superutf8buf = *_subutf8buf = 0;
+{*_utf8buf = 0; *_superutf8buf = 0; *_subutf8buf = 0;
     if (text)
         write_text2buf_utf8(_utf8buf, sizeof(_utf8buf), text);
     if (supertext)
@@ -1556,8 +1547,7 @@ lib_display_screen_text(const char* text, const char* supertext, const char* sub
 
 void
 lib_display_popup_text(const char* text, const char* title, int style, int attr, int color, int glyph, uint64_t tflags)
-{
-    *_utf8buf = *_titleutf8buf = 0;
+{*_utf8buf = 0; *_titleutf8buf = 0;
     if (text)
         write_text2buf_utf8(_utf8buf, sizeof(_utf8buf), text);
     if (title)
