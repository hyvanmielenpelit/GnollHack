---
name: game_data_files
description: Guide to understanding and modifying GnollHack game data files including level descriptions (.des), dungeon definitions, text databases, and the data compilation pipeline from source text to nhdat archive.
---

# Game Data Files

## Critical Rules
- **Never edit `.lev` files directly**. Edit `.des` files and compile them with `levcomp`.
- **Never edit `nhdat` directly**. It is an archive generated by `dlb`.

## Data Pipeline
1. Edit source files in `dat/` (e.g., `*.des`, `dungeon.pdf`, `data.base`).
2. Run `makedefs` for text databases.
3. Run `levcomp` to compile `.des` into `.lev`.
4. Run `dgncomp` to compile `dungeon.pdf` into `dungeon`.
5. Run `dlb` to package everything into `nhdat`.

## Level Files (`.des`)
- Syntax defines dungeon layouts, rooms, corridors, monsters, and objects.
- Uses a custom scripting language parsed by `levcomp`.
- `MAZE` syntax defines strict grid layouts.
- `ROOM` syntax defines procedurally generated rooms.

## Text Databases
- `data.base`: Monster and object descriptions.
- `rumors.tru` / `rumors.fal`: Fortune cookie text.
- `oracles.txt`: Oracle responses.
- `engrave.txt`: Random engravings found on the floor.
