<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="dirs.props"/>
  <Import Project="files.props"/>
  <!-- TODO not copying record-->
  <Target Name="Build"
    Inputs="$(WslOutDir)include\date.h"
    Outputs="$(LibDroidDir)date.h">
    <Copy SourceFiles="$(WslOutDir)include\date.h" DestinationFolder="$(LibDroidDir)"/>
  </Target>
  <Target Name="Clean">
    <Delete Files="$(LibDroidDir)date.h"/>
  </Target>
</Project>
