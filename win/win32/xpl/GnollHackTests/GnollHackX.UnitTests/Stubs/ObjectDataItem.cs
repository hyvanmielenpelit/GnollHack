namespace GnollHackX
{
    /* Test-side stand-in for the app's ObjectDataItem. GHDrawCommand only stores a
       reference to it, so no members are needed. */
    public sealed class ObjectDataItem
    {
    }
}
